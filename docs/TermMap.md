# HighDimProb Term Map

Note: some declarations exist as scaffolds, but the corresponding concept is not yet stable or processed as part of the v0.1 public API. These rows use `scaffold`, `experimental scaffold`, or `TODO/stable API pending` instead of `implemented`.

| Informal name | Book location or heading | Informal mathematical meaning | Existing Mathlib object | HighDimProb declaration | Target file | Status | Notes |
|---|---|---|---|---|---|---|---|
| Event | General probability vocabulary | Set of outcomes | `Set Ω`, `MeasurableSet` | `Event`, `IsMeasurableEvent` | `HighDimProb/Basic.lean` | implemented | Events remain sets. |
| Probability space | Probability foundations | Measure with total mass one | `Measure Ω`, `IsProbabilityMeasure` | `ProbabilityMeasure`, `IsProbability` | `HighDimProb/ProbabilitySpace.lean` | implemented | Alias only. |
| Finite union bound / Boole inequality | Probability and Measure, finite subadditivity | Measure of a finite union is bounded by the sum of event measures. | `MeasureTheory.measure_biUnion_finset_le` | `measure_biUnion_le` | `HighDimProb/ProbabilitySpace.lean` | proven stable API | Stated for arbitrary measures and finite `Finset`-indexed event families; no measurability assumptions are needed. |
| Random variable | General probability vocabulary | Function from outcomes to values | Function | `RandomVariable`, `RealRandomVariable` | `HighDimProb/RandomVariable.lean` | implemented | No structure. Assumptions are separate. |
| Measurable random variable | General probability vocabulary | Random variable with measurable sample map | `Measurable` | `IsRandomVariable`, `IsRealRandomVariable` | `HighDimProb/RandomVariable.lean` | implemented | Uses `Measurable`, not `AEMeasurable`, in Stage 1A. |
| Distribution/law | General probability vocabulary | Pushforward measure | `Measure.map` | `law`, `realLaw` | `HighDimProb/Distribution.lean` | implemented | Definitional alias. |
| Expectation | General probability vocabulary | Integral of a real random variable | `integral omega, X omega dP` | `expect` | `HighDimProb/Expectation.lean` | implemented | No integrability predicate is bundled. |
| Book statement specification | `# 定理` | Typechecked theorem target represented as a `Prop`, not as a proved theorem | `Prop` | `tailEventMeasurabilityStatement`, `lawMapApplyStatement`, `realLawMapApplyStatement`, `expectAliasStatement`, `tailProbabilityWrapperStatement` | `HighDimProb/BookStatements.lean` | implemented | Stage 1S statement layer; Stage 1B proves the tail-event specification. |
| Lp norm | `Lp范数` | `L^p` seminorm value | `eLpNorm` | `lpNormRandomVariable`, `realLpNorm` | `HighDimProb/Lp.lean` | implemented | Thin wrappers around Mathlib extended seminorm; no new Lp theory. |
| Lp membership | `Lp范数` | Membership/integrability in `L^p` | `MemLp` | `MemLpRandomVariable`, `MemLpRealRandomVariable` | `HighDimProb/Lp.lean` | implemented | Definitional aliases. |
| Integrable random variable | Expectation and moment vocabulary | Integrability under a measure | `Integrable` | `IntegrableRandomVariable`, `IntegrableRealRandomVariable` | `HighDimProb/Lp.lean` | implemented | Kept separate from the expectation alias. |
| Finite moment | Moment and concentration sections | `p`-moment finite | `MemLp` | `HasFiniteMoment`, `momentSeminorm` | `HighDimProb/Moment.lean` | implemented | Moment vocabulary reuses `MemLp` and `eLpNorm`. |
| Natural absolute moment | Concentration moment implications | `ENNReal` integral of `|X|^q` for `q : Nat` | `lintegral`, `ENNReal.ofReal`, natural powers | `absMomentNat`, `finiteAbsMomentNat` | `HighDimProb/Concentration/MomentImplications.lean` | implemented/proven experimental API | Stage G2B proves all natural absolute moments from psi2 subGaussian-tail control with crude factorial constants; Stage G2C bridges nonzero natural exponents to `MemLp` and `realLpNorm`; Stage G2D extracts linear-in-`q` `realLpNorm` growth from the factorial estimate; Stage G2F packages the sharp natural sqrt form. |
| Upper tail event | tail distribution | Event `{omega | t <= X omega}` | Set comprehension | `upperTailEvent` | `HighDimProb/Tail.lean` | implemented | Measurability bridge implemented. |
| Lower tail event | tail distribution | Event `{omega | X omega <= t}` | Set comprehension | `lowerTailEvent` | `HighDimProb/Tail.lean` | implemented | Measurability bridge implemented. |
| Absolute tail event | tail distribution | Event `{omega | t <= |X omega|}` | Set comprehension | `absTailEvent` | `HighDimProb/Tail.lean` | implemented | Measurability bridge implemented. |
| Tail-event measurability | `尾分布` | Tail events are measurable for measurable real random variables | `Measurable`, `measurableSet_Ici`, `measurableSet_Iic`, `continuous_abs.measurable` | `measurableSet_upperTailEvent`, `measurableSet_lowerTailEvent`, `measurableSet_absTailEvent`, `isMeasurableEvent_upperTailEvent`, `isMeasurableEvent_lowerTailEvent`, `isMeasurableEvent_absTailEvent`, `tailEventMeasurabilityStatement_holds` | `HighDimProb/Tail.lean`, `HighDimProb/BookStatements.lean` | implemented | Stage 1B bridge lemmas and statement connector. |
| Tail probability | `尾分布` | Probability of a tail event | Measure application `P s` | `upperTailProb`, `lowerTailProb`, `absTailProb` | `HighDimProb/Tail.lean` | implemented | Thin wrappers; no measurability proof bundled. |
| Tail probability monotonicity | Tail distribution / concentration prerequisites | Tail probabilities are monotone in the threshold with the expected direction. | `measure_mono` | `upperTailProb_antitone`, `lowerTailProb_monotone`, `absTailProb_antitone` | `HighDimProb/Tail.lean` | proven | Stage P5 proof pilot; no measurability assumptions were needed. |
| Tail event inclusions | Tail distribution / concentration prerequisites | Named set inclusions underlying threshold monotonicity. | Set inclusion, order transitivity | `upperTailEvent_subset_of_le`, `lowerTailEvent_subset_of_le`, `absTailEvent_subset_of_le` | `HighDimProb/Concentration/Basic.lean` | proven experimental API | Added as reusable concentration bridges. |
| Nonnegative expectation bridge | Markov inequality prerequisites | Pointwise nonnegative real random variables have nonnegative expectation. | `integral_nonneg_of_ae` | `expect_nonneg_of_nonneg`, `expect_nonneg_of_nonneg_integrable` | `HighDimProb/Concentration/Basic.lean` | proven experimental API | The compatibility form keeps the old integrability argument available; an a.e.-nonnegative wrapper is future API work. |
| Nonnegative lintegral expectation bridge | Markov inequality prerequisites | `lintegral (ENNReal.ofReal o X)` equals `ENNReal.ofReal (expect P X)` for nonnegative integrable `X`. | `ofReal_integral_eq_lintegral_ofReal` | `lintegral_ofReal_eq_ofReal_expect` | `HighDimProb/Concentration/Basic.lean` | proven experimental API | Converts Mathlib's lintegral Markov inequality back to HighDimProb expectation notation. |
| Markov inequality | Classical scalar concentration | `P(X >= a) <= E[X] / a` for nonnegative integrable `X` and `0 < a`. | `MeasureTheory.meas_ge_le_lintegral_div` | `markov_inequality_nonneg`, `markov_inequality` | `HighDimProb/Concentration/Markov.lean` | proven experimental API | Proved as a HighDimProb wrapper using `upperTailProb` and `expect`; the short alias keeps the user-facing API clean. |
| Chebyshev inequality | Classical scalar concentration | `P(|X - E X| >= t) <= Var(X) / t^2` for `L^2` random variables. | `ProbabilityTheory.meas_ge_le_variance_div_sq` | `chebyshev_inequality`, `chebyshev_inequality_prob` | `HighDimProb/Concentration/Chebyshev.lean` | proven experimental API | Uses `absTailProb`, `centered`, and `variance`; Stage G1B adds a probability-measure wrapper. |
| Sample sum | Weak law of large numbers | Finite sum of scalar random variables indexed by `Fin n`. | `Finset.univ.sum` | `sampleSum`, `isRealRandomVariable_sampleSum`, `integrable_sampleSum` | `HighDimProb/LimitTheorems/Basic.lean` | implemented experimental API | No `0 < n` assumption is needed for the definition. |
| Sample mean | Weak law of large numbers | Finite average `(1 / n) * sum i, X i`. | `Finset.univ.sum`, scalar multiplication | `sampleMean`, `isRealRandomVariable_sampleMean`, `integrable_sampleMean` | `HighDimProb/LimitTheorems/Basic.lean` | implemented experimental API | Uses Lean total division at `n = 0`; theorem statements should add positivity when needed. |
| Centered sample mean | Weak law of large numbers via Chebyshev | Sample mean shifted by a target mean `mu`. | subtraction of scalar random variables | `sampleMeanCentered`, `isRealRandomVariable_sampleMeanCentered`, `integrable_sampleMeanCentered` | `HighDimProb/LimitTheorems/Basic.lean` | implemented experimental API | Centered integrability requires finite measure because it subtracts a constant. |
| Weak law Chebyshev bound | Weak law of large numbers via Chebyshev | Deviation probability of sample mean bounded by variance over `n * eps^2`. | `chebyshev_inequality_prob`, future variance-of-sum facts | `weakLawChebyshevBoundStatement` | `HighDimProb/LimitTheorems/WeakLaw.lean` | typed-prop experimental API | Statement only; Stage C1 adds assumption wrappers, but variance-of-sum/sample-mean bridges are missing. |
| Weak law convergence in probability | Weak law of large numbers | Sample means converge in probability to the common mean. | `MeasureTheory.TendstoInMeasure` | `weakLawFiniteVarianceStatement` | `HighDimProb/LimitTheorems/WeakLaw.lean` | typed-prop experimental API | Uses Mathlib convergence-in-measure vocabulary directly; no HighDimProb convergence alias yet. |
| Independent scalar sample | Weak law and independent-sum prerequisites | Indexed scalar random variables are independent. | `ProbabilityTheory.iIndepFun` | `IndependentSample`, `IndependentFinSample`, `IndependentSequence` | `HighDimProb/LimitTheorems/Assumptions.lean` | implemented experimental API | Thin vocabulary only; no variance or covariance theorem bridge is bundled. |
| Pairwise independent finite sample | Weak law variance-of-sum prerequisites | Finite scalar sample has pairwise independent coordinates. | `ProbabilityTheory.IndepFun`, `Set.Pairwise` | `PairwiseIndependentFinSample` | `HighDimProb/LimitTheorems/Assumptions.lean` | implemented experimental API | Designed to line up with Mathlib `IndepFun.variance_sum`. |
| Iid scalar sample | Weak law and iid sample prerequisites | Independence plus identical distribution for scalar sample families. | `ProbabilityTheory.iIndepFun`, `ProbabilityTheory.IdentDistrib` | `IdenticallyDistributedSample`, `IidSample`, `IdenticallyDistributedFinSample`, `IidFinSample`, `IdenticallyDistributedSequence`, `IidSequence` | `HighDimProb/LimitTheorems/Assumptions.lean` | implemented experimental API | Does not bundle common mean, common variance, or measurability assumptions. |
| Orlicz function | `Orilicz函数` | Convex gauge function, kept as vocabulary first | No dedicated Mathlib Orlicz API found | `OrliczFunction`, `psiPower`, `psi1Function`, `psi2Function` | `HighDimProb/Orlicz.lean` | implemented | Structural assumptions are still separate. |
| Orlicz bound | `Orilicz范数` | Predicate-level Luxemburg-style bound at scale `K` | `lintegral`, `ENNReal.ofReal` | `OrliczBound` | `HighDimProb/Orlicz.lean` | implemented | Uses nonnegative integral formulation; not a norm. |
| psi2 bound | subGaussian norm, Orlicz norm | Exponential-square Orlicz control at scale `K` | `lintegral`, `Real.exp`, `ENNReal.ofReal` | `Psi2Bound`, `HasFinitePsi2` | `HighDimProb/Orlicz.lean` | implemented | Not named subGaussian; equivalence theorem deferred. |
| psi1 bound | subExponential norm, Orlicz norm | Exponential-linear Orlicz control at scale `K` | `lintegral`, `Real.exp`, `ENNReal.ofReal` | `Psi1Bound`, `HasFinitePsi1` | `HighDimProb/Orlicz.lean` | implemented | Not named subExponential; equivalence theorem deferred. |
| psi2 exponential moment bridge | subGaussian norm | Shifted psi2 Orlicz bound implies `E exp((|X|/K)^2) <= 2` in `lintegral` form under a probability measure. | `lintegral`, `lintegral_add_right`, `IsProbabilityMeasure.measure_univ` | `lintegral_exp_sq_div_le_two_of_psi2Bound` | `HighDimProb/Concentration/OrliczToTail.lean` | proven experimental API | Uses the shifted `exp - 1` definition; requires probability mass one. |
| psi1 exponential moment bridge | subExponential norm | Shifted psi1 Orlicz bound implies `E exp(|X|/K) <= 2` in `lintegral` form under a probability measure. | `lintegral`, `lintegral_add_right`, `IsProbabilityMeasure.measure_univ` | `lintegral_exp_abs_div_le_two_of_psi1Bound` | `HighDimProb/Concentration/OrliczToTail.lean` | proven experimental API | Uses the shifted `exp - 1` definition; requires probability mass one. |
| psi2 to-tail implication | subGaussian property, subGaussian norm | psi2 Orlicz control implies the two-sided subGaussian tail predicate with matching scale. | `MeasureTheory.meas_ge_le_lintegral_div`, `Real.exp_le_exp`, `pow_le_pow_left0`, `ENNReal.ofReal_div_of_pos` | `subGaussianTail_of_psi2Bound` | `HighDimProb/Concentration/OrliczToTail.lean` | proven experimental API | Requires `IsRealRandomVariable P X` because `Psi2Bound` does not bundle measurability. |
| psi1 to-tail implication | subExponential property, subExponential norm | psi1 Orlicz control implies the two-sided subExponential tail predicate with matching scale. | `MeasureTheory.meas_ge_le_lintegral_div`, `Real.exp_le_exp`, `ENNReal.ofReal_div_of_pos` | `subExponentialTail_of_psi1Bound` | `HighDimProb/Concentration/OrliczToTail.lean` | proven experimental API | Requires `IsRealRandomVariable P X` because `Psi1Bound` does not bundle measurability. |
| Tail-to-psi2 implication | subGaussian Orlicz characterization | SubGaussian tail control implies shifted exponential-square Orlicz control with scale `2 * K`. | Mathlib layer-cake formula and improper exponential integral | `psi2Bound_of_subGaussianTail` | `HighDimProb/Concentration/TailToOrlicz.lean` | proven experimental API | Stage G1D-fix proves the fixed-scale reverse implication using a nonnegative-tail calculus bridge. |
| Tail-to-psi1 implication | subExponential Orlicz characterization | SubExponential tail control implies shifted exponential-linear Orlicz control with scale `3 * K`. | Mathlib layer-cake formula and improper exponential integral | `psi1Bound_of_subExponentialTail` | `HighDimProb/Concentration/TailToOrlicz.lean` | proven experimental API | Stage S2.1 proves the fixed-scale reverse implication using the same nonnegative-tail calculus bridge pattern as psi2. |
| Tail-to-natural/full-moment bridge | subGaussian moment characterization | SubGaussian tail control implies all natural absolute moments are finite with a crude factorial quantitative bound; psi2 control gives sharp natural `sqrt(q)` growth, and the natural theorem now promotes to the full finite-`ENNReal` `SubGaussianMoment` interface. | `Psi2Bound`, `psi2Bound_of_subGaussianTail`, `Real.pow_div_factorial_le_exp`, `lintegral_exp_sq_div_le_two_of_psi2Bound`, deterministic real inequalities, `eLpNorm_le_eLpNorm_of_exponent_le` | `abs_pow_le_exp_sq_factorial`, `absMomentNat_le_of_psi2Bound`, `finiteAbsMomentNat_of_psi2Bound`, `absMomentNat_le_of_subGaussianTail`, `finiteAbsMomentNat_of_subGaussianTail`, `absMomentNat_le_sqrt_growth_of_psi2Bound`, `SubGaussianMomentNat`, `SubGaussianMomentNatSqrt`, `subGaussianMomentNat_of_psi2Bound`, `subGaussianMomentNat_of_subGaussianTail`, `subGaussianMomentNatSqrt_of_psi2Bound`, `subGaussianMomentNatSqrt_of_subGaussianTail`, `subGaussianMoment_of_psi2Bound`, `subGaussianMoment_of_subGaussianTail`, `subGaussianMomentNatOfSubGaussianTailStatement` | `HighDimProb/Concentration/MomentImplications.lean` | proven experimental API | Factorial bound is `2 * exp(1/4) * K^q * q!`; sharp psi2 bound is `((4*K*sqrt q)^q)` for `q >= 1`; the sharp natural predicate uses scales `4*K`/`8*K`; full `SubGaussianMoment` uses scales `8*K`/`16*K`. |
| Natural moment to Lp bridge | `Lp范数`, subGaussian and subExponential moment characterizations | Finite or bounded `absMomentNat` gives `MemLp` and `realLpNorm` control at the matching nonzero natural exponent; natural sqrt or linear growth is promoted to every finite `p : ENNReal` by monotonicity to `ceil(p.toReal)`. | `MemLp`, `eLpNorm`, `eLpNorm_eq_lintegral_rpow_enorm_toReal`, `eLpNorm_lt_top_iff_lintegral_rpow_enorm_lt_top`, `eLpNorm_le_eLpNorm_of_exponent_le`, `ENNReal.toReal`, `Nat.ceil` | `lintegral_enorm_rpow_nat_eq_absMomentNat`, `memLp_of_finiteAbsMomentNat`, `memLp_one_of_finiteAbsMomentNat_one`, `memLp_two_of_finiteAbsMomentNat_two`, `realLpNorm_nat_le_of_absMomentNat_le_ennreal`, `realLpNorm_nat_le_of_absMomentNat_le`, `realLpNorm_nat_le_linear_of_psi2Bound`, `realLpNorm_nat_le_linear_of_subGaussianTail`, `realLpNorm_nat_le_linear_of_psi1Bound`, `realLpNorm_nat_le_linear_of_subExponentialTail`, `realLpNorm_nat_le_sqrt_of_psi2Bound`, `realLpNorm_nat_le_sqrt_of_subGaussianTail`, `realLpNorm_le_natCeil_of_realExponent`, `sqrt_natCeil_toReal_le_two_sqrt`, `natCeil_toReal_le_two_mul_toReal`, `realLpNorm_le_sqrt_of_psi2Bound`, `realLpNorm_le_sqrt_of_subGaussianTail`, `realLpNorm_le_linear_of_psi1Bound`, `realLpNorm_le_linear_of_subExponentialTail`, `SubGaussianMomentNatSqrt`, `subGaussianMomentNatSqrt_of_psi2Bound`, `subGaussianMomentNatSqrt_of_subGaussianTail`, `sqrtMomentGrowthOfPsi2`, `sqrtMomentGrowthOfSubGaussianTail` | `HighDimProb/Concentration/MomentImplications.lean` | proven experimental API | Requires `q != 0` for the generic natural bridge and `1 <= p`, `p != ∞` for the full real/`ENNReal` bridge; full subGaussian constants are `8` for psi2 and `16` for tails, while subExponential constants are `16` for psi1 and `48` for tails. |
| Deterministic power-exponential inequality | Real-analysis helper for sharp moment growth | Natural powers are dominated by a Gaussian exponential envelope. | `Real.log`, `Real.exp`, `Real.sqrt`, natural powers | `log_le_sq_of_nonneg`, `pow_le_exp_nat_mul_sq`, `pow_le_two_sqrt_mul_exp_sq`, `pow_le_four_sqrt_mul_exp_sq` | `HighDimProb/Analysis/RealInequalities.lean` | proven helper API | Stage G2E-fix proves the constant-`2` envelope and keeps a constant-`4` compatibility wrapper. |
| Layer-cake bridge | Tail-to-Orlicz prerequisites | Nonnegative real lintegrals can be represented by integrating tail measures. | `MeasureTheory.lintegral_eq_lintegral_meas_le` | `lintegral_ofReal_eq_lintegral_tail` | `HighDimProb/Concentration/TailToOrlicz.lean` | proven experimental API | This is the reusable entry point for future tail-to-Orlicz and moment-from-tail proofs. |
| Layer-cake import boundary | Tail-to-Orlicz prerequisites | Reusable layer-cake and exponential-tail calculus helpers are available through a focused concentration leaf. | import re-export | `HighDimProb.Concentration.LayerCake` | `HighDimProb/Concentration/LayerCake.lean` | implemented experimental API | Stage C1 keeps theorem bodies stable in `TailToOrlicz` while adding a cleaner public import path. |
| Exponential tail calculus bridge | Tail-to-Orlicz prerequisites | A nonnegative `Z` with tail `P(Z >= s) <= 2 exp(-s)` satisfies `E[exp(Z/4)-1] <= 1` in `lintegral` form. | Mathlib layer-cake formula and `integral_exp_mul_Ioi` | `lintegral_exp_quarter_sub_one_le_of_exp_tail` | `HighDimProb/Concentration/TailToOrlicz.lean` | proven experimental API | This is the abstract helper used to prove the psi2 reverse implication. |
| Orlicz norm/gauge | `Orilicz范数` | Infimum over scales satisfying an Orlicz bound | TODO | TODO | `HighDimProb/Orlicz.lean` | TODO/stable API pending | Full infimum/gauge object deferred until properties are needed. |
| Orlicz space | `Orilicz空间` | Space with finite Orlicz norm | TODO | TODO | `HighDimProb/Orlicz.lean` | TODO | Future abstraction. |
| SubGaussian tail formulation | `次高斯性质` | Two-sided exponential-square tail decay | `Measure`, `ENNReal.ofReal`, `Real.exp` | `SubGaussianTail` | `HighDimProb/SubGaussian.lean` | implemented | Uses `absTailProb`; no equivalence theorem. |
| SubGaussian moment formulation | `次高斯性质` | `L^p` growth bounded by `K sqrt p` | `eLpNorm`, `ENNReal` exponents | `SubGaussianMoment`, `subGaussianMoment_of_psi2Bound`, `subGaussianMoment_of_subGaussianTail` | `HighDimProb/SubGaussian.lean`, `HighDimProb/Concentration/MomentImplications.lean` | implemented and proved from psi2/tail inputs | Uses Mathlib `ENNReal` exponent type and excludes `∞`; the all-finite-real-exponent connector is proved from psi2 with scale `8*K` and from tails with scale `16*K`. |
| Natural subGaussian moment formulation | `次高斯性质` | Natural-exponent moment control proved from psi2/tail hypotheses, with both factorial and sharp real-Lp consequences. | `absMomentNat`, `Nat.factorial`, `ENNReal.ofReal`, `realLpNorm` | `SubGaussianMomentNat`, `SubGaussianMomentNatSqrt`, `subGaussianMomentNatSqrt_of_psi2Bound`, `subGaussianMomentNatSqrt_of_subGaussianTail`, `absMomentNat_le_sqrt_growth_of_psi2Bound`, `realLpNorm_nat_le_sqrt_of_psi2Bound`, `realLpNorm_nat_le_sqrt_of_subGaussianTail`, `realLpNorm_le_sqrt_of_psi2Bound`, `realLpNorm_le_sqrt_of_subGaussianTail` | `HighDimProb/Concentration/MomentImplications.lean` | proven experimental API | `SubGaussianMomentNat` retains the factorial normal form; `SubGaussianMomentNatSqrt` records the sharp natural real-Lp form with constants `4` for psi2 and `8` for tails; the full bridge doubles those scales through the ceiling comparison. |
| SubGaussian MGF formulation | subGaussian property | Centered MGF-style control | `ProbabilityTheory.HasSubgaussianMGF`, `lintegral`, `MeasureTheory.meas_ge_le_lintegral_div` | `CenteredSubGaussianMGF`, `CenteredSubGaussianMGFLIntegral`, `centeredSubGaussianMGFLIntegral_of_centeredSubGaussianMGF`, `upperTailProb_le_exp_neg_sq_of_centeredSubGaussianMGF`, `lowerTailProb_le_exp_neg_sq_of_centeredSubGaussianMGF`, `subGaussianTail_of_centeredSubGaussianMGF`, `psi2Bound_of_centeredSubGaussianMGF`, `subGaussianMomentNatSqrt_of_centeredSubGaussianMGF` | `HighDimProb/SubGaussian.lean`, `HighDimProb/Concentration/MGF.lean` | proven forward implication experimental API | `CenteredSubGaussianMGF` remains the Mathlib-backed predicate; the lintegral form supports Chernoff proofs. Forward MGF-to-tail/psi2 natural-moment links are proved with scales `2*K`, `4*K`, and `16*K`; reverse MGF links remain future work. |
| Independent finite subGaussian sum | Hoeffding prerequisites / subGaussian sums | Independent centered subGaussian variables remain centered subGaussian under finite unweighted and weighted sums. | `ProbabilityTheory.iIndepFun`, `ProbabilityTheory.HasSubgaussianMGF.sum_of_iIndepFun`, `ProbabilityTheory.HasSubgaussianMGF.const_mul` | `centeredSubGaussianMGF_sum_of_iIndepFun_of_pos`, `centeredSubGaussianMGF_weighted_sum_of_iIndepFun_of_pos`, `subGaussianTail_sum_of_iIndepFun_of_pos`, `subGaussianTail_weighted_sum_of_iIndepFun_of_pos` | `HighDimProb/Concentration/SubGaussianSums.lean` | proven experimental API | General finite index type wrappers use `[Fintype ι]`; Finset helper theorems are also exposed. Scales are `sqrt (sum_i K_i^2)` and `sqrt (sum_i (a_i*K_i)^2)`, with positive proxy-sum assumptions because current predicates require positive scales. |
| Bounded Hoeffding | Hoeffding inequality / bounded variable MGF | A centered real variable bounded a.e. in `[a,b]` is subGaussian; independent finite bounded centered sums have conservative and sharp Hoeffding tails; independent finite non-centered bounded sums have the sharp Wikipedia-form tail around `E[sum_i X_i]`; deterministic weighted sums have the sharp weighted denominator. | `ProbabilityTheory.hasSubgaussianMGF_of_mem_Icc_of_integral_eq_zero`, `ProbabilityTheory.iIndepFun`, `ProbabilityTheory.iIndepFun.comp`, `MeasureTheory.integral_finset_sum`, Stage H5 finite-sum MGF/tail wrappers, local eighth-MGF Chernoff optimization | `centeredSubGaussianMGF_of_ae_mem_Icc_of_centered`, `centeredSubGaussianMGF_of_forall_mem_Icc_of_centered`, `centeredSubGaussianMGF_sum_of_iIndepFun_bounded_centered`, `subGaussianTail_sum_of_iIndepFun_bounded_centered`, `hoeffding_sum_bounded_centered`, `upperTailProb_le_exp_neg_two_mul_sq_div_of_mgf_eighth`, `lowerTailProb_le_exp_neg_two_mul_sq_div_of_mgf_eighth`, `absTailProb_le_two_mul_exp_neg_two_mul_sq_div_of_mgf_eighth`, `hoeffding_sum_bounded_centered_sharp`, `expect_finset_sum`, `iIndepFun_centered_of_iIndepFun`, `ae_mem_Icc_centered_of_ae_mem_Icc`, `sum_centered_eq_sum_sub_expect_sum`, `sum_weighted_centered_eq_weighted_sum_sub_expect_weighted_sum`, `hoeffding_sum_bounded`, `hoeffding_weighted_sum_bounded_centered_sharp`, `hoeffding_weighted_sum_bounded` | `HighDimProb/Concentration/Hoeffding.lean` | proven experimental API | One-variable scale is `(b-a)/2`; the old explicit centered theorem keeps the conservative exponent `-t^2 / sum_i (b_i-a_i)^2`; the sharp centered and non-centered theorems match the classical/Wikipedia exponent `-2*t^2 / sum_i (b_i-a_i)^2`; weighted centered and non-centered theorems use denominator `sum_i c_i^2 * (b_i-a_i)^2`. Negative weights are handled by the weighted MGF proxy. |
| Hoeffding milestone | Hoeffding inequality closeout | Documentation milestone for the finite Hoeffding theorem family, including constants, assumptions, import exposure, retained conservative theorem rationale, and remaining work. | documentation/import audit | `docs/HoeffdingMilestone.md` | docs only | complete | Stage H8 records weighted bounded Hoeffding as proved; the broader scalar closeout records this family in the scalar theorem index. |
| Scalar implication aggregate | scalar concentration proof spine | Single import for proved scalar tail, Orlicz, natural-moment, MGF, independent-sum, Rademacher, and Hoeffding implication arrows. | import re-export | `HighDimProb.Concentration.Implications` | `HighDimProb/Concentration/Implications.lean` | proven experimental aggregate | Stage H7 keeps this as the discoverable user-facing import for the scalar implication graph; theorem bodies remain in their owning leaves. |
| Canonical Rademacher variable | Rademacher variables, Hoeffding prerequisites | Symmetric sign variable on `Bool` with probability `1/2` at each sign. | `PMF.bernoulli`, `PMF.toMeasure`, `PMF.integral_eq_sum`, Mathlib bounded-zero-mean MGF lemma | `rademacherPMF`, `rademacherMeasure`, `rademacher`, `isRealRandomVariable_rademacher`, `rademacher_mem_Icc`, `integral_rademacher`, `centeredSubGaussianMGF_rademacher`, `subGaussianTail_rademacher` | `HighDimProb/Distributions/Rademacher.lean` | proven experimental API | Stage H0 audits the Bool uniform/Bernoulli representation. MGF scale is `1`; tail scale is `2` by composition. |
| Finite Rademacher product family | Rademacher variables, Hoeffding prerequisites | Independent coordinate signs indexed by `Fin n` on the product space `Fin n -> Bool`. | `Measure.pi`, `Measure.toPMF`, `Measure.toPMF_toMeasure`, `measurePreserving_eval`, `ProbabilityTheory.iIndepFun_pi` | `rademacherVectorMeasure`, `rademacherVectorPMF`, `rademacherCoord`, `rademacherVector`, `isRealRandomVariable_rademacherCoord`, `rademacherCoord_mem_Icc`, `integral_rademacherCoord`, `iIndepFun_rademacherCoord` | `HighDimProb/Distributions/RademacherFamily.lean` | proven experimental API | Stage H2A chooses product measure as the primary proof object; the PMF is induced from the product probability measure. |
| Weighted Rademacher sum | Rademacher variables, Hoeffding prerequisites | Finite weighted sign sum `sum_i a_i eps_i` with subGaussian MGF proxy `sum_i a_i^2`, Hoeffding tails, and zero-weight helpers. | `ProbabilityTheory.HasSubgaussianMGF.sum_of_iIndepFun`, `subGaussianTail_of_centeredSubGaussianMGF`, `Finset.sum_eq_zero_iff_of_nonneg`, finite sums | `weightedRademacherSum`, `isRealRandomVariable_weightedRademacherSum`, `hasSubgaussianMGF_weightedRademacherSum`, `centeredSubGaussianMGF_weightedRademacherSum`, `subGaussianTail_weightedRademacherSum`, `hoeffding_rademacher_sum`, `weightedRademacherSum_eq_zero_of_sum_sq_eq_zero`, `absTailProb_weightedRademacherSum_eq_zero_of_sum_sq_eq_zero_of_pos`, `hoeffding_rademacher_sum_of_pos_variance` | `HighDimProb/Concentration/RademacherSums.lean` | proven experimental API | Positive-square-sum theorems keep scale `2 * sqrt (sum_i a_i^2)` and denominator `4 * sum_i a_i^2`; zero-square-sum helper covers positive thresholds directly because scale `0` is not accepted by current predicates. |
| Rademacher/Hoeffding milestone | Rademacher variables, Hoeffding prerequisites | Closeout summary for the finite Rademacher concentration mini-domain. | documentation/import audit | `docs/RademacherMilestone.md` | docs only | complete | Stage H4 records import boundaries, constants, test coverage, blockers, and the next theorem family. |
| SubGaussian Orlicz formulation | subGaussian norm, Orlicz norm | psi2 Orlicz control | `Psi2Bound`, `HasFinitePsi2` | `SubGaussianOrlicz`, `HasSubGaussianOrlicz` | `HighDimProb/SubGaussian.lean` | implemented | Definitional wrapper around Stage 2B psi2 predicates. |
| Canonical subGaussian predicate | `次高斯随机变量` | A single selected definition of subGaussian | TODO / Mathlib components | TODO | `HighDimProb/SubGaussian.lean` | TODO/stable API pending | Deferred until formulation choices and equivalence goals are clearer. |
| SubGaussian norm | `次高斯范数` | Minimal subGaussian scale | TODO | TODO | `HighDimProb/SubGaussian.lean` | TODO | Avoid minimization now. |
| SubExponential tail formulation | `次指数性质` | Two-sided exponential-linear tail decay | `Measure`, `ENNReal.ofReal`, `Real.exp` | `SubExponentialTail` | `HighDimProb/SubExponential.lean` | implemented | Uses `absTailProb`; no equivalence theorem. |
| SubExponential moment formulation | `次指数性质` | `L^p` growth bounded by `K p` | `eLpNorm`, `ENNReal` exponents | `SubExponentialMoment`, `absMomentNat_le_of_psi1Bound`, `realLpNorm_nat_le_linear_of_psi1Bound`, `realLpNorm_le_linear_of_psi1Bound`, `subExponentialMoment_of_psi1Bound`, `subExponentialMoment_of_subExponentialTail` | `HighDimProb/SubExponential.lean`, `HighDimProb/Concentration/MomentImplications.lean` | implemented and proved from psi1/tail inputs | Uses Mathlib `ENNReal` exponent type and excludes `∞`; Stage M-real-2 proves `Psi1Bound -> SubExponentialMoment (16*K)` and `SubExponentialTail -> SubExponentialMoment (48*K)`. |
| SubExponential MGF formulation | subExponential property | Centered local-MGF control for `|lambda| <= 1 / K` | `integral`, `Real.exp` | `CenteredSubExponentialMGF` | `HighDimProb/SubExponential.lean` | implemented | Raw expectation predicate; no centering or integrability theorem. |
| SubExponential Orlicz formulation | subExponential norm, Orlicz norm | psi1 Orlicz control | `Psi1Bound`, `HasFinitePsi1` | `SubExponentialOrlicz`, `HasSubExponentialOrlicz` | `HighDimProb/SubExponential.lean` | implemented | Definitional wrapper around Stage 2B psi1 predicates. |
| Canonical subExponential predicate | `次指数随机变量` | A single selected definition of subExponential | TODO / Mathlib components | TODO | `HighDimProb/SubExponential.lean` | TODO/stable API pending | Deferred until formulation choices and equivalence goals are clearer. |
| SubExponential norm | `次指数范数` | Minimal subExponential scale | TODO | TODO | `HighDimProb/SubExponential.lean` | TODO | Avoid minimization now. |
| Random vector | High-dimensional distribution sections | Finite-dimensional random vector | Function to finite vector, `EuclideanSpace` | `RandomVector`, `IsRandomVector` | `HighDimProb/RandomVector.lean` | implemented experimental API | Available through `HighDimProb.Experimental`; concrete representation is `Omega -> Fin n -> Real`. |
| Coordinate random variable | Random vector coordinates | Scalar coordinate map `omega -> X omega i` | Function application | `coord`, `coordinate`, `isRealRandomVariable_coord`, `isRealRandomVariable_coordinate` | `HighDimProb/RandomVector.lean` | implemented experimental API | `coordinate` is retained as compatibility vocabulary. |
| One-dimensional marginal | high-dimensional subGaussian distribution, one-dimensional marginals `<X,x>` | Finite linear scalar marginal of a random vector | `Finset.sum`, pointwise scalar multiplication | `linearForm`, `marginal`, `isRealRandomVariable_linearForm`, `isRealRandomVariable_marginal` | `HighDimProb/RandomVector.lean` | implemented experimental API | Uses coefficient vectors `a : Fin n -> Real`; no inner-product abstraction yet. |
| Squared norm random variable | Norm concentration sections | `omega -> sum_i (X omega i)^2` | `Finset.sum`, powers on `Real` | `sqNorm`, `isRealRandomVariable_sqNorm` | `HighDimProb/RandomVector.lean` | implemented experimental API | No norm concentration theorem. |
| Norm random variable | Norm concentration sections | `omega -> sqrt (sqNorm X omega)` | `Real.sqrt` | `euclideanNorm`, `isRealRandomVariable_euclideanNorm` | `HighDimProb/RandomVector.lean` | implemented experimental API | Concrete Euclidean norm vocabulary only. |
| Directional Euclidean scale | subGaussian norm, `sup_{x in S^{n-1}}` | Concrete coefficient length and scale `K ||a||_2` | `Finset.sum`, `Real.sqrt` | `directionNorm`, `directionScale` | `HighDimProb/SubGaussianVector.lean` | implemented experimental API | Concrete expression `Real.sqrt (sum_i (a i)^2)`; no unit-sphere abstraction yet. |
| High-dimensional subGaussian vector | `高维次高斯分布` | All one-dimensional marginals are subGaussian with directional scaling | `linearForm`, `marginal`, scalar subGaussian predicates | `SubGaussianVectorOrlicz`, `HasSubGaussianVectorOrlicz`, `SubGaussianVectorTail`, `SubGaussianVectorMoment`, `CenteredSubGaussianVectorMGF` | `HighDimProb/SubGaussianVector.lean` | implemented experimental API | Predicate forms remain separate; no canonical `SubGaussianVector` yet. |
| Orlicz subGaussian vector | high-dimensional subGaussian distribution, subGaussian norm | Every nonzero directional marginal is psi2 controlled at scale `K ||a||_2` | `SubGaussianOrlicz`, `Real.sqrt`, `Finset.sum` | `SubGaussianVectorOrlicz`, `HasSubGaussianVectorOrlicz` | `HighDimProb/SubGaussianVector.lean` | implemented experimental API | Zero direction is excluded because scalar psi2 bounds require positive scale. |
| Tail subGaussian vector formulation | `高维次高斯分布` | Directional marginals satisfy the scalar tail formulation | `SubGaussianTail` | `SubGaussianVectorTail` | `HighDimProb/SubGaussianVector.lean` | implemented experimental API | No equivalence theorem with Orlicz form. |
| Moment subGaussian vector formulation | `高维次高斯分布` | Directional marginals satisfy the scalar moment formulation | `SubGaussianMoment` | `SubGaussianVectorMoment` | `HighDimProb/SubGaussianVector.lean` | implemented experimental API | Uses scalar `ENNReal` exponent API. |
| MGF subGaussian vector formulation | `高维次高斯分布` | Directional marginals satisfy the centered scalar MGF formulation | `ProbabilityTheory.HasSubgaussianMGF` via `CenteredSubGaussianMGF` | `CenteredSubGaussianVectorMGF` | `HighDimProb/SubGaussianVector.lean` | implemented experimental API | Does not prove centering of marginals. |
| Unit-sphere subGaussian vector formulation | `||X||_psi2 = sup_{x in S^{n-1}} ||<X,x>||_psi2` | Supremum over unit directions | Mathlib sphere / norm APIs | TODO | `HighDimProb/SubGaussianVector.lean` | TODO | Deferred until unit sphere and psi2 gauge/norm vocabulary are stable. |
| Mean | `高维空间的协方差矩阵`, `中心化` | Expectation of a scalar random variable | `∫`, Mathlib probability expectation notation | `mean` | `HighDimProb/Scalar/Centering.lean` | implemented stable scalar API | Thin wrapper around `expect`; no integrability assumption bundled. |
| Centered random variable | `中心化` | Mean-zero scalar random variable, plus centered version `X - E X` | `∫`, `Measurable.sub`, `Integrable`, probability mass one | `centered`, `Centered`, `centered_centered`, `isRealRandomVariable_centered`, `integrable_centered` | `HighDimProb/Scalar/Centering.lean` | proven stable scalar API | Stage G1B moves scalar centering out of vector covariance and keeps compatibility through `HighDimProb.Covariance`. |
| Variance | Classical probability inequalities | Scalar variance | `ProbabilityTheory.variance` | `variance` | `HighDimProb/Scalar/Variance.lean` | implemented stable scalar API | Alias only; theorem wrappers live in the experimental concentration branch. |
| Scalar covariance | `高维空间的协方差矩阵`, `随机过程的协方差` | Centered second moment of two scalar random variables | `ProbabilityTheory.covariance` | `covariance` | `HighDimProb/Scalar/Variance.lean` | implemented stable scalar API | Alias only; algebraic identities remain theorem-layer work. |
| Scalar second moment | `二阶矩矩阵` | Uncentered `E[X Y]` | `expect`, multiplication | `secondMoment` | `HighDimProb/Scalar/Variance.lean` | implemented stable scalar API | Entry-level vocabulary for second moment matrices; vector matrix entries remain in `HighDimProb/Covariance.lean`. |
| Mean vector | `mu = E X` | Coordinatewise mean of a random vector | Function `Fin n -> Real` | `meanVector` | `HighDimProb/Covariance.lean` | implemented experimental API | Uses `coord`. |
| Centered random vector | `中心化`, `Z = Σ^{-1/2}(X-μ)` | Coordinatewise centered vector and predicate | Coordinate functions | `centeredVector`, `CenteredVector`, `centeredVector_iff_forall_centered_coord`, `isRandomVector_centeredVector` | `HighDimProb/Covariance.lean` | proven experimental API | Stage P3 proves the predicate bridge; whitening and covariance inverse are not implemented. |
| Second moment matrix | `二阶矩矩阵` | Entrywise `E[X_i X_j]` | `Matrix (Fin n) (Fin n) ℝ` | `secondMomentMatrixEntry`, `secondMomentMatrix` | `HighDimProb/Covariance.lean` | implemented experimental API | No PSD/symmetry theorem. |
| Covariance matrix | `高维空间的协方差矩阵` | Entrywise covariance matrix | `Matrix (Fin n) (Fin n) ℝ`, `ProbabilityTheory.covariance` | `covarianceMatrixEntry`, `covarianceMatrix` | `HighDimProb/Covariance.lean` | implemented experimental API | No identity `E[XXᵀ] - μμᵀ` theorem yet. |
| Isotropic random vector | `各向同性` | Random vector with identity second moment or centered identity covariance | `Matrix (Fin n) (Fin n) ℝ`, `Matrix.one_apply` | `IsotropicSecondMoment`, `IsotropicCovariance`, `IsIsotropic` | `HighDimProb/Isotropic.lean` | implemented experimental API | `IsIsotropic` is a compatibility alias for `IsotropicCovariance`; no canonical `Isotropic` predicate yet. |
| Second-moment isotropic formulation | `Σ(X)=E XX^T=I_n` | Entrywise second moment matrix equals identity | `Matrix`, identity matrix, `Matrix.ext`, `Matrix.one_apply` | `IsotropicSecondMoment`, `IsotropicSecondMomentMatrix`, `isotropicSecondMomentMatrix_iff_isotropicSecondMoment` | `HighDimProb/Isotropic.lean` | proven experimental API | Stage P2 proves the matrix/entrywise bridge; covariance and marginal equivalences remain separate theorem work. |
| Covariance isotropic formulation | `cov(X)=I`, centered isotropic vector | Centered coordinates and covariance matrix equals identity | `ProbabilityTheory.covariance`, `Matrix` | `IsotropicCovariance` | `HighDimProb/Isotropic.lean` | implemented experimental API | Kept separate from second-moment isotropicity; no equivalence theorem. |
| Marginal isotropic formulation | `E <X,x>^2 = ||x||_2^2` | Every finite linear marginal has second moment equal to squared coefficient norm | `Finset.sum`, `linearForm`, `secondMoment` | `IsotropicMarginal` | `HighDimProb/Isotropic.lean` | implemented experimental API | Concrete coefficient-vector formulation over `Fin n -> Real`. |
| Isotropic equivalence theorem | `各向同性` characterizations | Equivalence of second-moment, covariance, and marginal formulations | TODO | TODO | `HighDimProb/Isotropic.lean` | TODO | Theorem-layer work; not proved in Stage 4C. |
| Epsilon net | `$\\varepsilon$-网` | `N` covers `K` by metric balls of radius `ε` | `Metric.IsCover` | `IsEpsilonNet`, `IsInternalEpsilonNet`, `epsilonRadius` | `HighDimProb/Nets.lean` | implemented Mathlib-backed experimental API | Real radii use `Real.toNNReal`; `IsEpsilonNet` allows external centers, while `IsInternalEpsilonNet` requires centers in `K`. |
| Separated set | `ε-分离的` | Distinct points are farther than `ε` apart | `Metric.IsSeparated` | `IsEpsilonSeparated`, `epsilonERadius` | `HighDimProb/Nets.lean` | implemented Mathlib-backed experimental API | Real radii use `Real.toNNReal`, coerced to `ℝ≥0∞`. |
| Covering number | `覆盖数` | Minimal internal cover cardinality | `Metric.coveringNumber` | `coveringNumber` | `HighDimProb/MetricEntropy.lean` | implemented Mathlib-backed experimental API | Returns Mathlib `ℕ∞`; no custom minimization. |
| External covering number | `覆盖数` | Minimal cover cardinality with centers not constrained to lie in `K` | `Metric.externalCoveringNumber` | `externalCoveringNumber` | `HighDimProb/MetricEntropy.lean` | implemented Mathlib-backed experimental API | Returns Mathlib `ℕ∞`. |
| Packing number | `填充数` | Maximal separated subset cardinality | `Metric.packingNumber` | `packingNumber` | `HighDimProb/MetricEntropy.lean` | implemented Mathlib-backed experimental API | Returns Mathlib `ℕ∞`; no custom maximization. |
| Maximal epsilon-separated in a set | separated set and net | A subset `N subset K` is epsilon-separated and cannot be enlarged by inserting one point of `K` | `Metric.IsSeparated`, `Metric.isSeparated_insert_of_notMem` | `MaximalEpsilonSeparatedIn` | `HighDimProb/Nets.lean` | implemented experimental API | Single-point maximality predicate used for the Stage P1 proof pilot. |
| Maximal separated set is a net | `分离集与网` | A maximal epsilon-separated subset of `K` covers `K` at radius epsilon | `Metric.IsCover`, `Metric.IsSeparated`, `Metric.isSeparated_insert_of_notMem` | `isInternalEpsilonNet_of_maximalEpsilonSeparatedIn`, `maximalSeparatedNetStatement` | `HighDimProb/Nets.lean`, `HighDimProb/MetricEntropyStatements.lean` | proven experimental API | Stage P1 proves the single-point maximality version; global/cardinality variants remain optional. |
| Epsilon-net cardinality bound | `覆盖数` | An internal epsilon-net gives an upper bound for the covering number | `Metric.IsCover.coveringNumber_le_encard` | `epsilonNetCoveringNumberStatement` | `HighDimProb/MetricEntropyStatements.lean` | typed-prop experimental API | Statement only; future proof needs real-radius bridge lemmas. |
| Packing-covering inequalities | `覆盖数与填充数` | `P(K, 2 eps) <= N(K, eps) <= P(K, eps)` | `Metric.packingNumber_two_mul_le_externalCoveringNumber`, `Metric.coveringNumber_le_packingNumber` | `packingCoveringInequalityStatement` | `HighDimProb/MetricEntropyStatements.lean` | typed-prop experimental API | Statement only; proof wrapper deferred. |
| Euclidean ball covering bounds | `欧几里得球的覆盖数` | Volume-scale bounds for covering Euclidean balls | TODO / Mathlib Euclidean geometry APIs | TODO | `HighDimProb/MetricEntropy.lean` | blocked theorem TODO | Needs Euclidean ball, volume, and finite/infinite cardinal conventions. |
| Hamming cube covering and packing | `汉明立方体的覆盖数和填充数` | Covering and packing estimates for binary cubes | TODO / finite metric cube APIs | TODO | `HighDimProb/MetricEntropy.lean` | blocked theorem TODO | Needs Hamming cube and Hamming metric vocabulary. |
| Epsilon-net operator norm bound | `$\\varepsilon$-网与算子范数` | Operator norm controlled by suprema over an epsilon-net | `IsEpsilonNet`, `Matrix.Norms.L2Operator` | `epsilonNetOperatorNormStatement` | `HighDimProb/RandomMatrix/Statements.lean` | typed-prop experimental API | Stage S2.4 adds the deterministic typed statement; Stage MC2-fix supplies the basic L2 operator-norm comparison bridges, but finite net approximation remains TODO. |
| Metric entropy | `度量熵` | Log covering number / coding complexity | `Metric.coveringNumber` as dependency | TODO | `HighDimProb/MetricEntropy.lean` | TODO | Deferred because `Real.log` over `ℕ∞` needs a deliberate finite/infinite convention. |
| Metric entropy log covering statement | `度量熵` | Real logarithm of covering number | `Metric.coveringNumber` as dependency | TODO | `HighDimProb/MetricEntropy.lean` | blocked theorem TODO | No `Real.log` convention for Mathlib `ℕ∞` covering numbers yet. |
| Metric entropy coding interpretation | `度量熵` | Coding complexity bounded by log covering numbers at nearby radii | TODO | TODO | `HighDimProb/MetricEntropy.lean` | blocked theorem TODO | Needs finite real metric entropy and coding vocabulary. |
| Dudley integral dependency | `Dudley积分不等式` | Entropy integral involving square roots of log covering numbers | TODO | TODO | `HighDimProb/RandomProcess.lean` | blocked theorem TODO | Needs random-process and entropy-integral vocabulary. |
| Random matrix | Random matrix sections | Matrix-valued random variable | `Matrix (Fin m) (Fin n) Real` | `RandomMatrix`, `IsRandomMatrix` | `HighDimProb/RandomMatrix/Basic.lean` | implemented experimental API | Concrete-first representation; available through `HighDimProb.Experimental`, not stable root. |
| Matrix entry random variable | Random matrix sections | Scalar entry map `omega |-> A omega i j` | Function application on `Matrix` | `matrixEntry`, `matrixEntry_apply`, `isRealRandomVariable_matrixEntry` | `HighDimProb/RandomMatrix/Basic.lean` | implemented experimental API | Entrywise measurability is the basic random-matrix predicate. |
| Random row vector | Random matrix sections | Row `i` as a `RandomVector Omega n` | Matrix row access | `rowVector`, `isRandomVector_rowVector` | `HighDimProb/RandomMatrix/RowsCols.lean` | implemented experimental API | Reuses the existing random-vector API. |
| Row dot product | Sample covariance algebra | Dot product of a random-matrix row with a deterministic vector. | `Finset.univ.sum` | `rowDot`, `rowDot_apply`, `rowDot_sq_nonneg`, `sum_rowDot_sq_nonneg` | `HighDimProb/RandomMatrix/Algebra.lean` | implemented/proven experimental API | Stage C1 adds the small helper names needed for PSD/symmetry statement layers. |
| Random column vector | Random matrix sections | Column `j` as a `RandomVector Omega m` | Matrix column access | `colVector`, `isRandomVector_colVector` | `HighDimProb/RandomMatrix/RowsCols.lean` | implemented experimental API | Reuses the existing random-vector API. |
| Matrix-vector action | Random matrix sections, Johnson-Lindenstrauss prerequisites | Random vector `omega |-> A(omega) x` and transposed action | finite sums over `Fin` | `matVec`, `vecMat`, `isRandomVector_matVec`, `isRandomVector_vecMat` | `HighDimProb/RandomMatrix/Action.lean` | implemented experimental API | Uses explicit finite sums instead of committing to a linear-map representation. |
| Frobenius norm vocabulary | `矩阵范数` | Entrywise squared Frobenius norm and square-root norm as random variables | finite sums, `Real.sqrt` | `frobeniusSq`, `frobeniusNorm`, `isRealRandomVariable_frobeniusSq`, `isRealRandomVariable_frobeniusNorm` | `HighDimProb/RandomMatrix/Norms.lean` | implemented experimental API | Kept separate from the Stage 6B operator norm wrapper. |
| Entrywise max absolute value | Random matrix assumption vocabulary | Maximum absolute entry, with zero on empty index products | `Finset.sup`, `nnnorm` | `entrywiseMaxAbs` | `HighDimProb/RandomMatrix/Norms.lean` | implemented experimental API | No theorem-level use yet. |
| Operator norm | `$\\varepsilon$-网与算子范数`, random matrix norm bounds | Induced spectral norm of a matrix as a linear operator | `Matrix.Norms.L2Operator` scoped norm | `operatorNorm`, `operatorNorm_apply`, `isRealRandomVariable_operatorNorm` | `HighDimProb/RandomMatrix/OperatorNorm.lean` | implemented/proven experimental API | Stage 6B uses Mathlib's Euclidean/L2 operator norm; Stage MC2-fix proves measurability from entrywise random-matrix measurability. |
| Unit-vector vocabulary | Operator-norm bridge prerequisites | Unit vectors represented by explicit squared Euclidean norm equal to one. | finite sums over `Fin`, `EuclideanSpace` L2 norm | `vectorSqNorm`, `IsUnitVector`, `unitSphere`, `vectorSqNorm_eq_norm_sq_toLp`, `norm_sq_toLp_eq_vectorSqNorm`, `norm_toLp_eq_one_of_isUnitVector`, `isUnitVector_of_norm_toLp_eq_one` | `HighDimProb/RandomMatrix/UnitSphere.lean` | implemented/proven experimental API | Chosen to align with existing explicit finite-sum conventions while now bridged to Mathlib's L2 norm. |
| Matrix-vector squared norm | Operator-norm bridge prerequisites | Squared Euclidean norm of `A x` using explicit matrix-vector finite sums. | `Finset.univ.sum`, `matVec` normal form, `Matrix.mulVec`, `EuclideanSpace` L2 norm | `matVecSqNorm`, `randomMatVecSqNorm`, `sqNorm_matVec_eq_matVecSqNorm`, `isRealRandomVariable_randomMatVecSqNorm`, `matVecSqNorm_eq_norm_sq_toLp_mulVec`, `norm_sq_toLp_mulVec_eq_matVecSqNorm` | `HighDimProb/RandomMatrix/OperatorNorm.lean` | implemented/proven experimental API | Provides a theorem-friendly finite-sum normal form and an exact bridge to the Mathlib L2 norm of `A x`. |
| Squared operator-norm bound predicate | Matrix concentration prerequisites | Pointwise squared matrix-vector bound over all explicit unit vectors. | `IsUnitVector`, `matVecSqNorm`, `Matrix.Norms.L2Operator` | `OperatorNormBoundSq`, `RandomOperatorNormBoundSq`, `matVecSqNorm_le_of_operatorNormBoundSq`, `operatorNorm_le_of_operatorNormBoundSq`, `operatorNormBoundSq_of_operatorNorm_le` | `HighDimProb/RandomMatrix/OperatorNorm.lean` | implemented/proven experimental API | The explicit predicate is retained for theorem statements and is now bridged in both directions with `deterministicOperatorNorm` under the visible nonnegative-scale hypotheses. |
| Exact operator-norm bridge targets | Matrix concentration prerequisites | Typed targets and proved bridges connecting explicit squared unit-vector bounds to Mathlib's scoped L2 matrix norm and measurability. | `Matrix.Norms.L2Operator`, product measurable spaces, `measurable_norm` | `operatorNorm_le_of_operatorNormBoundSqStatement`, `operatorNormBoundSq_of_operatorNorm_leStatement`, `operatorNormMeasurabilityStatement`, `operatorNorm_le_of_operatorNormBoundSq`, `operatorNormBoundSq_of_operatorNorm_le`, `instOpensMeasurableSpaceMatrixL2Operator`, `isRealRandomVariable_operatorNorm` | `HighDimProb/RandomMatrix/OperatorNorm.lean` | proven bridge plus typed-prop compatibility targets | Stage MC2-fix proves the exact comparison directions and operator-norm measurability; the statement-only targets remain as API signposts. |
| Rank-one operator-norm bound | Rank-one covariance and matrix-concentration prerequisites | The named self-outer product `rankOneMatrix v` has induced L2 operator norm bounded by `||v||₂²`. | `rankOneMatrix`, `Matrix.Norms.L2Operator`, `ContinuousLinearMap.opNorm_le_bound`, Cauchy-Schwarz | `rankOneOperatorNorm_le_vectorSqNorm` | `HighDimProb/RandomMatrix/OperatorNorm.lean` | proven experimental API | The proof uses the existing `deterministicOperatorNorm` convention while keeping the rank-one matrix object named. |
| Rank-one pointwise operator-norm bound | Matrix Bernstein boundedness prerequisites | A pointwise squared-vector-norm bound implies a pointwise operator-norm bound for `rankOneRandomMatrix X` and indexed families. | `operatorNorm`, `rankOneRandomMatrix`, `rankOneRandomMatrixFamily`, `vectorSqNorm`, `PointwiseOperatorNormBound` | `BoundedOperatorNorm_rankOneRandomMatrix_of_sqNorm_bound`, `PointwiseOperatorNormBound_rankOneRandomMatrix_of_sqNorm_bound` | `HighDimProb/RandomMatrix/Assumptions.lean` | proven experimental API | This is the uncentered boundedness bridge; centered expectation-contraction, centered rank-one, measurability/integrability, and PSD nullspace APIs are documented in separate proved rows below. |
| Centered operator-norm bound | Matrix Bernstein boundedness prerequisites | Centering satisfies `||X(ω) - E X||op <= ||X(ω)||op + ||E X||op`; expectation contraction turns a pointwise `R` bound on `X` into a `2 * R` bound on `centeredRandomMatrix`. | `norm_sub_le`, `matrixExpect`, `centeredRandomMatrix`, `centeredRandomMatrixFamily`, `PointwiseOperatorNormBound` | `matrixExpect_eq_integral_l2Operator`, `matrixExpect_eq_integral`, `deterministicOperatorNorm_matrixExpect_le_of_boundedOperatorNorm`, `expectationOperatorNormBound_of_pointwiseOperatorNormBound`, `deterministicOperatorNorm_sub_le_add`, `BoundedOperatorNorm_centered_of_bound_expect_bound`, `PointwiseOperatorNormBound_centered_of_bound_expect_bound`, `PointwiseOperatorNormBound_centered_of_bound_expect_bound_same`, `BoundedOperatorNorm_centered_of_boundedOperatorNorm`, `PointwiseOperatorNormBound_centered_of_pointwiseOperatorNormBound`, `PointwiseOperatorNormBound_centered_of_pointwiseOperatorNormBound_same` | `HighDimProb/RandomMatrix/Expectation.lean`, `HighDimProb/RandomMatrix/OperatorNorm.lean`, `HighDimProb/RandomMatrix/Assumptions.lean` | proven experimental API | The wrappers still require the existing entrywise measurability/integrability assumptions; sample-covariance Matrix Bernstein assumption adapters and tails remain separate tasks. |
| Centered rank-one structural adapter | Matrix Bernstein covariance-style prerequisites | Named centered rank-one random matrices preserve random-matrix measurability, entrywise integrability under coordinate `MemLp ... 2`, self-adjointness, and zero entrywise expectation in family form. | `centeredRandomMatrix`, `rankOneRandomMatrix`, `rankOneRandomMatrixFamily`, `CenteredSelfAdjointRandomMatrixFamily` | `centeredRankOneRandomMatrix`, `centeredRankOneRandomMatrixFamily`, `centeredRankOneRandomMatrix_isRandomMatrix`, `centeredRankOneRandomMatrix_integrable_of_memLp_two`, `centeredRankOneRandomMatrix_centeredSelfAdjoint_of_memLp_two` | `HighDimProb/RandomMatrix/Expectation.lean`, `HighDimProb/RandomMatrix/Assumptions.lean` | proven experimental API | This is the structural covariance-summand adapter only; sample-covariance Matrix Bernstein assumption adapters and tails remain separate tasks. |
| Centered rank-one operator-norm adapter | Matrix Bernstein boundedness prerequisites | A pointwise squared-vector-norm bound plus coordinate second-moment assumptions gives a `2 * R` bound for named centered rank-one summands and their indexed family. | `centeredRankOneRandomMatrix`, `centeredRankOneRandomMatrixFamily`, `BoundedOperatorNorm`, `PointwiseOperatorNormBound` | `BoundedOperatorNorm_centeredRankOneRandomMatrix_of_sqNorm_bound`, `PointwiseOperatorNormBound_centeredRankOneRandomMatrix_of_sqNorm_bound` | `HighDimProb/RandomMatrix/Assumptions.lean` | proven experimental API | This adapter does not prove sample-covariance Matrix Bernstein assumption adapters, lambda-max/operator-norm Matrix Bernstein tails, Tropp/Lieb, Bernstein CFC, or Golden-Thompson. |
| PSD nullspace converse | Matrix Bernstein covariance-style prerequisites | For a real PSD matrix, a zero quadratic form `xᵀ A x = 0` is equivalent to `A x = 0`; HighDimProb exposes both Mathlib `Matrix.PosSemidef` and explicit `IsPSDMatrix` variants. | `Matrix.PosSemidef.dotProduct_mulVec_zero_iff`, `matrixQuadraticForm`, `Matrix.mulVec`, `IsPSDMatrix` | `posSemidef_of_isPSDMatrix`, `matrixQuadraticForm_eq_zero_iff_mulVec_eq_zero_of_posSemidef`, `matrix_mulVec_eq_zero_of_posSemidef_quadraticForm_eq_zero`, `matrixQuadraticForm_eq_zero_iff_mulVec_eq_zero_of_isPSDMatrix`, `matrix_mulVec_eq_zero_of_isPSDMatrix_quadraticForm_eq_zero` | `HighDimProb/RandomMatrix/Spectral.lean` | proven experimental API | This is a deterministic PSD kernel bridge only; it does not prove covariance expectation identities, sample-covariance Matrix Bernstein assumption adapters, or Matrix Bernstein tails. |
| Rank-one kernel nullspace / invisible directions | Kernel, NTK Gram, random-feature, and covariance-style examples | A rank-one quadratic form is a squared feature inner product; a direction is invisible to a rank-one matrix iff it is orthogonal to the generating vector, and orthogonality to all generators makes it invisible to their finite rank-one sum. | `rankOneMatrix`, `rankOneMatrixSum`, `matrixQuadraticForm`, `Matrix.mulVec`, PSD nullspace bridge | `rankOneMatrixSum`, `rankOneMatrix_quadraticForm_eq_inner_sq`, `rankOneMatrix_mulVec_eq_zero_iff_inner_eq_zero`, `rankOneSum_mulVec_eq_zero_of_forall_inner_eq_zero` | `HighDimProb/RandomMatrix/Spectral.lean` | proven experimental API | This is deterministic rank-one API only; it does not define a general nullspace theory, effective dimension, restricted Matrix Bernstein, or sample-covariance concentration. |
| Matrix symmetry/self-adjointness | Matrix Bernstein prerequisites | Pointwise real symmetric/self-adjoint matrix predicates and random-matrix wrappers. | `Matrix.IsSymm`, `Matrix.IsHermitian` | `IsSymmetricMatrix`, `IsSelfAdjointMatrix`, `RandomSymmetricMatrix`, `RandomSelfAdjointMatrix` | `HighDimProb/RandomMatrix/SelfAdjoint.lean` | implemented experimental API | Stage MC1 keeps the vocabulary explicit and does not prove spectral theorems. |
| Vector-to-rank-one matrix measurability / integrability | Matrix Bernstein covariance-style prerequisites | A random vector `X` defines the rank-one random matrix with entries `X_i X_j`; entrywise measurability follows from coordinate measurability, while entrywise integrability requires explicit product or square-moment assumptions. | `Measurable.mul`, `MemLp.integrable_mul`, `IsRandomVector`, `IntegrableRandomMatrix` | `rankOneRandomMatrix`, `rankOneRandomMatrixFamily`, `isRandomMatrix_rankOneRandomMatrix`, `integrableRandomMatrix_rankOneRandomMatrix_of_integrable_products`, `integrableRandomMatrix_rankOneRandomMatrix_of_memLp_two` | `HighDimProb/RandomMatrix/Basic.lean`, `HighDimProb/RandomMatrix/Expectation.lean` | proven experimental API | This bridge deliberately separates measurability from integrability; centered rank-one adapters are now separate proved API rows. |
| PSD and Loewner-style order | Matrix Bernstein and covariance prerequisites | PSD means symmetric plus nonnegative quadratic forms; `MatrixLE A B` means `B - A` is PSD. | finite sums, explicit quadratic forms | `matrixQuadraticForm`, `IsPSDMatrix`, `RandomPSDMatrix`, `MatrixLE`, `quadraticForm_le_of_matrixLE` | `HighDimProb/RandomMatrix/MatrixOrder.lean` | implemented/proven experimental API | No global matrix order instance is installed. Stage MC2 proves the quadratic-form monotonicity bridge for the explicit Loewner-style order. |
| MatrixLE / PSD algebra | Matrix Bernstein single-summand MGF route | Small algebra for explicit PSD and Loewner-style order: addition, nonnegative scalar multiplication, reflexivity, equality, transitivity, and add/smul monotonicity. | explicit quadratic forms, matrix add/sub/smul | `matrixQuadraticForm_add`, `matrixQuadraticForm_smul`, `isPSDMatrix_zero`, `isPSDMatrix_add`, `isPSDMatrix_smul_of_nonneg`, `matrixLE_refl`, `matrixLE_of_eq`, `matrixLE_trans`, `matrixLE_add`, `matrixLE_add_left`, `matrixLE_add_right`, `matrixLE_smul_of_nonneg` | `HighDimProb/RandomMatrix/MatrixOrder.lean` | proven experimental API | MB-S9-matrixle-algebra-proof closes the MatrixLE algebra/RHS-normalization blocker; the Bernstein CFC proof, single-summand MGF provider, trace-mgf provider, and Matrix Bernstein remain unproved. |
| Matrix expectation and centering | Matrix concentration prerequisites | Entrywise expectation, entrywise integrability, Bochner-integral bridge for the scoped L2 operator-norm matrix structure, and entrywise centering for random matrices. | `expect`, `matrixEntry`, `IntegrableRealRandomVariable`, `ContinuousLinearMap.integral_comp_comm`, `Matrix.IsHermitian.ext` | `matrixExpect`, `IntegrableRandomMatrix`, `matrixExpect_eq_integral_l2Operator`, `matrixExpect_eq_integral`, `centeredRandomMatrix`, `isRandomMatrix_centeredRandomMatrix`, `integrableRandomMatrix_centeredRandomMatrix`, `matrixExpect_centeredRandomMatrix`, `randomSelfAdjointMatrix_centeredRandomMatrix`, `centeredSelfAdjointRandomMatrixFamily_centeredRandomMatrixFamily` | `HighDimProb/RandomMatrix/Expectation.lean`, `HighDimProb/RandomMatrix/Assumptions.lean` | proven experimental API | Core definitions remain entrywise; the Bochner bridge is exposed only as a reusable theorem for operator-norm arguments. |
| Matrix concentration assumptions | Matrix Bernstein/Hoeffding/Chernoff prerequisites | Centered self-adjoint families, matrix-valued independence, and pointwise/a.e. operator-norm bounds. | `ProbabilityTheory.iIndepFun`, product measurable spaces, `operatorNorm` | `instMeasurableSpaceMatrix`, `measurable_randomMatrix_of_isRandomMatrix`, `IndependentRandomMatrices`, `SelfAdjointRandomMatrixFamily`, `IndependentSelfAdjointRandomMatrices`, `CenteredSelfAdjointRandomMatrixFamily`, `CenteredRandomSelfAdjointMatrices`, `BoundedOperatorNorm`, `PointwiseOperatorNormBound`, `UniformOperatorNormBound`, `AeOperatorNormBound` | `HighDimProb/RandomMatrix/Basic.lean`, `HighDimProb/RandomMatrix/Assumptions.lean` | implemented experimental API | Stage MC3 separates pointwise and a.e. boundedness instead of hiding the distinction; theorem proofs remain future work. |
| Finite random-matrix sums | Matrix Bernstein/Hoeffding prerequisites | Finite sums of random matrices and structural measurability/self-adjointness lemmas. | `Finset.univ.sum`, `Matrix.sum_apply`, entrywise measurability | `randomMatrixSum`, `randomMatrixSum_apply`, `randomMatrixSum_entry`, `isRandomMatrix_sum`, `isSelfAdjointMatrix_sum`, `randomSelfAdjointMatrix_sum` | `HighDimProb/RandomMatrix/Sums.lean` | implemented/proven experimental API | Stage MC3 supplies the finite-sum object needed by typed matrix concentration statements without proving concentration. |
| Matrix variance proxy | Matrix Bernstein prerequisites | Standard self-adjoint Bernstein variance candidate `sum_i E[A_i^2]` plus its scalar operator norm and semantic upper-bound predicates. | `Matrix.mul_apply`, `matrixExpect`, `operatorNorm`, finite sums | `matrixSquare`, `randomMatrixSquare`, `matrixSecondMoment`, `matrixVarianceProxy`, `MatrixVarianceProxy`, `matrixVarianceProxyBound`, `MatrixVarianceProxyBound`, `MatrixVarianceProxyUpperBound`, `deterministicMatrixVarianceProxyNorm`, `matrixVarianceProxyNorm`, `MatrixVarianceProxyNormBound`, `integrableRandomMatrix_add`, `integrableRandomMatrix_smul`, `integrableRandomMatrix_zero`, `integrableRandomMatrix_const`, `matrixExpect_add`, `matrixExpect_smul`, `matrixExpect_zero`, `matrixExpect_const`, `matrixExpect_const_of_isProbabilityMeasure`, `matrixExpect_one_of_isProbabilityMeasure`, `isPSD_matrixSquare_of_selfAdjoint`, `isPSD_matrixSecondMoment_of_selfAdjoint`, `isPSD_matrixVarianceProxy_of_selfAdjoint` | `HighDimProb/RandomMatrix/VarianceProxy.lean` | implemented/proven experimental API | MB-S1 proves the PSD square, PSD second moment, and PSD variance proxy layer; MB-S9 adds semantic matrix/scalar variance-proxy predicates and proves small matrix expectation linearity/normalization without proving trace-mgf inequalities. |
| Matrix spectral vocabulary | Matrix Bernstein prerequisites | Semantic spectral upper bounds, explicit Rayleigh upper bounds, scalar/matrix upper-tail event vocabulary, and concrete lambda endpoint providers. | `Matrix.PosSemidef`, `Matrix.IsHermitian.eigenvalues`, `Matrix.IsHermitian.eigenvalues₀`, `matrixQuadraticForm`, `IsUnitVector` | `SpectralUpperBound`, `RayleighUpperBound`, `scalarUpperTailEvent`, `matrixUpperBoundTailEvent`, `rayleighUpperBound_of_spectralUpperBound`, `quadraticFormUpperTailEvent_subset_scalarUpperTailEvent_of_rayleighUpperBound`, `quadraticFormUpperTailEvent_subset_matrixUpperBoundTailEvent_of_rayleighUpperBound`, `quadraticFormUpperTailEvent_subset_matrixUpperBoundTailEvent_of_spectralUpperBound`, `lambdaMax`, `lambdaMaxOrdered`, `lambdaMaxOrdered_eq_eigenvalues₀_zero`, `lambdaMin`, `QuadraticFormUpperBound`, `QuadraticFormLowerBound`, `quadraticFormUpperBound_mono`, `quadraticFormLowerBound_mono`, `LambdaMaxBound`, `LambdaMaxPSDUpperBound`, `LambdaMaxOrderedPSDUpperBound`, `spectralUpperBound_of_lambdaMaxPSDUpperBound`, `spectralUpperBound_of_lambdaMaxOrderedPSDUpperBound`, `lambdaMaxOrdered_spectralUpperBound`, `lambdaMaxOrderedPSDUpperBound`, `lambdaMaxOrdered_rayleighUpperBound`, `lambdaMaxOrdered_smul_of_nonneg`, `lambdaMaxOrdered_le_trace_of_posSemidef`, `quadraticFormUpperTailEvent`, `quadraticFormLowerTailEvent`, `twoSidedQuadraticFormTailEvent`, `SelfAdjointOperatorNormTailEvent`, `selfAdjointOperatorNormTailViaQuadraticFormStatement`, `lambdaMax_le_iff_quadraticForm_le_statement`, `lambdaMax_eq_lambdaMaxOrdered_statement`, `lambdaMaxOrdered_is_greatest_eigenvalue`, `matrixQuadraticForm_le_lambdaMaxOrdered_statement`, `operatorNorm_eq_max_abs_lambda_statement`, `matrixQuadraticForm_nonneg_of_posSemidef`, `matrixQuadraticForm_smul_one_of_isUnitVector`, `matrixQuadraticForm_le_lambdaMax_of_lambdaMax_sub_posSemidef`, `matrixQuadraticForm_le_lambdaMax_of_lambdaMaxPSDUpperBound`, `matrixQuadraticForm_le_lambdaMaxOrdered_of_lambdaMaxOrderedPSDUpperBound`, `lambdaMaxUpperTailEvent`, `lambdaMaxOrderedUpperTailEvent`, `lambdaMaxUpperTailEvent_eq_matrixUpperBoundTailEvent`, `lambdaMaxOrderedUpperTailEvent_eq_matrixUpperBoundTailEvent`, `quadraticFormUpperTailEvent_subset_lambdaMaxOrderedUpperTailEvent_of_matrixQuadraticForm_le_lambdaMaxOrdered` | `HighDimProb/RandomMatrix/Spectral.lean` | implemented / semantic bridge experimental API | MC5.1 adds vocabulary; MB-S2 proves monotonicity and one-sided event subset lemmas; MB-S7A-index preserves legacy `lambdaMax`, adds ordered `lambdaMaxOrdered`, and proves the ordered endpoint greatest theorem. MB-S7A-abstract makes downstream proofs depend on `SpectralUpperBound` / `RayleighUpperBound`; MB-S7A-provider proves that `lambdaMaxOrdered` provides `SpectralUpperBound` and the direct ordered Rayleigh wrapper; MB-S7B-scalar-endpoint proves nonnegative scalar multiplication for `lambdaMaxOrdered`; MB-S7B-trace-dominates-endpoint proves the ordered endpoint trace bound; MB-S7B-provider-close proves the trace-exp provider in `Laplace.lean`. Full matrix Laplace and Matrix Bernstein remain unproved. |
| Deterministic operator-norm quadratic-form witness | Operator-norm spectral bridge prerequisites | For a nonempty real self-adjoint matrix, the deterministic L2 operator norm is attained as the absolute value of an explicit unit-vector quadratic form. | `Matrix.IsHermitian.spectral_theorem`, `Matrix.l2_opNorm_diagonal`, `CStarRing.norm_coe_unitary_mul`, `CStarRing.norm_mul_coe_unitary`, `Finset.exists_max_image`, `deterministicOperatorNorm`, `matrixQuadraticForm`, `IsUnitVector` | `exists_unitVector_abs_matrixQuadraticForm_eq_deterministicOperatorNorm` | `HighDimProb/RandomMatrix/Spectral.lean` | proven experimental API | RM-ON-S2 proves the deterministic nonempty witness used by the RM-ON-S3 event bridge. |
| Nonempty self-adjoint operator-norm event bridge | Operator-norm spectral bridge prerequisites | For a nonempty self-adjoint random square matrix, the operator-norm tail event is contained in the existing two-sided quadratic-form tail event. | `exists_unitVector_abs_matrixQuadraticForm_eq_deterministicOperatorNorm`, `SelfAdjointOperatorNormTailEvent`, `twoSidedQuadraticFormTailEvent`, `quadraticFormUpperTailEvent`, `quadraticFormLowerTailEvent` | `selfAdjointOperatorNormTailViaQuadraticFormStatement_nonempty` | `HighDimProb/RandomMatrix/Spectral.lean` | proven nonempty experimental API | RM-ON-S3 proves the existing typed statement in the exact nonempty `Fin (n + 1)` shape. For arbitrary dimensions, use the positive-threshold bridge below. |
| Matrix trace exponential vocabulary | Matrix Bernstein prerequisites | Matrix exponential, trace, trace of matrix exponential, real and lintegral trace-exponential moments, semantic trace-mgf bound predicates, and typed Tropp/Bernstein CFC/single-summand MGF primitives. | `NormedSpace.exp`, `Matrix.trace`, `Matrix.IsHermitian.exp`, `IsSelfAdjoint.exp_nonneg`, `Matrix.nonneg_iff_posSemidef`, `Matrix.PosSemidef.trace_nonneg`, `CFC.log`, `matrixExpect`, `lintegral` | `matrixExp`, `matrixTrace`, `traceMatrixExp`, `isSelfAdjointMatrix_matrixExp`, `matrixTrace_nonneg_of_posSemidef`, `traceMatrixExp_nonneg_of_matrixExp_posSemidef`, `matrixExp_posSemidef_of_selfAdjoint_statement`, `matrixExp_posSemidef_of_selfAdjoint`, `traceExpIntegrand`, `traceExpMoment`, `traceExpMomentLIntegral`, `TraceMGFBound`, `TraceMGFBoundLIntegral`, `TraceMGFVarianceProxyBound`, `TraceMGFVarianceProxyBoundLIntegral`, `troppMasterTraceMGFStep_statement`, `bernsteinMatrixExp_le_quadratic_statement`, `singleSummandMatrixMGFVarianceProxy_statement`, `singleSummandMatrixMGFVarianceProxy_of_bernsteinMatrixExp_le_quadratic`, `traceExpMoment_nonneg_of_nonneg`, `traceExpIntegrand_nonneg_of_randomSelfAdjoint`, `traceExpMoment_nonneg_of_randomSelfAdjoint`, `traceExpMomentLIntegral_nonneg`, `traceExpMomentLIntegral_eq_ofReal_traceExpMoment`, `traceMatrixExp_nonneg_of_selfAdjoint_statement`, `traceMatrixExp_nonneg_of_selfAdjoint`, `traceExpMoment_nonneg_statement`, `traceExpMomentLIntegral_eq_ofReal_statement`, `traceExpMomentBoundStatement`, `traceExpVarianceProxyBoundStatement`, `traceMGFBound_statement`, `traceMGFBoundLIntegral_statement`, `traceMGFVarianceProxyBound_statement` | `HighDimProb/RandomMatrix/TraceExp.lean` | implemented / semantic bridge experimental API | MC5.2 adds vocabulary; MB-S2 adds the lintegral moment object and typed bridge targets; MB-S3 proves downstream nonnegativity and real/lintegral bridge theorems under explicit hypotheses; MB-S4 proves self-adjoint `exp(A)` PSD and self-adjoint trace-exp nonnegativity; MB-S9 adds semantic trace-mgf predicates and typed provider targets; MB-S9-Tropp adds the Tropp master step as a typed statement only; MB-S9-Bernstein-CFC adds the Bernstein-specific scalar-to-matrix primitive as a typed statement only; MB-S9-single adds the single-summand MGF variance-proxy primitive as a typed statement only; MB-S9-PSD-expectation proves matrix-valued expectation monotonicity in `VarianceProxy.lean`; MB-S9-expectation-linearity proves matrix expectation add/smul/zero/constant normalization; MB-S9-single-summand-provider-under-cfc proves the single-summand provider under explicit pointwise CFC assumptions. Trace-mgf provider, functional-calculus proof, Golden-Thompson, Lieb, full CFC-free single-summand provider, and Matrix Bernstein remain unproved. |
| Matrix Laplace vocabulary | Matrix Bernstein prerequisites | Typed matrix Laplace/Chernoff targets plus conditional trace-exponential Markov, semantic dominance, and dominance-wrapper bridges over quadratic-form tail events. | `MeasureTheory.meas_ge_le_lintegral_div`, `Measure`, `ENNReal.ofReal`, `Real.exp`, matrix trace-exp wrappers, semantic spectral upper-bound events | `matrixLaplaceRHS`, `matrixLaplaceRHSLIntegral`, `traceExpThresholdEvent`, `TraceExpDominatesUpperBound`, `lambdaMaxOrdered_traceExpDominatesUpperBound`, `matrixUpperBoundTailEvent_subset_traceExpThresholdEvent_of_traceExpDominatesUpperBound`, `quadraticFormUpperTailEvent_subset_traceExpThresholdEvent_of_rayleighUpperBound_of_traceExpDominatesUpperBound`, `quadraticFormUpperTailEvent_subset_traceExpThresholdEvent_of_spectralUpperBound_of_traceExpDominatesUpperBound`, `traceExpDominatesQuadraticFormUpperTail_of_rayleighUpperBound_of_traceExpDominatesUpperBound`, `traceExpDominatesQuadraticFormUpperTail_of_spectralUpperBound_of_traceExpDominatesUpperBound`, `traceExpDominatesQuadraticFormUpperTail_of_randomSelfAdjoint`, `matrixLaplaceRHSLIntegralDiv`, `matrixLaplaceRHSLIntegralDiv_eq_matrixLaplaceRHSLIntegral`, `traceExpThresholdEvent_lintegral_bound`, `matrixLaplaceTransformLIntegralDiv_of_traceExpThreshold_subset`, `matrixLaplaceTransformLIntegral_of_traceExpThreshold_subset`, `TraceExpDominatesQuadraticFormUpperTail`, `traceExpDominatesQuadraticFormUpperTailStatement`, `quadraticFormUpperTailEvent_subset_traceExpThresholdEvent_of_traceExpDominates`, `matrixLaplaceTransformLIntegralDiv_of_traceExpDominatesQuadraticFormUpperTail`, `matrixLaplaceTransformLIntegral_of_traceExpDominatesQuadraticFormUpperTail`, `matrixLaplaceTransformLIntegralDiv_of_randomSelfAdjoint`, `matrixLaplaceTransformLIntegral_of_randomSelfAdjoint`, `matrixLaplaceTransformStatement`, `matrixLaplaceTransformLIntegralStatement`, `matrixChernoffFromTraceExpStatement`, `matrixChernoffFromTraceExpLIntegralStatement`, `selfAdjointOperatorNormLaplaceStatement`, `selfAdjointOperatorNormLaplaceLIntegralStatement` | `HighDimProb/RandomMatrix/Laplace.lean` | conditional bridge plus semantic dominance experimental API | MC5.3 adds honest typed targets; MB-S2 adds lintegral variants; MB-S5 proves the lintegral Markov/Laplace bridge under explicit measurability and subset hypotheses; MB-S6 names the explicit dominance hypothesis and proves conditional Laplace wrappers from it; MB-S7B-semantic adds `TraceExpDominatesUpperBound` and generic event bridges under explicit pointwise dominance assumptions; MB-S7B-scalar-endpoint resolves the ordered endpoint scalar-multiplication split; MB-S7B-trace-dominates-endpoint resolves the trace endpoint split; MB-S7B-provider-close proves the `lambdaMaxOrdered` trace-exp provider theorem; MB-S7C proves concrete random self-adjoint dominance assembly; MB-S8 proves concrete random self-adjoint lintegral Laplace wrappers. The real RHS bridge, trace-mgf, Golden-Thompson/Lieb inputs, and matrix Bernstein remain unproved. |
| Trace-mgf semantic foundation | Matrix Bernstein trace-mgf route | Semantic real and lintegral trace-mgf bound predicates plus variance-proxy trace-mgf targets. | `traceExpMoment`, `traceExpMomentLIntegral`, `traceMatrixExp`, `matrixVarianceProxy` | `TraceMGFBound`, `TraceMGFBoundLIntegral`, `TraceMGFVarianceProxyBound`, `TraceMGFVarianceProxyBoundLIntegral`, `traceMGFBound_statement`, `traceMGFBoundLIntegral_statement`, `traceMGFVarianceProxyBound_statement` | `HighDimProb/RandomMatrix/TraceExp.lean` | semantic API plus typed provider targets | MB-S9 adds the reusable trace-mgf abstraction layer. The provider statements remain unproved and require future noncommutative trace-mgf machinery. |
| Matrix variance-proxy semantic bounds | Matrix Bernstein trace-mgf route | Semantic comparison predicates for the deterministic variance proxy matrix and its scalar operator-norm proxy, plus crude norm control from pointwise operator-norm bounds. | `matrixVarianceProxy`, `matrixVarianceProxyNorm`, `matrixExpect`, `MatrixLE`, `PointwiseOperatorNormBound` | `MatrixVarianceProxyUpperBound`, `MatrixVarianceProxyNormBound`, `pointwiseOperatorNormVarianceProxyNormRHS`, `deterministicOperatorNorm_matrixSquare_le_sq`, `deterministicOperatorNorm_matrixSecondMoment_le_sq_of_forall`, `matrixVarianceProxyNorm_le_pointwiseOperatorNormVarianceProxyNormRHS`, `MatrixVarianceProxyNormBound_of_pointwiseOperatorNormBound`, `integrableRandomMatrix_sub`, `integrableRandomMatrix_add`, `integrableRandomMatrix_smul`, `integrableRandomMatrix_zero`, `integrableRandomMatrix_const`, `matrixExpect_sub`, `matrixExpect_add`, `matrixExpect_smul`, `matrixExpect_zero`, `matrixExpect_const`, `matrixExpect_const_of_isProbabilityMeasure`, `matrixExpect_one_of_isProbabilityMeasure`, `isPSDMatrix_matrixExpect_of_pointwise_isPSD`, `matrixExpect_matrixLE_of_pointwise_matrixLE` | `HighDimProb/RandomMatrix/VarianceProxy.lean`, `HighDimProb/RandomMatrix/Assumptions.lean` | semantic/proven API plus crude bound theorem | The semantic predicates package existing objects. The crude theorem gives RHS `(Fintype.card I : Real) * R ^ 2` under square-integrability and `0 <= R`; it is not a sharp moment-optimal variance proxy. |
| Matrix concentration statement layer | Matrix Bernstein, matrix Hoeffding, matrix Chernoff, covariance estimation | Typed `Prop` targets for future matrix concentration and covariance-estimation theorems. | `upperTailProb`, `operatorNorm`, matrix assumptions, finite random-matrix sums, variance proxies | `matrixBernsteinStatement`, `matrixBernsteinSelfAdjointStatement`, `matrixBernsteinLaplacePrerequisitesStatement`, `matrixBernsteinTraceMGF_statement`, `operatorNorm_eq_spectralRadius_of_selfAdjointStatement`, `matrixHoeffdingStatement`, `matrixChernoffStatement`, `covarianceEstimationStatement`, `sampleCovarianceOperatorNormStatement`, `sampleCovarianceOperatorNormViaUnitSphereStatement` | `HighDimProb/RandomMatrix/ConcentrationStatements.lean` | typed-prop experimental API | Constants are future proof targets, not proved sharp constants; MB-S1 makes the additive Bernstein statement explicit about probability-measure, summand integrability, square integrability, positivity, variance-proxy norm, and denominator assumptions. MB-S2 adds the analytic prerequisite bundle for the future proof route; MB-S9 adds the typed semantic trace-mgf target. |
| Centered entries | Random matrix norm assumptions | Every scalar entry has mean zero | `Centered` | `CenteredEntries` | `HighDimProb/RandomMatrix/Assumptions.lean` | implemented experimental API | Independence remains separate and deferred. |
| SubGaussian entries | `带有次高斯元素矩阵的范数` | Every scalar entry satisfies a subGaussian predicate | scalar subGaussian predicates | `SubGaussianEntriesOrlicz`, `SubGaussianEntriesTail` | `HighDimProb/RandomMatrix/Assumptions.lean` | implemented experimental API | No norm bound theorem. |
| SubGaussian rows | `次高斯矩阵的双侧界` | Every row is a subGaussian random vector | `SubGaussianVectorOrlicz` | `SubGaussianRowsOrlicz` | `HighDimProb/RandomMatrix/Assumptions.lean` | implemented experimental API | Row independence is deferred. |
| Isotropic rows | `次高斯矩阵的双侧界`, covariance estimation | Every row satisfies an isotropic random-vector predicate | `IsotropicSecondMoment`, `IsotropicCovariance` | `IsotropicRowsSecondMoment`, `IsotropicRowsCovariance` | `HighDimProb/RandomMatrix/Assumptions.lean` | implemented experimental API | Uses row vectors from Stage 6A. |
| Independent entries | `带有次高斯元素矩阵的范数` | Matrix entries are mutually independent | Mathlib `iIndepFun` / independence APIs | TODO | `HighDimProb/RandomMatrix/Assumptions.lean` | TODO | Deferred until independence convention is selected. |
| Gram matrix | Random matrix sections, covariance estimation prerequisites | `omega |-> A(omega)^T A(omega)` as a square random matrix | finite sums over `Fin` | `gramMatrixEntry`, `gramMatrix`, `isRealRandomVariable_gramMatrixEntry` | `HighDimProb/RandomMatrix/SampleCovariance.lean` | implemented experimental API | Entrywise representation avoids committing theorem statements to matrix multiplication rewrites. |
| Row Gram matrix | Random matrix sections, covariance estimation prerequisites | `omega |-> A(omega) A(omega)^T` as a square random matrix | finite sums over `Fin` | `rowGramMatrixEntry`, `rowGramMatrix`, `isRealRandomVariable_rowGramMatrixEntry` | `HighDimProb/RandomMatrix/SampleCovariance.lean` | implemented experimental API | Useful for row-space and rectangular random-matrix statements. |
| Sample covariance | `协方差估计` | Uncentered row-sample covariance `(1 / m) A^T A` | finite sums over `Fin` | `sampleCovarianceEntry`, `sampleCovariance`, `isRealRandomVariable_sampleCovarianceEntry` | `HighDimProb/RandomMatrix/SampleCovariance.lean` | implemented experimental API | Uses rows as samples and the `(1 / (m : Real))` scaling convention; centered/unbiased covariance objects are still future work. |
| Sample covariance row rank-one sum bridge | Matrix Bernstein covariance-style prerequisites | The uncentered sample covariance is the named normalized sum of row rank-one random matrices. | `sampleCovariance`, `rowVector`, `rankOneRandomMatrixFamily`, `randomMatrixSum` | `sampleCovarianceRowRankOneFamily`, `sampleCovarianceRowRankOneSum`, `normalizedSampleCovarianceRowRankOneSum`, `sampleCovariance_eq_normalized_rowRankOne_sum` | `HighDimProb/RandomMatrix/Algebra.lean` | proven experimental API | This is an algebraic representation only; it does not prove concentration or Matrix Bernstein assumptions. |
| Centered sample covariance row rank-one sum bridge | Matrix Bernstein covariance-style prerequisites | The centered sample covariance deviation is the named normalized sum of centered row rank-one random matrices under explicit row rank-one integrability. | `centeredRandomMatrix`, `centeredRankOneRandomMatrixFamily`, `randomMatrixSum`, `IntegrableRandomMatrix` | `centeredSampleCovarianceRowRankOneFamily`, `centeredSampleCovarianceRowRankOneSum`, `normalizedCenteredSampleCovarianceRowRankOneSum`, `sampleCovariance_deviation_eq_normalized_centered_rowRankOne_sum` | `HighDimProb/RandomMatrix/Algebra.lean` | proven experimental API | This does not prove sample covariance concentration, row independence, variance-proxy bounds, or Matrix Bernstein tails. |
| Sample covariance quadratic-form tail wrapper | Matrix Bernstein covariance-style tail route | Under explicit row measurability, coordinate `MemLp 2`, row squared-norm, independence, integrability, Tropp, and CFC assumptions, the bounded-row wrapper derives the crude variance proxy and proves the optimized one-sided quadratic-form Bernstein tail. | `centeredRandomMatrix`, `sampleCovariance`, `centeredSampleCovarianceRowRankOneFamily`, `centeredSampleCovarianceRowRankOneSum`, `MatrixVarianceProxyNormBound`, `matrixBernsteinQuadraticFormUpperTailOptimizedScalarRHSWithBernsteinCoeff_under_primitives` | `sampleCovarianceCenteredRankOneRadius`, `sampleCovarianceCenteredRankOneVarianceProxyBound`, `sampleCovarianceCenteredRankOneVarianceProxyBoundOfRows`, `sampleCovarianceTailTheta`, `sampleCovarianceTailThetaOfRows`, `sampleCovarianceQuadraticFormTailRHS`, `sampleCovariance_quadraticForm_tail_optimized_under_explicit_variance_proxy`, `sampleCovariance_quadraticForm_tail_optimized_under_rowSqNorm_bound` | `HighDimProb/RandomMatrix/ConcentrationStatements.lean` | proven conditional API plus bounded-row crude-VP API | This is not an unconditional sample-covariance concentration theorem and does not prove sharp variance control, unconditional lambda-max/operator-norm Matrix Bernstein tails, Tropp/Lieb, Bernstein CFC, or Golden-Thompson. |
| Two-sided quadratic-form Matrix Bernstein wrapper | Matrix Bernstein spectral/tail route | The existing optimized one-sided quadratic-form tail theorem can be applied to `A` and pointwise `-A`, then combined by the existing two-sided event and finite union bound. | `twoSidedQuadraticFormTailEvent`, `quadraticFormUpperTailEvent`, `quadraticFormLowerTailEvent`, `randomMatrixSum`, `MatrixVarianceProxyNormBound`, `PointwiseOperatorNormBound`, `measure_union_le` | `quadraticFormLowerTailEvent_subset_quadraticFormUpperTailEvent_neg`, `matrixBernsteinTwoSidedQuadraticFormTailOptimizedScalarRHSWithBernsteinCoeff_under_primitives`, `matrixBernsteinTwoSidedQuadraticFormTailOptimizedScalarRHSWithBernsteinCoeff_of_assumptions` | `HighDimProb/RandomMatrix/Spectral.lean`, `HighDimProb/RandomMatrix/ConcentrationStatements.lean` | proven conditional API | The wrapper keeps variance proxies, operator-norm radii, integrability, independence, CFC primitives, and Tropp primitives explicit for both signs; the assumption-bundle variant only shortens public signatures. It does not prove a self-adjoint operator-norm tail, finite-net reduction, variance-proxy control, Tropp/Lieb, Bernstein CFC, Golden-Thompson, or a CFC-free Matrix Bernstein theorem. |
| Self-adjoint operator-norm Matrix Bernstein wrapper | Matrix Bernstein spectral/tail route | The self-adjoint operator-norm tail event is reduced to the two-sided quadratic-form event through the explicit typed bridge, then bounded by the RM-S7B two-sided wrapper. | `SelfAdjointOperatorNormTailEvent`, `selfAdjointOperatorNormTailViaQuadraticFormStatement`, `twoSidedQuadraticFormTailEvent`, `randomSelfAdjointMatrix_sum`, `measure_mono` | `matrixBernsteinSelfAdjointOperatorNormTailOptimizedScalarRHSWithBernsteinCoeff_under_primitives` | `HighDimProb/RandomMatrix/ConcentrationStatements.lean` | proven conditional API | Conditional on `selfAdjointOperatorNormTailViaQuadraticFormStatement (randomMatrixSum A) t`. It keeps both-sign variance proxies, radii, integrability, independence, CFC, and Tropp assumptions explicit. For arbitrary dimensions, prefer the positive-threshold wrapper. |
| Positive-threshold arbitrary self-adjoint operator-norm spectral bridge | Matrix Bernstein spectral/tail route | The zero-dimensional endpoint is empty for positive threshold, and nonempty dimensions reuse the existing nonempty bridge. | `SelfAdjointOperatorNormTailEvent`, `twoSidedQuadraticFormTailEvent`, `unitSphere`, `Fin 0`, `selfAdjointOperatorNormTailViaQuadraticFormStatement_nonempty` | `selfAdjointOperatorNormTailEvent_empty_of_zero_dim_of_pos`, `selfAdjointOperatorNormTailViaQuadraticFormStatement_of_pos` | `HighDimProb/RandomMatrix/Spectral.lean` | proven positive-threshold API | This is the current arbitrary-dimensional bridge for operator-norm tails. It requires `0 < t`; the zero-dimensional `t = 0` endpoint is intentionally outside this route. |
| Nonempty self-adjoint operator-norm Matrix Bernstein wrapper | Matrix Bernstein spectral/tail route | For matrices indexed by `Fin (n + 1)`, the nonempty operator-norm event bridge is supplied internally before applying the retained bridge-explicit wrapper. | `matrixBernsteinSelfAdjointOperatorNormTailOptimizedScalarRHSWithBernsteinCoeff_under_primitives`, `selfAdjointOperatorNormTailViaQuadraticFormStatement_nonempty`, `randomMatrixSum`, `SelfAdjointOperatorNormTailEvent` | `matrixBernsteinSelfAdjointOperatorNormTailOptimizedScalarRHSWithBernsteinCoeff_nonempty_under_primitives` | `HighDimProb/RandomMatrix/ConcentrationStatements.lean` | proven nonempty explicit-primitive API | This removes the explicit spectral-bridge assumption for nonempty square dimensions. It keeps both-sign variance proxies, radii, integrability, independence, CFC, and Tropp assumptions explicit and does not apply the new crude variance-proxy theorem automatically, prove Tropp/Lieb, Bernstein CFC, Golden-Thompson, full Matrix Bernstein, or sample-covariance concentration. |
| Positive-threshold arbitrary self-adjoint operator-norm Matrix Bernstein wrapper | Matrix Bernstein spectral/tail route | For arbitrary dimensions, the positive-threshold spectral bridge is supplied internally before applying the retained bridge-explicit wrapper. | `matrixBernsteinSelfAdjointOperatorNormTailOptimizedScalarRHSWithBernsteinCoeff_under_primitives`, `selfAdjointOperatorNormTailViaQuadraticFormStatement_of_pos`, `randomMatrixSum`, `SelfAdjointOperatorNormTailEvent` | `matrixBernsteinSelfAdjointOperatorNormTailOptimizedScalarRHSWithBernsteinCoeff_arbitrary_of_pos_under_primitives`, `matrixBernsteinSelfAdjointOperatorNormTailOptimizedScalarRHSWithBernsteinCoeff_arbitrary_of_assumptions` | `HighDimProb/RandomMatrix/ConcentrationStatements.lean` | proven arbitrary positive-threshold explicit-primitive API plus assumption-bundle API | Requires `0 < t`. It keeps both-sign variance proxies, radii, integrability, independence, CFC, and Tropp assumptions explicit; the assumption-bundle variant only shortens public signatures and does not apply the crude variance-proxy theorem automatically or prove Tropp/Lieb, Bernstein CFC, Golden-Thompson, full Matrix Bernstein, or the zero-dimensional `t = 0` endpoint. |
| Matrix Bernstein assumption bundles | Matrix Bernstein API ergonomics | The positive side for `A` and the negative side for `negRandomMatrixFamily A` are packaged as reusable theorem-interface structures. | `negRandomMatrixFamily`, `bernsteinThetaChoice`, `MatrixVarianceProxyNormBound`, `PointwiseOperatorNormBound`, `troppMasterTraceMGFFiniteFamily_statement`, `bernsteinMatrixExp_le_quadratic_statement` | `MatrixBernsteinPositiveSideAssumptions`, `MatrixBernsteinNegativeSideAssumptions` | `HighDimProb/RandomMatrix/ConcentrationStatements.lean` | public convenience API | These bundles prevent repeated argument lists in examples and downstream wrappers. They do not prove positive-to-negative transfer, Tropp/Lieb, Bernstein CFC, Golden-Thompson, or full Matrix Bernstein. |
| Example-layer Matrix Bernstein usage surface | Example-layer covariance-style routes | Attention-feature, empirical Fisher, LoRA adapter, and negative-family examples reuse core Matrix Bernstein bundles or random-feature assumptions; bounded-row sample covariance reuses the core sample-covariance bounded-row wrapper and explicit-row helpers. | `MatrixBernsteinPositiveSideAssumptions`, `MatrixBernsteinNegativeSideAssumptions`, `RandomFeatureKernelOptimizedMatrixBernsteinAssumptions`, `negRandomMatrixFamily`, `sampleCovarianceCenteredRankOneVarianceProxyBoundOfRows`, `sampleCovarianceTailThetaOfRows` | `AttentionFeatureGramTailAssumptions`, `attentionFeatureGram_quadraticForm_tail_usage`, `attentionFeatureGram_operatorNorm_tail_usage`, `EmpiricalFisherTailAssumptions`, `empiricalFisher_operatorNorm_tail_usage`, `LoRAAdapterSubspaceCovarianceAssumptions`, `loraAdapterSubspaceCovariance_operatorNorm_tail_usage`, `negativeFamily_twoSided_quadraticForm_tail_usage`, `negativeFamily_selfAdjoint_operatorNorm_tail_usage`, `boundedRowSampleCovariance_operatorNorm_tail_usage` | `HighDimProb/Examples/RandomMatrix/...` | example API | These wrappers are usage and documentation surfaces, not new proof providers. Feature, gradient, adapter, and row-bound fields do not derive all Matrix Bernstein primitives; positive/negative-side bundles, Tropp, CFC, variance proxies, independence, and integrability remain explicit where the called theorem requires them. |
| Sample covariance operator-norm tail wrapper | Matrix Bernstein covariance-style tail route | The centered sample-covariance operator-norm event is bridged to the unnormalized centered row-rank-one sum at threshold `(m : Real) * t`; the latest bounded-row wrapper applies the positive-threshold self-adjoint operator-norm Matrix Bernstein wrapper and supplies the positive-side crude variance proxy from the bounded-row assumption. | `centeredRandomMatrix`, `sampleCovariance`, `centeredSampleCovarianceRowRankOneSum`, `centeredSampleCovarianceRowRankOneFamilyNeg`, `centeredSampleCovarianceRowRankOneSumNeg`, `SelfAdjointOperatorNormTailEvent`, `sampleCovariance_deviation_eq_normalized_centered_rowRankOne_sum`, `matrixBernsteinSelfAdjointOperatorNormTailOptimizedScalarRHSWithBernsteinCoeff_arbitrary_of_pos_under_primitives` | `sampleCovariance_selfAdjointOperatorNormTailEvent_subset_centeredRowRankOneSum`, `sampleCovariance_selfAdjointOperatorNorm_tail_optimized_under_explicit_variance_proxy`, `sampleCovariance_selfAdjointOperatorNorm_tail_optimized_nonempty_under_explicit_variance_proxy`, `sampleCovariance_selfAdjointOperatorNorm_tail_optimized_arbitrary_of_pos_under_explicit_variance_proxy`, `sampleCovariance_selfAdjointOperatorNorm_tail_optimized_arbitrary_of_pos_under_rowSqNorm_bound` | `HighDimProb/RandomMatrix/ConcentrationStatements.lean` | proven arbitrary positive-threshold API plus retained conditional/nonempty APIs | The bounded-row arbitrary theorem requires `0 < t`, negative-family pointwise bounds, and both-sign integrability/Tropp/CFC assumptions. The negative-side adapters are the next cleanup. It does not prove sharp variance control, full Matrix Bernstein, or unconditional sample covariance concentration. |
| Sample covariance tail usage surface | Example-layer sample covariance Matrix Bernstein route | The preferred example wrappers bundle the sample-covariance bounded-row assumptions and call the public bounded-row quadratic-form and arbitrary positive-threshold operator-norm theorems using the core scalar RHS and core negative-family names. | `sampleCovariance_quadraticForm_tail_optimized_under_rowSqNorm_bound`, `sampleCovariance_selfAdjointOperatorNorm_tail_optimized_arbitrary_of_pos_under_rowSqNorm_bound`, `sampleCovarianceQuadraticFormTailRHS`, `sampleCovarianceCenteredRankOneVarianceProxyBound`, `centeredSampleCovarianceRowRankOneFamilyNeg`, `centeredSampleCovarianceRowRankOneSumNeg`, `centeredRandomMatrix`, `sampleCovariance` | `SampleCovarianceTailAssumptions`, `sampleCovariance_quadraticForm_tail_usage`, `SampleCovarianceOperatorNormTailAssumptions`, `sampleCovariance_operatorNorm_tail_usage` | `HighDimProb/Examples/RandomMatrix/SampleCovarianceTailUsage.lean` | example API | This discharges the positive-side explicit variance-proxy assumption via the crude bounded-row theorem. Independence, square/exponential/trace integrability, Tropp, CFC, and negative-family primitives remain explicit; `sampleCovarianceQuadraticFormTailRHS` uses the actual column dimension parameter, with nonempty examples passing `n + 1`. |
| Quadratic form | Hanson-Wright prerequisites | Scalar form `omega |-> x^T A(omega) x` | finite double sums over `Fin` | `quadraticForm`, `isRealRandomVariable_quadraticForm` | `HighDimProb/RandomMatrix/QuadraticForm.lean` | implemented experimental API | Definition only; no Hanson-Wright or trace identity proof. |
| Bilinear form | Hanson-Wright and chaos vocabulary | Scalar form `omega |-> x^T A(omega) y` | finite double sums over `Fin` | `bilinearForm`, `isRealRandomVariable_bilinearForm` | `HighDimProb/RandomMatrix/QuadraticForm.lean` | implemented experimental API | General rectangular form used as future theorem vocabulary. |
| Unbiased sample covariance | Covariance estimation refinements | Centered or Bessel-corrected empirical covariance | TODO | TODO | future `HighDimProb/RandomMatrix/SampleCovariance.lean` | TODO | Requires centering convention and nonzero/sample-size hypotheses. |
| Random process | `随机过程` | Indexed family of random variables | Function family | `RandomProcess`, `processAt` | `HighDimProb/RandomProcess.lean` | experimental scaffold | Available through `HighDimProb.Experimental`; separability and paths TODO. |
| Gaussian process | `高斯过程` | Process with Gaussian finite marginals | TODO | TODO | `HighDimProb/RandomProcess.lean` | TODO | Needs Gaussian distribution vocabulary. |
| Gaussian width | `高斯宽度` | Expected supremum over Gaussian process | TODO | `GaussianWidthFunctional` | `HighDimProb/GaussianWidth.lean` | experimental scaffold | Available through `HighDimProb.Experimental`; scaffold functional only. |
| Empirical process | `经验过程` | Random fluctuation indexed by functions | TODO | `RandomSample`, `sampleEvaluation` | `HighDimProb/EmpiricalProcess.lean` | experimental scaffold | Available through `HighDimProb.Experimental`; empirical measure TODO. |
| Empirical measure | `经验测度` | Average of Dirac masses | `Measure.dirac`, finite sums | TODO | `HighDimProb/EmpiricalProcess.lean` | TODO | Needs finite sum measure design. |
| Sparse vector | `向量的稀疏性` | Few nonzero coordinates | TODO | TODO | `HighDimProb/SignalRecovery.lean` | TODO/stable API pending | Signal recovery scaffold module is available through `HighDimProb.Experimental`. |
| RIP | `RIP` | Restricted isometry property | TODO | TODO | `HighDimProb/SignalRecovery.lean` | TODO | Future theorem layer. |
| A.e.-nonnegative Markov inequality | Scalar concentration foundations | Markov inequality with `0 <= X` only almost everywhere. | `ae`, `MeasureTheory.meas_ge_le_lintegral_div` | `lintegral_ofReal_eq_ofReal_expect_ae_nonneg`, `markov_inequality_ae_nonneg` | `HighDimProb/Concentration/Basic.lean`, `HighDimProb/Concentration/Markov.lean` | proven experimental API | Stage S3 keeps the tail event pointwise but uses a.e. nonnegativity for the lintegral-to-expectation bridge. |
| Scalar variance nonnegativity | Variance prerequisites | Scalar variance is nonnegative. | `ProbabilityTheory.variance_nonneg` | `variance_nonneg` | `HighDimProb/Scalar/Variance.lean` | proven stable scalar API | Thin HighDimProb-facing wrapper around Mathlib variance. |
| Centered variance invariance | Variance prerequisites | Subtracting the mean preserves scalar variance. | `ProbabilityTheory.variance_sub_const` | `variance_centered_eq_variance` | `HighDimProb/Scalar/Variance.lean` | proven stable scalar API | Requires scalar measurability and a probability measure. |
| Explicit net covering-number bridge | Metric entropy prerequisites | Explicit external/internal epsilon-nets bound external/internal covering numbers. | Mathlib `Metric.IsCover` covering-number lemmas | `externalCoveringNumber_le_encard_of_isEpsilonNet`, `externalCoveringNumber_le_card_of_isEpsilonNet`, `coveringNumber_le_encard_of_isInternalEpsilonNet`, `coveringNumber_le_card_of_isInternalEpsilonNet` | `HighDimProb/MetricEntropy.lean` | proven experimental API | Stage S3 keeps the external/internal distinction explicit. |
| Covariance-form isotropic centeredness | Isotropic random vectors | Covariance-form isotropicity includes centered-vector structure. | product projection | `IsotropicCovariance.centeredVector` | `HighDimProb/Isotropic.lean` | proven experimental API | Definitional projection from `IsotropicCovariance`. |
| Frobenius-square nonnegativity | Random matrix norm vocabulary | The squared Frobenius norm is pointwise nonnegative. | finite sums, `sq_nonneg` | `frobeniusSq_nonneg` | `HighDimProb/RandomMatrix/Norms.lean` | proven experimental API | Uses the explicit finite-sum representation. |
| Sample covariance diagonal nonnegativity | Sample covariance vocabulary | Diagonal entries of uncentered sample covariance are nonnegative. | finite sums, `sq_nonneg`, nonnegative reciprocal of natural cast | `sampleCovarianceEntry_diag_nonneg` | `HighDimProb/RandomMatrix/SampleCovariance.lean` | proven experimental API | Handles `m = 0` by Lean total division convention. |
| Sample covariance quadratic-form bridge | Sample covariance PSD prerequisites | The explicit double-sum quadratic form of uncentered sample covariance rewrites to a scaled sum of squared row marginals, hence is nonnegative. | finite sums over `Fin`, `Finset.mul_sum`, `Finset.sum_mul`, `Finset.sum_comm`, `sq_nonneg` | `quadraticForm_sampleCovariance_eq_sum_sq`, `quadraticForm_sampleCovariance_eq_scaled_sum_rowDot_sq`, `quadraticForm_sampleCovariance_nonneg` | `HighDimProb/RandomMatrix/Algebra.lean` | proven experimental API | Normal forms include both explicit sums and `rowDot`; no `0 < m` assumption is used. |
| SubExponential lintegral MGF formulation | Bernstein prerequisites | Proof-friendly local MGF control for ENNReal Chernoff tails. | `lintegral`, `ENNReal.ofReal`, `Real.exp` | `CenteredSubExponentialMGFLIntegral` | `HighDimProb/Concentration/SubExponentialSums.lean` | implemented experimental API | Added in Stage B1 because the raw predicate is not enough for ENNReal tail proofs. |
| Finite max-scale and variance proxy | Bernstein prerequisites | Deterministic finite-family scale vocabulary for `B = max_i K_i` and `V = sum_i K_i^2`. | `Finset.univ.sup`, `Real.toNNReal`, finite sums | `maxScale`, `varianceProxy` | `HighDimProb/Concentration/MaxScale.lean` | proven experimental API | `maxScale` clips negative entries through `Real.toNNReal`; subExponential use supplies positive scales. |
| Independent finite subExponential sum MGF | Bernstein prerequisites | Independent local-MGF variables have raw and lintegral finite-sum MGF bounds with variance proxy `varianceProxy K` and domain `1 / maxScale K`. | `ProbabilityTheory.iIndepFun.mgf_sum`, `iIndepFun.integrable_exp_mul_sum`, `lintegral_ofReal_ne_top_iff_integrable` | `centeredSubExponentialMGF_sum_mgf_bound_of_iIndepFun_maxScale`, `centeredSubExponentialMGFLIntegral_sum_mgf_bound_of_iIndepFun_maxScale` | `HighDimProb/Concentration/SubExponentialSums.lean` | proven experimental API | The packaged raw predicate theorem still uses conservative scale `sqrt (sum_i K_i^2)`; the normalized bridge keeps the Bernstein domain. |
| Bernstein scalar min-form | Bernstein inequality | Finite independent centered subExponential sums satisfying the lintegral local-MGF predicate have two-regime min-form tails. | `min`, `absTailProb`, `ENNReal.ofReal`, `Real.exp`, `MeasureTheory.meas_ge_le_lintegral_div` | `subExponentialBernsteinRate`, `upperTailProb_le_exp_neg_quarter_bernsteinRate_of_centeredSubExponentialMGFLIntegral_sum`, `bernstein_sum_subExponential` | `HighDimProb/Concentration/Bernstein.lean` | proven experimental API | Constant is `1/4`; raw-predicate statement variants remain typed `Prop` scaffolds. |
| Weighted Bernstein scalar min-form | Bernstein inequality | Deterministic weighted finite independent centered subExponential sums satisfying the lintegral local-MGF predicate have two-regime min-form tails. | finite sums, `abs`, `min`, `absTailProb`, `ENNReal.ofReal`, `Real.exp`, Mathlib independent MGF products | `weightedVarianceProxy`, `weightedMaxScale`, `centeredSubExponentialMGFLIntegral_weighted_sum_mgf_bound_of_iIndepFun_maxScale`, `bernstein_weighted_sum_subExponential` | `HighDimProb/Concentration/MaxScale.lean`, `HighDimProb/Concentration/SubExponentialSums.lean`, `HighDimProb/Concentration/Bernstein.lean` | proven experimental API | Constant is `1/4`; `V_c = sum_i c_i^2 * K_i^2`, `B_c = max_i |c_i| * K_i`, and individual zero weights are allowed under the positive total proxy assumptions. |
| Scalar concentration milestone | Scalar concentration inequalities | Closeout document and import-test audit for Markov/Chebyshev, Orlicz/tail, moment, MGF, Rademacher, Hoeffding, subExponential, and Bernstein theorem families. | documentation plus import tests | `docs/ScalarConcentrationMilestone.md`, `HighDimProbTest.ScalarConcentrationMilestoneAPI` | docs and `HighDimProbTest/ScalarConcentrationMilestoneAPI.lean` | documented/tested experimental milestone | No theorem meanings changed; scalar concentration remains experimental. |
| Scalar concentration branch closure | Scalar concentration inequalities | Final leaf audit, theorem index, test coverage map, and milestone document for the completed experimental scalar concentration branch. | documentation plus existing focused `#check` tests | `docs/ConcentrationLeafAudit.md`, `docs/ScalarConcentrationTheoremIndex.md`, `docs/ConcentrationTestCoverage.md`, `docs/Milestone-ScalarConcentration.md` | docs | documented experimental milestone | `HighDimProb.Concentration` remains experimental; Stages M-real-1 and M-real-2 resolve the full subGaussian and subExponential finite-`ENNReal` moment bridges, and Stage SC-final-update refreshes the milestone docs. |
| Lean path visualization layer | Repository navigation and theorem-spine visualization | Human/agent-facing diagrams for module structure, imports, scalar concentration, Hoeffding, Bernstein, random matrix infrastructure, and KG-to-Lean validation. | Mermaid, Graphviz DOT, upstream `importGraph` | `docs/visualizations/index.md`, `docs/visualizations/*.mmd`, `docs/visualizations/lake_import_graph.dot`, `docs/visualizations/lake_import_graph.html`, `tools/build_import_graph.sh`, `tools/build_import_graph.ps1` | docs/script | implemented documentation infrastructure | No Lean declarations or theorem meanings are changed; the import graph is generated and the theorem/path diagrams are curated from milestone docs. |
| Compile-time judge suite | OJ-style API and policy checks | Separate Lean library that verifies downstream imports, selected theorem applications, and repository policy constraints. | Lean `#check`, examples, Python standard-library policy script | `HighDimProbJudge.lean`, `HighDimProbJudge/`, `scripts/judge_policy_check.py`, `docs/JudgeSystem.md` | judge/test infrastructure | implemented | Does not replace `HighDimProbTest`; no theorem meanings or optional dependencies changed. |
| Quadratic-form to lambda-max spectral bridge | Matrix Bernstein spectral prerequisites | Typed Rayleigh bridge from explicit unit-vector quadratic forms to the legacy `lambdaMax`, plus an ordered endpoint provider route through `lambdaMaxOrdered` and the semantic `SpectralUpperBound` / `RayleighUpperBound` layer. | Mathlib Hermitian eigenvalue endpoint and Rayleigh quotient APIs | `SpectralUpperBound`, `RayleighUpperBound`, `matrixQuadraticForm_le_lambdaMax_statement`, `LambdaMaxPSDUpperBound`, `lambdaMaxUpperTailEvent`, `quadraticFormUpperBound_of_lambdaMax_le_of_matrixQuadraticForm_le_lambdaMax`, `quadraticFormUpperTailEvent_subset_lambdaMaxUpperTailEvent_of_matrixQuadraticForm_le_lambdaMax`, `lambdaMaxOrdered`, `lambdaMaxOrdered_eq_eigenvalues₀_zero`, `lambdaMax_eq_lambdaMaxOrdered_statement`, `lambdaMaxOrdered_is_greatest_eigenvalue`, `LambdaMaxOrderedPSDUpperBound`, `lambdaMaxOrdered_spectralUpperBound`, `lambdaMaxOrderedPSDUpperBound`, `lambdaMaxOrdered_rayleighUpperBound`, `lambdaMaxOrdered_smul_of_nonneg`, `matrixQuadraticForm_le_lambdaMaxOrdered_statement`, `matrixQuadraticForm_le_lambdaMaxOrdered_of_lambdaMaxOrderedPSDUpperBound`, `lambdaMaxOrderedUpperTailEvent`, `quadraticFormUpperTailEvent_subset_lambdaMaxOrderedUpperTailEvent_of_matrixQuadraticForm_le_lambdaMaxOrdered`, `rayleighUpperBound_of_spectralUpperBound`, `unitSphere_empty_of_zero_dim`, `quadraticFormUpperTailEvent_empty_of_zero_dim`, `matrixQuadraticForm_nonneg_of_posSemidef`, `matrixQuadraticForm_smul_one_of_isUnitVector`, `matrixQuadraticForm_le_lambdaMax_of_lambdaMax_sub_posSemidef`, `matrixQuadraticForm_le_lambdaMax_of_lambdaMaxPSDUpperBound` | `HighDimProb/RandomMatrix/Spectral.lean` | semantic layer plus ordered endpoint wrapper with proved provider lemmas | Legacy direct Rayleigh theorem remains unproved; MB-S7A-provider proves the ordered endpoint semantic provider and ordered Rayleigh route; MB-S7B-scalar-endpoint proves nonnegative scalar multiplication for the ordered endpoint; MB-S7B-exp-spectral-mapping proves `lambdaMaxOrdered_matrixExp` in `TraceExp.lean`; dimension route is `Fin (n + 1)` plus zero-dimensional emptiness lemmas. |

## RM-S7A Lambda-Max Tail Bridge

- RM-S7A proves `lambdaMaxOrderedUpperTailEvent_subset_quadraticFormUpperTailEvent`,
  the reverse ordered endpoint event bridge into the existing explicit
  quadratic-form upper-tail event. The proof reuses Mathlib Hermitian
  `eigenvectorBasis` / `eigenvalues_eq`, `lambdaMaxOrdered`, `quadraticFormUpperTailEvent`,
  and `isUnitVector_of_norm_toLp_eq_one`; operator-norm reduction remains
  tracked by `selfAdjointOperatorNormTailViaQuadraticFormStatement`.

## RM-ON-S2 Deterministic Operator-Norm Witness

- RM-ON-S2 proves
  `exists_unitVector_abs_matrixQuadraticForm_eq_deterministicOperatorNorm`
  for nonempty `Fin (n + 1)` real self-adjoint matrices. The proof reuses
  Mathlib Hermitian diagonalization, the scoped L2 diagonal operator-norm
  theorem, unitary norm invariance, and a finite maximum over eigenvalue
  absolute values. RM-ON-S3 uses this witness to prove the nonempty event
  bridge.

## RM-ON-S3 Nonempty Operator-Norm Event Bridge

- RM-ON-S3 proves
  `selfAdjointOperatorNormTailViaQuadraticFormStatement_nonempty`, the
  nonempty `Fin (n + 1)` instance of the existing typed spectral bridge from
  `SelfAdjointOperatorNormTailEvent` to `twoSidedQuadraticFormTailEvent`.
- For arbitrary dimensions, use the positive-threshold bridge below.

## RM-ON Arbitrary Positive Operator-Norm Event Bridge

- The arbitrary-dimension bridge leaf proves
  `selfAdjointOperatorNormTailEvent_empty_of_zero_dim_of_pos` for `Fin 0` and
  `0 < t`.
- It proves `selfAdjointOperatorNormTailViaQuadraticFormStatement_of_pos` by
  case-splitting on the dimension: the zero-dimensional branch uses the empty
  event endpoint and the successor branch reuses
  `selfAdjointOperatorNormTailViaQuadraticFormStatement_nonempty`.
- It does not package a zero-dimensional `t = 0` endpoint; the current
  arbitrary-dimensional operator-norm route is positive-threshold.

## RM-S7B Two-Sided Quadratic-Form Tail Wrapper

- RM-S7B proves `quadraticFormLowerTailEvent_subset_quadraticFormUpperTailEvent_neg`
  as the smallest event bridge from a lower-tail event for `A` to an upper-tail
  event for pointwise `-A`.
- RM-S7B proves
  `matrixBernsteinTwoSidedQuadraticFormTailOptimizedScalarRHSWithBernsteinCoeff_under_primitives`
  by reusing
  `matrixBernsteinQuadraticFormUpperTailOptimizedScalarRHSWithBernsteinCoeff_under_primitives`
  for `A` and `-A`, plus `twoSidedQuadraticFormTailEvent` and
  `measure_union_le`.
- The wrapper intentionally keeps all sign-specific variance-proxy,
  radius, integrability, independence, CFC, and Tropp assumptions explicit.
  RM-S7C consumes this wrapper for the conditional self-adjoint operator-norm
  tail step.

## RM-S7C Self-Adjoint Operator-Norm Tail Wrapper

- RM-S7C proves
  `matrixBernsteinSelfAdjointOperatorNormTailOptimizedScalarRHSWithBernsteinCoeff_under_primitives`
  by reusing
  `selfAdjointOperatorNormTailViaQuadraticFormStatement`,
  `randomSelfAdjointMatrix_sum`, and the RM-S7B two-sided quadratic-form
  wrapper.
- The theorem is conditional on the explicit bridge
  `selfAdjointOperatorNormTailViaQuadraticFormStatement (randomMatrixSum A) t`.
- It does not discharge the general bridge assumption, variance-proxy control,
  finite-net reduction, Tropp/Lieb, Bernstein CFC, Golden-Thompson, or a
  CFC-free Matrix Bernstein theorem.

## RM-ON-S4 Nonempty Operator-Norm Matrix Bernstein Wrapper

- RM-ON-S4 proves
  `matrixBernsteinSelfAdjointOperatorNormTailOptimizedScalarRHSWithBernsteinCoeff_nonempty_under_primitives`
  by reusing the RM-S7C conditional wrapper and the RM-ON-S3 nonempty event
  bridge.
- The theorem has no explicit
  `selfAdjointOperatorNormTailViaQuadraticFormStatement (randomMatrixSum A) t`
  argument for nonempty square dimensions.
- It keeps Tropp, CFC, variance-proxy, independence, integrability, and
  pointwise operator-norm assumptions explicit for both signs.

## RM-ON Arbitrary Positive Operator-Norm Matrix Bernstein Wrapper

- The arbitrary-dimension bridge leaf proves
  `matrixBernsteinSelfAdjointOperatorNormTailOptimizedScalarRHSWithBernsteinCoeff_arbitrary_of_pos_under_primitives`
  by supplying `selfAdjointOperatorNormTailViaQuadraticFormStatement_of_pos`
  to the retained bridge-explicit wrapper.
- It requires `0 < t`, keeps Tropp, CFC, variance-proxy, independence,
  integrability, and pointwise operator-norm assumptions explicit for both
  signs, and does not prove full Matrix Bernstein or variance-proxy control.

## RM-S7E/RM-S7F Sample Covariance Operator-Norm Tail Wrapper

- RM-S7E proves
  `sampleCovariance_selfAdjointOperatorNormTailEvent_subset_centeredRowRankOneSum`,
  the event bridge from the centered sample-covariance operator-norm event to
  the unnormalized centered row-rank-one sum at threshold `(m : Real) * t`.
- RM-S7F proves
  `sampleCovariance_selfAdjointOperatorNorm_tail_optimized_under_explicit_variance_proxy`
  by reusing RM-S7E and the conditional RM-S7C wrapper.
- The theorem is conditional on the explicit spectral bridge
  `selfAdjointOperatorNormTailViaQuadraticFormStatement
    (centeredSampleCovarianceRowRankOneSum (P := P) A) ((m : Real) * t)`.
- It does not discharge the general spectral-bridge assumption,
  variance-proxy control, Tropp/Lieb, Bernstein CFC, Golden-Thompson, full
  Matrix Bernstein, or an unconditional sample-covariance concentration
  theorem.

## RM-ON-S5 Nonempty Sample Covariance Operator-Norm Tail Wrapper

- RM-ON-S5 proves
  `sampleCovariance_selfAdjointOperatorNorm_tail_optimized_nonempty_under_explicit_variance_proxy`
  by reusing the RM-S7E event bridge and the RM-ON-S4 nonempty
  self-adjoint operator-norm Matrix Bernstein wrapper.
- The theorem has no explicit
  `selfAdjointOperatorNormTailViaQuadraticFormStatement
    (centeredSampleCovarianceRowRankOneSum (P := P) A) ((m : Real) * t)`
  argument in the nonempty `Fin (n + 1)` shape.
- It keeps variance proxy, Tropp/CFC primitives, independence, integrability,
  row `MemLp 2`, row squared-norm, and positivity assumptions explicit. The
  later bounded-row wrapper applies the crude variance-proxy theorem; this
  retained wrapper does not prove Tropp/Lieb, Bernstein CFC, Golden-Thompson,
  full Matrix Bernstein, or unconditional sample-covariance concentration.

## RM-ON Arbitrary Positive Sample Covariance Operator-Norm Tail Wrapper

- The arbitrary-dimension bridge leaf proves
  `sampleCovariance_selfAdjointOperatorNorm_tail_optimized_arbitrary_of_pos_under_explicit_variance_proxy`
  by reusing the generalized sample-covariance event bridge and the arbitrary
  positive-threshold self-adjoint operator-norm Matrix Bernstein wrapper.
- It keeps variance proxy, Tropp/CFC primitives, independence, integrability,
  row `MemLp 2`, row squared-norm, and positivity assumptions explicit. The
  later bounded-row wrapper applies the crude variance-proxy theorem; this
  retained wrapper does not prove Tropp/Lieb, Bernstein CFC, Golden-Thompson,
  full Matrix Bernstein, or unconditional sample-covariance concentration.

## MB-S7B Trace-Exp Provider Splits

- MB-S7B-scalar-endpoint proves `lambdaMaxOrdered_smul_of_nonneg`.
- MB-S7B-exp-spectral-mapping proves `lambdaMaxOrdered_matrixExp` in
  `HighDimProb/RandomMatrix/TraceExp.lean`.
- MB-S7B-trace-dominates-endpoint proves
  `lambdaMaxOrdered_le_trace_of_posSemidef` in
  `HighDimProb/RandomMatrix/Spectral.lean`.
- MB-S7B-provider-close proves
  `lambdaMaxOrdered_traceExpDominatesUpperBound` in
  `HighDimProb/RandomMatrix/Laplace.lean`.
- MB-S7C-assemble-dominance proves
  `traceExpDominatesQuadraticFormUpperTail_of_randomSelfAdjoint` in
  `HighDimProb/RandomMatrix/Laplace.lean`.
- MB-S8-laplace-assembly proves
  `matrixLaplaceTransformLIntegralDiv_of_randomSelfAdjoint` and
  `matrixLaplaceTransformLIntegral_of_randomSelfAdjoint` in
  `HighDimProb/RandomMatrix/Laplace.lean`.
- MB-S9-foundation adds `TraceMGFBound`, `TraceMGFBoundLIntegral`,
  `TraceMGFVarianceProxyBound`, `TraceMGFVarianceProxyBoundLIntegral`,
  `MatrixVarianceProxyUpperBound`, `MatrixVarianceProxyNormBound`, and
  `matrixBernsteinTraceMGF_statement`.
- MB-S9-Tropp-master-typed-primitive adds
  `troppMasterTraceMGFStep_statement` as a typed-only Tropp/Lieb master
  trace-mgf primitive with explicit assumptions.
- MB-S9-single-summand-mgf-typed-primitive adds
  `singleSummandMatrixMGFVarianceProxy_statement` as a typed-only
  single-summand matrix MGF variance-proxy primitive with explicit
  self-adjointness, centeredness, boundedness, integrability, theta range,
  square comparison, and `MatrixLE` conclusion.
- MB-S9-bernstein-cfc-typed-primitive adds
  `bernsteinMatrixExp_le_quadratic_statement` as a typed-only
  Bernstein-specific scalar-to-matrix functional-calculus primitive with
  explicit self-adjointness, deterministic operator-norm bound, theta range,
  Bernstein quadratic coefficient, and `MatrixLE` conclusion.
- MB-S9-expectation-linearity-proof proves
  `integrableRandomMatrix_add`, `integrableRandomMatrix_smul`,
  `integrableRandomMatrix_zero`, `integrableRandomMatrix_const`,
  `matrixExpect_add`, `matrixExpect_smul`, `matrixExpect_zero`,
  `matrixExpect_const`, `matrixExpect_const_of_isProbabilityMeasure`, and
  `matrixExpect_one_of_isProbabilityMeasure`.
- The real RHS bridge, trace-mgf provider theorem, Golden-Thompson, Lieb, and
  Matrix Bernstein remain unproved.
- MB-S9-bernstein-coefficient-proof proves `bernsteinCoefficient_nonneg`,
  the scalar nonnegativity helper for
  `(theta ^ 2 / 2) / (1 - abs theta * R / 3)` under `abs theta * R < 3`.
  The single-summand provider remains unproved. The downstream matrix
  exponential lower bound `MatrixLE (1 + c smul V) (matrixExp (c smul V))`, the
  Bernstein CFC proof, trace-mgf provider, Golden-Thompson, Lieb, and Matrix
  Bernstein remain unproved.
- MB-S9-exp-lower-bound-proof proves
  `matrixLE_one_add_self_le_matrixExp_of_selfAdjoint` and
  `matrixLE_one_add_smul_le_matrixExp_smul_of_selfAdjoint`, the MatrixLE
  affine lower bound `1 + A <= matrixExp A` for self-adjoint real matrices and
  its scalar-multiple wrapper. The Bernstein CFC primitive remains typed only,
  and trace-mgf provider, Golden-Thompson, Lieb, and Matrix Bernstein remain
  unproved.
- MB-S9-single-summand-provider-under-cfc proves
  `singleSummandMatrixMGFVarianceProxy_of_bernsteinMatrixExp_le_quadratic`,
  the single-summand MGF variance-proxy provider under an explicit pointwise
  `bernsteinMatrixExp_le_quadratic_statement` assumption. The Bernstein CFC
  primitive itself, trace-mgf provider, Golden-Thompson, Lieb, full CFC-free
  single-summand provider, and Matrix Bernstein remain unproved.
- MB-S9-rhs-normalization-proof adds `bernsteinMGFCoeff`,
  `bernsteinMGFCoeff_nonneg`, `TraceMGFBernsteinVarianceProxyBound`,
  `TraceMGFBernsteinVarianceProxyBoundLIntegral`,
  `traceMGFBernsteinVarianceProxyBound_statement`, and
  `matrixBernsteinTraceMGFWithBernsteinCoeff_statement` for the bounded
  Matrix Bernstein denominator target. The retained
  `TraceMGFVarianceProxyBound` and `matrixBernsteinTraceMGF_statement` use
  `theta ^ 2 / 2` and are not the bounded Bernstein RHS. Trace-mgf provider,
  Tropp/Lieb, Bernstein CFC, and Matrix Bernstein remain unproved.
- MB-S9-tropp-shape-refactor adds
  `troppMasterTraceMGFFiniteFamily_statement`, a typed-only finite-family
  Tropp/Lieb iteration interface for the bounded Matrix Bernstein trace-mgf
  route. It preserves `troppMasterTraceMGFStep_statement` as the one-step
  log-form primitive and does not prove Lieb, Golden-Thompson, the trace-mgf
  provider, or Matrix Bernstein.
- MB-S9-trace-mgf-provider-thin-wrapper-proof proves
  `traceMGFBernsteinVarianceProxyBound_of_troppMasterTraceMGFFiniteFamily`
  and
  `matrixBernsteinTraceMGFWithBernsteinCoeff_of_troppMasterTraceMGFFiniteFamily`.
  These are thin wrappers from the finite-family Tropp typed primitive to the
  bounded Bernstein semantic and high-level trace-MGF targets. The
  finite-family Tropp primitive remains typed only, the Bernstein CFC primitive
  remains typed only, and Lieb, Golden-Thompson, and Matrix Bernstein remain
  unproved. Follow-up at the time:
  MB-S9-trace-mgf-to-laplace-tail-contract.
- MB-S9-matrix-bernstein-trace-mgf-under-primitives-proof proves
  `matrixBernsteinTraceMGFWithBernsteinCoeff_under_primitives`, packaging the
  bounded Matrix Bernstein trace-MGF statement from ordinary finite-family
  assumptions plus explicit `troppMasterTraceMGFFiniteFamily_statement` and
  pointwise `bernsteinMatrixExp_le_quadratic_statement` assumptions. The
  finite-family Tropp primitive remains typed only, the Bernstein CFC
  primitive remains typed only, and Lieb, Golden-Thompson, and the Matrix
  Bernstein tail theorem remain unproved. Follow-up at the time:
  MB-S9-trace-mgf-to-laplace-tail-contract.
