[toc]

# 概念

## 凸组合

$$
z_1,\cdots,z_m\in \R^n的凸组合\\
\sum_{i=1}^m \lambda_i z_i,其中\lambda_i \ge 0, \sum_{i=1}^m \lambda_i =1\\
$$

## 凸包

有限点集的所有图组合的集合。

$\R^n$中子集的凸包的每一个点最多可以表现为n+1个点的凸组合。

## Lp范数

$$
\|X\|_{L^p}=(\mathbb{E}|X|^p)^{1\over p}, p\in (0,\infty)\\
\|X\|_{L^\infty}=\text{ess sup} |X|
$$

## 经典向量空间

$$
L^p=\{X:\|X\|_{L^p}<\infty \}
$$

## 尾分布

$$
F_X(t)=\mathbb{P}\{X\le t \}, t\in \R\\
\mathbb{P}\{X> t \}=1-F_X(t)\\
\mathbb{E}X=\int_0^\infty \mathbb{P}\{ X>t\}{\rm d}t-\int_{-\infty}\mathbb{P}\{X<t\}{\rm d}t\\
$$

## 正则图

如果一个图中每个顶点的度都相等的图

## 随机图Erdos-Renyi模型

$G(n,p)$，这种模型由n个顶点构成，每对不同的顶点依概率p随机独立地连接起来。

稀疏图大多数是正则地，稀疏图大多不正则。

## 次高斯性质

设X是一个随机变量，下列结论是等价的，其中的参数$K_i>0$至多相差一个绝对常数因子。

1. X的尾分布满足
   $$
   P(|X|\ge t)\le 2\exp (-{t^2\over K_1^2}), \forall t\ge 0\\
   $$

2. X的矩满足
   $$
   \|X\|_{L^p}=(\mathbb{E}|X|^p)^{1\over p} \le K_2 \sqrt{q}, \forall p\ge 1\\
   $$

3. $X^2$的矩母函数满足
   $$
   \mathbb{E}\exp(\lambda^2 X^2) \le \exp (K_3^2 \lambda^2),对所有满足|\lambda|\le {1\over K_3}的\lambda
   $$

4. $X^2$的矩母函数在某点有限，即
   $$
   \mathbb{E}\exp({X^2\over K_4^2})\le 2
   $$

5. 进一步，如果$\mathbb{E}X=0$，那么下面的结论也等价。$X$的矩母函数满足
   $$
   \mathbb{E}\exp(\lambda X)\le \exp(K_5^2 \lambda^2), \forall \lambda \in \R\\
   $$

## 次高斯随机变量

满足次高斯性质的变量称为次高斯随机变量。

## 次高斯范数

$$
\|X\|_{\psi_2}=\inf\{t>0:\mathbb{E}({X^2\over t^2})\le 2 \}
$$

## 次指数性质

设X是一个随机变量，下列结论是等价的，其中的$K_i>0$至多相差一个绝对常数因子。

1. X的尾分布满足
   $$
   \mathbb{P}\{|X|\ge t\} \le 2\exp (-{t\over K_1}), \forall t \ge 0\\
   $$

2. X的矩满足
   $$
   \|X\|_{L^p}=(\mathbb{E}|X|^p)^{1\over p}\le K_2 p,\forall p\ge 1\\
   $$

3. $|X|$的矩母函数满足
   $$
   \mathbb{E}(\lambda |X|)\le \exp (K_3\lambda),对所有满足0\le \lambda \le {1\over K_3}的\lambda\\
   $$

4. $|X|$的矩母函数在某点有限，即
   $$
   \mathbb{E}\exp\left({|X|\over K_4} \right)\le 2
   $$

5. 如果$\mathbb{E}X=0$，X的矩母函数满足
   $$
   \mathbb{E}\exp(\lambda X)\le \exp (K_5^2 \lambda^2), 对所有满足|\lambda|\le {1\over K_5}的\lambda\\
   $$

## 次指数随机变量

满足次指数性质的随机变量X

## 次指数范数

$$
\|X\|_{\psi_1}=\inf \left\{t>0:\mathbb{E}\left({|X|\over t} \right) \le 2 \right\}
$$

## Orilicz函数

一个函数$\psi:[0,\infty)\to [0,\infty)$，称为一个Orlicz函数

如果$\psi$是凸的，递增的，且当$x\to \infty$满足
$$
\psi(0)=0,\psi(x)\to \infty
$$

## Orilicz范数

一个随机变量X的Orlicz范数定义为：
$$
\|X\|_{\psi}:=\inf\left\{t>0:\mathbb{E}\psi\left({|X|\over t} \right)\le 1 \right\}
$$

## Orilicz空间

$L_{\psi}=(\Omega, \Sigma, \mathbb{P})$是由概率空间熵$(\Omega, \Sigma,\mathbb{P})$上具有有限Orilicz范数的所有随机变量X组成的集合。即
$$
L_{\psi}:=\{X:\|X\|_{\psi}<\infty \}
$$

## 各向异性分布

$\R^n$上随机向量X的范数的集中，当$\|X\|_2^2$以$O(\sqrt{n})$为偏差围绕$n$时，$\|X\|_2$以$O(1)$为偏差围绕$\sqrt{n}$

## 高维空间的协方差矩阵

$$
X\in \R^n, cov(X)=\mathbb{E}((X-\mu)(X-\mu)^T)=\mathbb{E}XX^T-\mu\mu^T\\
\mu=\mathbb{E}X\\
$$

## 二阶矩矩阵

$$
\Sigma=\Sigma(X)=\mathbb{E}XX^T\\
$$

对称半正定，根据谱理论，其各个特征值$s_i$是非负实数，$u_i\in \R^n$是$s_i$的特征向量。
$$
\Sigma=\sum_{i=1}^{n}s_iu_iu_i^T
$$

## 主方向

谱分解的最大特征值所对应的主方向。

## 主成分分析法

PCA，会计算前几个主方向，并将$\R^n$中的数据投影到由此生成的子空间E中。生成子空间是二维或三维的，那么PCA还使得数据可视化的。

## 各向同性

$\R^n$中的随机向量X满足
$$
\Sigma(X)=\mathbb{E}XX^T=I_n
$$

## 球面分布

在$\R^d$中，球面分布定义在半径为$\sqrt{n}$，原点为中心的球面上：
$$
X\sim Unif(\sqrt{n}S^{n-1})\\
S^{n-1}=\{x\in \R^n:\|x\|=1\}
$$
若随机向量X服从球面分布，则其分布具有旋转对称性，即对任意正交矩阵Q，QX与X同分布。

## 对称伯努利分布

随机变量$X=(X_1,\cdots,X_n)$，若坐标$X_i$是独立的、对称的伯努利随机变量，称X在$\R^n$中的单位立方体上服从均匀分布：
$$
X\sim Unif(\{-1, 1\}^n)
$$
也是各向同性

## 框架

一般形式的离散的、各向同性的分布来自于信号处理领域，被称为框架。

一个$\R^n$中的向量集$\{u_i\}_{i=1}^N$是一个框架，如果近似满足Parseval条件，即存在常数$A,B>0$，使得对于任意$x\in \R^n$都有
$$
A\|x\|_2^2 \le \sum_{i=1}^N \lang u_i, x\rang^2 \le B\|x\|_2^2,\forall x \in \R^n\\
$$
A，B称为框架界。当A=B时，这个框架称为紧框架。

## 凸体

$\R^n$空间中含有内点的非空有界凸集K。

设X为在K上按照K的标准体积确定的概率测度服从均匀分布的随机向量，即
$$
X\sim Unif(K)\\
$$
记X的协方差矩阵为$\Sigma$，随机向量$Z:=\Sigma^{-{1\over 2}}X$是各向同性的，即Z在K经过线性变换后的集合上服从均匀分布，即
$$
Z\sim Unif(\Sigma^{-{1\over 2}}K)
$$
各向同性凸体式凸体K的良好条件。

## 高维次高斯分布

如果对所有$x\in \R^n$，一维边际向量$\lang X,x\rang$都是次高斯随机变量，则称$\R^n$中的随机向量X为次高斯随机变量。

定义X的次高斯范数为
$$
\|X\|_{\psi_2}=\sup_{x\in S^{n-1}}\|\lang X, x\rang\|_{\psi_2}\\
$$

## 半正定规划

最优化问题
$$
\max \lang A, X\rang:X \succeq 0,\lang B_i,X
$$

$$
\rang =b_i,i=1,\cdots,n\\
A,B_i:给定的n\times n矩阵\\
b_i:给定的实数\\
X:n\times n半正定矩阵\\
\lang A, X\rang = tr(A^TX)^T:是n\times n矩阵空间上的典范内积\\
$$

半正定规划都是凸规划

## 半正定放松

$$
\max \sum_{i,j=1}^n A_{ij}\lang X_i, X_j\rang: \|X_i\|_2=1对i=1,\cdots,n\\
$$

等价于
$$
\max \lang A, X\rang: X\succeq 0, X_{ii}=1对i=1,\cdots,n\\
$$

## 张量

张量是一个多维数组，一个k阶张量$a_{i_1\cdots i_k}$是k维数组，比如$(a_{ij})$是二阶张量。

## 张量内积

由$\R^{n_1\times \cdots \times n_k}$空间的典范内积定义：
$$
\lang A , B\rang :=\sum_{i_1,\cdots, i_k}a_{i_1\cdots i_k}b_{i_1\cdots i_k}
$$
假设$m\times n$矩阵的张量内积为
$$
\lang A, B\rang =tr(A^TB)=\sum_{i=1}^m\sum_{j=1}^nA_{ij}B_{ij}\\
$$

## 张量积

每个向量$u\in \R^n$定义了一个k阶张量积$u\otimes \cdots \otimes u$，它是一个张量，其元素为u的所有元素的k元组的乘积，换句话说
$$
u\otimes \cdots \otimes u=u^{\otimes k}:=(u_{i_1}\cdots u_{i_k})\in \R^{n\times \cdots \times n}
$$

## 奇异值分解

设A是一个$m\times n$实值矩阵，可以被携程
$$
A=\sum_{i=1}^r s_i u_iv_i^T,r=rank(A)
$$
其中非负数$s_i$是A的奇异值，向量$u_i\in \R^m$是左奇异向量，$v_i\in \R$是A的右奇异向量。

$u_i$是$AA^T$的标准正交特征向量，$v_i$是矩阵$A^TA$的标准正交特征向量，奇异值是$AA^T$或$A^TA$特征值的平方根。

## 算子范数

诱导范数/谱范数，矩阵A作为一个线性算子，它的算子范数为
$$
\|A\|=s_1(A):A的最大奇异值\\
$$

## Moore-Penrose广义逆矩阵

对于$m\times n$的矩阵A，
$$
A=U\Sigma V^*:
U:m\times m酉矩阵\\
\Sigma:m\times n对角矩阵,对角线元素是奇异值\sigma_1\ge \sigma_2\ge \cdots \ge \sigma_r>0\\
V:n\times n酉矩阵\\
$$
广义逆定义为
$$
A^+=V\Sigma^+U^*\\
\Sigma^+:n\times m矩阵，由\Sigma转置，并将每个非零奇异值\sigma_i替换为{1\over \sigma_i}\\
A*:A的共轭转置矩阵\\
$$

## 弗罗贝尼乌斯范数

$$
\|A\|_F=\left(\sum_{i=1}^m\sum_{j=1}^n |A_{ij}|^2 \right)^{1\over 2}\\
\|A\|_F=\left(\sum_{i=1}^rs_i(A)^2 \right)^{1\over 2}
$$

## 等距

设A为$m\times n$矩阵，$m\ge n$，下面的陈述是等价：

1. $A^TA=I_n$

2. $P:=AA^T$是$\R^m$中维数为$n$的子空间上的正交投影。

3. A是等距的，也称为A是将$\R^n$等距嵌入到$\R^m$的，即
   $$
   \|Ax\|_2=\|x\|_2,\forall x \in \R^n\\
   $$

4. A的所有奇异值等于1

## 近似等距

设A为一个$m\times n$矩阵，$\delta>0$，如果
$$
\|A^TA-I_n\|\le \max (\delta, \delta^2)\\
\Rightarrow (1-\delta)\|x\|_2 \le \|Ax\|_2 \le (1+\delta)\|x\|_2,\forall x\in \R^n\\
1-\delta \le s_n(A)\le s_1(A)\le 1+\delta
$$
如果$1-\delta \le s_n(A)\le s_1(A)\le 1+\delta$成立，那么
$$
\|A^TA-I_n\|\le 3\max(\delta, \delta^2)\\
$$

## 投影与等距

考虑一个$n\times m$矩阵$Q$，那么$QQ^T=I_n$，当且仅当
$$
P:=Q^TQ
$$
是$\R^m\to \R^n$维子空间上的正交投影。矩阵Q通常被称为$\R^m$到$\R^n$的投影。

当且仅当$A^T$是$\R^m$到$\R^n$的投影时，它才是$\R^n$到$\R^m$的等距嵌入。

## $\varepsilon$-网

设$(T,d)$时一个度量空间，考虑子集$K\sub T$，并设$\varepsilon$>0，称子集$\mathcal{N}\sube K$为$K$的一个$\varepsilon$-网，如果$K$中的每个点都与$\mathcal{N}$中某个点的距离不超过$\varepsilon$，即
$$
\forall x \in K, \forall x_0\in \mathcal{N}:d(x,x_0)\le \varepsilon
$$
等价地，$\mathcal{N}$是$K$的一个$\varepsilon$-网，当且仅当仅当K可以被若干个中心在$\mathcal{N}$内，半径为$\varepsilon$的球覆盖。

## 覆盖数

K的所有$\varepsilon$-网的最小元素数称为K的覆盖数，记作$\mathcal{N}(K,d,\varepsilon)$，等价地，$\mathcal{N}(K,d,\varepsilon)$是中心在$K$内、半径为$\varepsilon$、覆盖K的闭球的最小数量。

## 紧致性

度量空间$(T,d)$的子集K是预紧的（即K的闭包是紧的），当且仅当$\mathcal{N}(K,d,\varepsilon)>0,\forall \varepsilon>0$

可作为K的紧致性的量化目标。

## 填充数

若度量空间$(T,d,\varepsilon)$的子集$\mathcal{N}$对所有不同的点$x,y$有$d(x,y)>\varepsilon$。则称$\mathcal{N}$是$\varepsilon$-分离的。集合$K\sub T$的$\varepsilon$-分离子集的最大可能数目为K的填充数，记作$\mathcal{P}(K,d,\varepsilon)$

## Minkowski和

设$A$和$B$是$\R^n$的子集，Minkowski和$A+B$定义为
$$
A+B:=\{a+b:a\in A, b\in B \}
$$

## 汉明立方体

汉明立方体$\{0,1\}^n$由所有长度为n的二进制字符串组成。

## 汉明距离

两个二进制字符串$x,y$的汉明距离$d_H(x,y)$定义为两者的不一致的二进制位数，集
$$
d_H(x,y):=\#\{i:x(i)\ne y(i) \}\ \ x,y\in \{0,1\}^n\\
$$

## 汉明空间

度量空间$(\{0,1 \}^n,d_H)$

## 度量熵

覆盖数的对数通常反应了K的度量熵。度量熵等于编码K中的点所需饿比特数。

设$(T,d)$是一个度量空间，考虑其熵的子集$K\sub T$，用$\mathcal{C}(K,d,\varepsilon)$表示可以按度量$d$以精度为$\varepsilon$表述每个点$x\in K$的最小比特数，那么有
$$
\log_2 \mathcal{N}(K,d,\varepsilon)\le \mathcal{C}(K,d,\varepsilon)\le \log_2 \mathcal{N}(k,d,{\varepsilon\over 2})
$$

## 纠错码

设k，n和r是固定整数，如下两个映射：
$$
E:\{0,1\}^k\to \{0,1\}^n,D:\{0,1\}^n\to \{0,1\}^k
$$
称为能纠正r个错误的编码和解码映射，如果对于任意$x\in \{0,1\}^n$，以及任意$y\in \{0,1\}^k$，其中$y$与$E(x)$至多有r个字符不同，都有
$$
D(y)=x
$$
编码映射E称为纠错码，它的像$E(\{0,1\}^k)$被称为编码本，像中的元素$E(x)$被称为码字。

## 随机分块模型

把n个点分配到两个集合（社区），且每个集合各有${n\over 2}$个点。以下列方式构造随机图$G$：对任意两个点，若它们属于同一社区，则以概率$p$将其连接；若它们属于不同社区，则以概率$q$将其连接起来。并记为$G(n,p,q)$

其邻接矩阵是一个随机矩阵。
$$
A=D+R\\
D=\mathbb{E}A
$$

## 扰动理论

描述了在随机矩阵的扰动下，矩阵的特征值和特征向量如何变化。

## 谱

矩阵特征值的集合

## 谱聚类算法

1. 根据邻接矩阵和度矩阵计算拉普拉斯矩阵。
2. 特征分解：选取前k个最小特征值对应的特征向量。
3. 原始数据映射到由这些特征变量构成的低维空间。
4. 在低维空间使用K-means聚类算法进行聚类。

## 利普希茨函数

设$(X,d_X)$和$(Y,d_Y)$为两个度量空间，函数$f:X\to Y$被称为利普希茨函数，如果$L\in \R$，使得
$$
d_Y(f(u),f(v))\le Ld_X(u,v),\forall u,v \in X\\
$$
满足上述不等式的所有L的下确界被称为$f$的利普希茨范数，记为$\|f\|_{Lip}$

## 矩阵函数

考虑函数$f:\R\to \R$和具有特征值$\lambda_i$和相应特征值向量$\mu_i$的$n\times n$对称矩阵$X$。$X$可以表示为谱分解
$$
X=\sum_{i=1}^n\lambda_i u_iu_i^T\\
$$
定义
$$
f(X):=\sum_{i=1}^nf(\lambda_i)u_iu_i^T
$$

## 混沌

设$A=(a_{ij})$是一个$n\times n$系数矩阵，$X=(X_1,\cdots, X_n)$是一个具有独立坐标的随机向量，则$X^TAX=\sum_{i,j=1}^n a_{ij}X_i X_j$，称为混沌。

## 解耦

设A是一个$n\times n$无对角矩阵（即A的对角元素等于零），$X=(X_1,\cdots,X_n)$是一个$X_i$相互独立并具有零均值的随机向量，那么对任意的凸函数$F:\R\to \R$，有
$$
\mathbb{E}F(X^TAX)\le \mathbb{E}F(4X^TAX')\\\
X'是X的独立副本\\
$$

## 比较

考虑$\R^n$中独立零均值的次高斯随机向量$X,X'$，满足$\|X\|_{\psi_2}\le K$，$\|X'\|_{\psi_2}\le K$，同时考虑相互独立的随机向量，$g,g'\sim N(0,I_n)$，设A是一个$n\times n$矩阵，则有
$$
\mathbb{E}\exp(\lambda X^T AX')\le \mathbb{E}\exp (CK^2 \lambda g^T Ag')\\
对任意\lambda \in \R成立
$$

## 对称

如果$X$和$-X$具有相同的分布。

## 随机过程

是同一个概率空间上的随机变量的集合$(X_t)_{t\in T}$，其指数是某个集合$T(T\sub \R^n)$的元素t

## 离散时间

$$
T=\{1,\cdots,n\}
$$

## 随机变量序列

如果$T=\N$，则离散时间的随机过程$(X_n)_{n\in \N}$就是随机变量序列，

## 随机游动

$$
X_n:=\sum_{i=1}^n Z_i\\
Z_i:独立零均值的随机变量\\
$$

## 标准布朗运动

维纳过程，具有连续的样本路径，即随机函数$f(t):=X_t$几乎必然连续，增量是独立的，并且对所有的$t\ge s$，满足$X_t-X_s\sim N(0,t-s)$

## 随机场

当指数集$T$是$\R^n$的子集时，随机过程称为随机场

## 随机过程的协方差

假设$\mathbb{E}X_i=0,\forall t\in T$，协方差函数定义为
$$
\Sigma(t,s):=\text{cov}(X_t,X_s)=\mathbb{E}X_tX_s, t,s\in T\\
$$

## 随机过程的增量

$$
d(t,s):=\|X_t-X_s\|_2=(\mathbb{E}(X_t-X_s)^2)^{1\over 2}\\
$$

## 高斯过程

如果任何有限子集$T_0\sub T$，随机向量$(X_t)_{t\in T_0}$具有正态分布，则随机过程$(X_t)_{t\in T}$被称为高斯过程。

## 典范高斯过程

考虑标准正态随机向量$g\sim N(0,I_n)$，定义随机过程X
$$
X_t:=\lang g,t\rang, t\in T\\
$$
增量
$$
\|X_t-X_s\|_2 =\|t-s\|_2, t,s\in T\\
$$

## 高斯随机向量

设$Y$是$\R^n$中的零均值高斯随机向量，那么存在点$t_1,\cdots, t_n\in \R^n$，使得
$$
Y\equiv (\lang g, t_i\rang)_{i=1}^n, g\sim N(0,I_n)\\
$$

## 高斯分部积分

设$X\sim N(0,1)$，那么对任意可微函数$f:\R\to \R$，有
$$
\mathbb{E}f'(X)=\mathbb{E}Xf(X)
$$
多维版本，设$X\sim N(0,\Sigma)$，那么对任意可微函数$f:\R^n\to \R$，有
$$
\mathbb{E}Xf(X)=\Sigma\mathbb{E}\nabla f(X)
$$

## 高斯正交系

GOE，控制对称$n\times n$高斯随机矩阵$A$的范数，其对角线上方元素是独立的服从$N(0,1)$分布的随机变量，其对角线元素是独立的服从$N(0,2)$的随机变量。

## 典范度量

定义了一个指标集$T$上的度量。

## 高斯宽度

子集$T\sub \R^n$的高斯宽度定义为w
$$
w(T):=\mathbb{E}\sup_{x\in T}\lang g,x\rang, g\sim N(0,I_n)
$$
满足6个性质：

1. T有界，当且仅当高斯宽度有界。

2. 仿射酉变换下，高斯宽度是不变的。

3. 高斯宽度取凸包后不变。

4. 具有绝对齐次性和Minkowski可加性。即对$T,S\sub \R^n,a\sub \R$，有
   $$
   w(T+S)=w(T)+w(S)\\
   w(aT)=|a|w(T)\\
   $$

5. 有
   $$
   w(T)={1\over 2}w(T-T)={1\over 2}\mathbb{E}\sup_{x,y\in T}\lang g, x-y\rang
   $$

6. 高斯宽度有界
   $$
   {1\over \sqrt{2\pi}}\text{diam} (T)\le w(T)\le {\sqrt{n}\over 2}\text{diam}(T)\\
   \text{diam}(T)=\sup\{\|x-y\|_2:x,y\in T \}
   $$

## 球面宽度

设$\R^n$的子集$T$的球面宽度定义为w
$$
W_s(T):=\mathbb{E}\sup_{x\in T}\lang \theta, x\rang \ \ \theta \sim \text{Unif}(S^{n-1})\\
$$

## $\mathscr{l}_p$球

设$1\le p <\infty$，
$$
B_p^n:=\{x\in \R^n:\|x\|_p\le 1 \}
$$

## 稳定维数

对于有界集$T\sub \R^n$，T的稳定维数定义为
$$
d(T):={h(T-T)^2 \over \text{diam}(T)^2 }\sim {w(T)^2 \over \text{diam}(T)^2 }
$$
稳定维数比代数维数更稳定。

## 稳定秩

一个$m\times n$矩阵A的稳定秩定义为
$$
r(A):={\|A\|_F^2\over \|A\|^2}
$$

## 高斯复杂度

$T$为$\R^n$中的子集，则其高斯复杂度定义为\
$$
\gamma(T):= \mathbb{E}\sup_{x\in T}|\lang g,x\rang|, \ \ g\sim N(0,I_n)\\
$$

## 随机投影

考虑集合$T\sub \R^n$，将其投影到$\R^n$中的一个随机$m$维子空间（一致地从格拉斯曼流形$G_{n,m}$中选取）。P当作降维的一种方式，投影集为$PT$

## 相变

$$
\text{diam}(PT)\le 
\begin{cases}
C\sqrt{m\over n}\text{diam}(T)& m \ge d(T)\\
Cw_s(T)& m\le d(T)\\
\end{cases}
$$

## 次高斯增量

考虑一个度量空间$(T,d)$上的随机过程$(X_t)_{t\in T}$，若存在$K\ge 0$，使得
$$
\|X_t-X_x\|_{\phi_2}\le K ,\forall t,s\le T
$$
称这个过程具有次高斯增量。

## 经验过程

设$\mathcal{F}$是一类实值函数$f:\Omega \to \R$，其中$(\Omega, \Sigma, \mu)$是概率空间，$X$是由其分布$\mu$确定的$\Omega$中一个随机点，设$X_1,X_2,\cdots,X_n$是$X$的独立副本。随机过程$(X_f)_{f\in \mathcal{F}}$按如下定义
$$
X_f:={1\over n}\sum_{i=1}^n f(X_i)\to \mathbb{E}f(X)
$$
称其为$\mathcal{F}$上的经验过程。

## 经验测度

考虑均匀分布在样本$X_1,\cdots,X_N$的概率测度$\mu_n$
$$
\mu_n(\{X_i\})={1\over n},对每个i=1,\cdots,n\\
$$
$\mu_n$是随机测度。

## Wasserstein距离

$$
W_1(\mu, \mu_n)=\mathbb{E}\sup_{f\in\mathcal{F}}|\mu_nf-\mu f|\\
$$

被认为是测度$\mu_n$和$\mu$之间的距离，与测度$\mu$与测度$\mu_n$的传递成本等价。

## VC维数

考虑在某个定义域$\Omega$上的布尔函数类$\mathcal{F}$。对于$\Omega$的子集$\Lambda \sube \Omega$，如果任意一个函数$g:\Lambda\to \{0,1\}$都可以通过把$\mathcal{F}$中的某个函数$f$限制到$\Lambda$得到，就称$\Omega$的子集$\Lambda$被$\mathcal{F}$散离了。$\mathcal{F}$的VC维数记为$vc(\mathcal{F})$，是能被$\mathcal{F}$散离的子集$\Lambda \sube \Omega$的最大势。

## 经验风险

函数$T:\Omega \to \R$的经验风险定义为：
$$
R_n(f):={1\over n}\sum_{i=1}^n (f(X_i)-T(x_i))^2\\
T(x):目标函数\\
f_n^*:=\arg \min_{f\in \mathcal{F}}R_n(f)\\
$$

## 额外风险

从有限样本$n$中得到的额外风险
$$
R(f_n^*)-R(f^*)\\
$$

## 链方法

有一个随机过程$(X_t)_{t\in T}$，其中$T$是一个指标集（可能是无限的、高维的）。我们关心$\mathbb{E}\sup_{t\in T}X_t$的界。如果$T$很大，直接取上确界，很难处理，于是把$T$用越来越细的网格（或划分）来近似，从粗糙到精细一层层“链接”起来，利用每一步的增量控制总的变换。

## 容许序列

固定某个子集$T_k\sub T$，使得
$$
|T_0|=1,|T_k|\le 2^{2^k},\ \ k=1,2,\cdots\\
$$
这样得到的集合序列$(T_k)_{k=1}^\infty$称为容许序列。

## Talagrand $\gamma_2$-泛函

$T$的$\gamma_2$-泛函定义为
$$
\gamma_2(T,d)=\inf_{(T_k)}\sup_{t\in T}\sum_{k=0}^\infty 2^{k\over 2}d(t,T_k)\\
$$
其中，下确界是对所有容许序列取值。

## 随机矩阵的核

将随机子空间$E$作为某个随机矩阵的核，即设
$$
E:=\text{ker}\ A\\
$$
其中$A$是$m\times n$维的随机矩阵，一般地，我们总有
$$
\text{dim}(E)\ge n-m
$$
且对于连续分布，$\text{dim}(E)=n-m$几乎处处成立。

## M*界

考虑一个集合$T\sub \R^n$。设$A$是$m\times n$维矩阵，其行向量$A_i$是$\R^n$中相互独立、各向同性的次高斯随机向量，那么随机子空间$E=\text{ker} A$满足
$$
\mathbb{E}\text{diam}(T\cap E)\le {CK^2 w(T)\over \sqrt{m}}\\
K=\max_i\|A_i\|_{\psi_2}
$$

## 高维信号恢复

将信号建模为向量$x\in \R^n$，假设事先不知道$x$，但是我们有$m$个随机的、线性的、可能有噪声的$x$测量值，这样的测量值可以表示为$y\in \R^m$，其形式如下：
$$
y=Ax+w
$$
这样$A$是已知的$m\times n$随机测量矩阵，$w\in \R^m$是未知的噪声向量，我们的目标是尽可能地从$A$和$y$恢复$x$

## 向量的稀疏性

$$
\|x\|_0:=|\{i:x_i\ne 0\}|
$$

## 核范数

迹范数
$$
\|X\|_*:=\text{tr}\sqrt{X^TX}\\
$$


## RIP

如果下面不等式对所有使得$\|v\|_0\le s$的向量$s\in \R^n$，则称$m\times n$的矩阵$A$关于参数$\alpha,\beta$和$s$满足RIP：
$$
\alpha\|v|_2 \le \|Av\|_2\le \beta \|v\|_2
$$

## 正齐次函数

设$V$是一个向量空间，函数$f:V\to \R$，如果$f(\alpha x)=\alpha f(x),\forall \alpha \ge 0, x\in V$都成立，那么函数被称为正齐次函数。

## 次可加函数

$$
f(x+y)\le f(x)+f(y), \forall x,y \in V
$$



# 定理

## 经典不等式

詹森不等式
$$
凸\phi:\R\to\R, 任意随机变量X\\
\phi(\mathbb{E}X)\le \mathbb{E}\phi(X)\\
$$
闵可夫斯基不等式
$$
\|X+Y\|_{L^p}\le \|X\|_{L^p}+\|Y\|_{L^p}
$$
柯西-施瓦茨不等式
$$
|\mathbb{E}XY|\le \|X\|_{L^2}\|Y\|_{L^2}\\
$$
赫尔德不等式
$$
{1\over p}+{1\over q}=1\\
|\mathbb{E}XY|\le \|X\|_{L^p}\|Y\|_{L^q}\\
$$

## 泊松极限定理

$$
X_n \sim B(n,p_n)\\
n\to \infty, p_n \to 0, np\to \lambda \\
X_n\to {\rm Poisson}(\lambda)
$$

## 集中不等式

量化了随机变量如何偏离它的均值μ，例如切比雪夫不等式。

## Bery-Esseen中心极限定理

$$
X_1,X_2,...,独立同分布,均值为\mu, 方差为\sigma^2 \\
S_N=X_1+\cdots +X_N \\
Z_N:={S_N-\mathbb{E}S_N \over \sqrt{{\rm Var}(S_N)} }\\
\mathbb{P} \{g\ge t\}={1\over \sqrt{2\pi} } \int_t^\infty e^{-{x^2\over 2}}{\rm d}x \\
|\mathbb{P}\{Z_n\ge  t \}-\mathbb{P}\{g\ge t \}|\le {\rho \over \sqrt{N}}\\
\rho = {\mathbb{E}|X_1-\mu|^3\over \sigma^3}, g\sim N(0,1)\\
$$

## 霍夫丁不等式

对称伯努利分布，-1和1的概率各为1/2
$$
X_1,X_2,\cdots,X_N是独立的对称伯努利分布\\
记a=(a_1,a_2,\cdots,a_N)\in \R^N\\
\forall t\ge 0,\\
\mathbb{P}\left\{\sum_{i=1}^N a_iX_i\ge t \right\}\le \exp \left({t^2\over2\|a\|^2_2} \right)
$$

## 切尔诺夫不等式

设$X_i$是参数为$p_i$的独立伯努利随机变量
$$
S_N=\sum_{i=1}^N X_i, \mu = \mathbb{E}S_N,\forall t>\mu\\
\mathbb{P}\{S_N\ge t \}\le e^{-\mu}\left({e\mu\over t} \right)^t\\
$$

## Boosting随机算法

假设对于某个$\delta >0$，随机算法做出决策并输出正确答案的概率是${1\over 2}+\delta$，这比随机猜测要好。为了提升算法的性能，我们运行了N次，并进行了服从多数的表决。

对于任意$\varepsilon \in (0,1)$，只要$N\ge ({1\over 2})\delta^{-2}\ln (\varepsilon^{-1})$，答案是正确的概率为$1-\varepsilon$

## 泊松分布的界

设$X\sim Pois(\lambda)$，对任意的$t>\lambda$，都有：
$$
P(X\ge t)\le e^{-\lambda} ({e\lambda \over t})^t
$$


## 正态分布的矩

对所有$p\ge 1$，随机变量$X\sim N(0,1)$，满足
$$
\|X\|_{L^p}=(\mathbb{E}|X|^p)^{1\over p}=\sqrt{2}\left({\Gamma ({1+p\over 2})\over \Gamma({1\over 2})} \right)^{1\over 2}\\
\|X\|_{L^p} =O(\sqrt{q}), p\rightarrow \infty\\
$$

$$
X\sim N(0,1), \mathbb{E}\exp (\lambda X) = e^{\lambda^2 \over 2}, \forall \lambda \in \R\\
$$

## 次高斯分布的例子

- 高斯分布

- 伯努利分布

- 任意有界随机变量都是次高斯随机变量，且
  $$
  \|X\|_{\psi_2} \le C\|X\|_{\infty}\\
  C={1\over \sqrt{\ln 2}}
  $$

## 独立次高斯分布的和

设$X_1,\cdots, X_N$为独立的零均值次高斯随机变量，那么$\sum_{i=1}^N X_i$也是次高斯随机变量，而且
$$
\left\|\sum_{i=1}^N X_i \right\|^2_{\psi_2}\le C\sum_{i=1}^N \|X_i\|^2_{\psi_2}
$$

## 广义霍夫丁不等式一

设$X_1,\cdots, X_N$为独立的零均值次高斯随机变量，对任意$t\ge 0$，必有
$$
\mathbb{P}\left\{\left|\sum_{i=1}^N X_i \right|\ge t \right\}\le 2 \exp \left(-{ct^2\over \sum_{i=1}^2 \|X_i\|^2_{\psi_2}} \right)
$$

## 广义霍夫丁不等式二

设$X_1,\cdots, X_N$为独立的零均值次高斯随机变量，并且记$a=(a_1,\cdots,a_N)\in \R^N$，那么对任意$t\ge 0$，必有
$$
\mathbb{P}\left\{\left|\sum_{i=1}^N a_i X_i \right|\ge t \right\}\le 2 \exp \left(-{ct^2\over K^2\|a\|_2^2} \right)\\
K=\max_i\|X_i\|_{\psi_2}
$$

## 辛钦不等式

设$X_1,\cdots, X_N$为零均值和单位方差的独立次高斯随机变量，并且记$a=(a_1,\cdots,a_N)\in \R^N$，对任意$p\in [2,\infty)$，必有
$$
\left(\sum_{i=1}^N a_i^2 \right)^{1\over 2} \le \left\|\sum_{i=1}^N a_i X_i \right\|_{L^p} \le CK\sqrt{p} \left(\sum_{i=1}^N a_i^2 \right)^{1\over 2}\\
K=\max_i \|X_i\|_{\psi_2}
$$

## 中心化

如果$X$是一个次高斯随机变量，那么$X-\mathbb{E}X$也是次高斯的，且
$$
\|X-\mathbb{E}X\|_{\psi_2}\le C\|X\|_{\psi_2}\\
C是绝对常数\\
$$
如果X是一个次指数随机变量，那么$X-\mathbb{E}X$也是次指数的，且
$$
\|X-\mathbb{E}X\|_{\psi_1}\le C\|X\|_{\psi_1 }
$$

## 次指数分布与次高斯分布的关系

X是次高斯的随机变量当且仅当$X^2$是次指数的，
$$
\|X^2\|_{\psi_1}=\|X\|_{\psi_2}^2
$$
如果X和Y是两个次高斯随机变量，那么XY是次指数随机变量，
$$
\|XY\|_{\psi_1}\le \|X\|_{\psi_2}\|Y\|_{\psi_2}
$$

## 空间关系

$$
L^{\infty}\sub L_{\psi_2}\sub L^p,\forall p\in [1,\infty)\\
$$

有限随机变量空间 $\sub$ 次高斯随机变量空间 $\sub$ Banach空间

## 伯恩斯坦不等式

设$X_1,\cdots, X_N$是独立的零均值次指数随机变量，对任意的$t\ge 0$，均有
$$
\mathbb{P}\left\{\left|\sum_{i=1}^N X_i \right|\ge t \right\}\le 2 \exp\left(-c \min\left({t^2\over \sum_{i=1}^N\|X_i\|^2_{\psi_1}}, {t\over \max_i \|X_i\|_{\psi_1}} \right) \right)
$$
设$X_1,\cdots, X_N$是独立的零均值次指数随机变量，记$a=(a_1,\cdots, a_N)\in \R^N$，对任意的$t\ge 0$，均有
$$
\mathbb{P}\left\{\left|\sum_{i=1}^N a_iX_i \right|\ge t \right\}\le 2 \exp\left(-c \min\left({t^2\over K^2\|a\|_2^2}, {t\over K\|a\|_{\infty} } \right) \right)\\
K=\max_i\|X_i\|_{\psi_1}
$$
设$X_1,\cdots, X_N$是独立的零均值次指数随机变量，对任意的$t\ge 0$，均有
$$
\mathbb{P}\left\{\left|\sum_{i=1}^N {1\over N}X_i \right|\ge t \right\}\le 2 \exp\left(-c \min\left({t^2\over K^2}, {t\over K } \right)N \right)\\
K=\max_i\|X_i\|_{\psi_1}
$$
设$X_1,\cdots, X_N$是独立的零均值次指数随机变量，满足对所有的$i$，均有$|X_i|\le K$，对任意的$t\ge 0$，均有
$$
\mathbb{P}\left\{\left|\sum_{i=1}^N X_i \right|\ge t \right\} \le 2 \exp\left(-{t^2/2\over \sigma^2+Kt/3} \right)\\
\sigma^2=\sum_{i=1}^N \mathbb{E}X_i^2
$$

## 有界随机变量矩母函数的界

设$X$是随机变量，满足$|X|\le K$，当$|\lambda| <{3\over K}$时，
$$
\mathbb{E}\exp(\lambda X)\le \exp (g(\lambda)\mathbb{E}X^2),其中g(\lambda)={\lambda^2/2\over 1-|\lambda|K/3}
$$

## 有界差分不等式

设$X_1,\cdots,X_N$是独立随机变量，$f:\R^n\to \R$为可测量化函数。又设在$x\in \R^n$的单个坐标的任意变化下，$f(x)$的值最多改变$c_i>0$，那么，对任何$t>0$，必有
$$
\mathbb{P}\{f(X)-\mathbb{E}f(X)\ge t \}\le \exp \left(-{2t^2\over \sum_{i=1}^N c_i^2} \right)\\
X=(X_1,\cdots,X_n)
$$

## Bennett不等式

设$X_1,\cdots,X_N$是独立随机变量，满足对每一个$i$，$|X_i-\mathbb{E}X_i|\le K$几乎处处成立，那么，对任意$t>0$，必有
$$
\mathbb{P}\left\{ \sum_{i=1}^N (X_i-\mathbb{E}X_i)\ge t \right\}\le \exp \left(-{\sigma^2\over K^2}h \left({Kt\over \sigma^2} \right) \right)\\
\sigma^2=\sum_{i=1}^N Var(X_i)是和的方差，h(u)=(1+u)\log(1+u)-u\\
$$

## 范数的集中定理

  设随机向量$X=(X_1,\cdots,X_n)\in \R^n$，满足其坐标$X_i$服从次高斯分布，且$\mathbb{E}X_i^2=1$，则有
$$
\left\|\|X\|_2-\sqrt{2} \right\|_{\psi_2}\le C K^2\\
K=\max_i \|X_i\|_{\psi_2}
$$

##  范数的期望

$$
\sqrt{n}-CK^2\le \mathbb{E}\|X\|_2\le \sqrt{n}+CK^2\\
$$

## 范数的方差

$$
Var(\|X\|_2)\le CK^4\\
$$

有限矩假设下，设随机向量$X=(X_1,\cdots,X_n)\in \R^n$有独立坐标$X_i$，且满足$\mathbb{E}X_i^2 =1$与$\mathbb{E}X_i^2 \le K^4$
$$
Var(\|X\|_2)\le CK^4\\
$$

## 小球概率问题

设随机向量$X=(X_1,\cdots,X_n)\in \R^n$的坐标$X_i$具有独立的连续分布，且$X_i$的密度函数以1为一致界，对任意$\varepsilon>0$，有
$$
\mathbb{P}\{\|X\|_2\le \varepsilon \sqrt{n} \}\le (C\varepsilon)^n
$$

## 各向同性

设X是一个随机向量，其协方差矩阵$\Sigma=cov(X)$可逆，检验随机向量
$$
Z:=\Sigma^{-{1\over 2}}(X-\mu)\\
\mu:均值向量
$$
$R^n$中的随机向量X是各向同性的充要条件是
$$
\mathbb{E}\lang X,x\rang^2 =\|x\|_2^2, \forall x\in \R^n
$$
设$X$为$\R^n$中各向同性的随机向量，那么有
$$
\mathbb{E}\|X\|_2^2 =n\\
$$
如果还有一个独立的各向同性的随机向量$Y$，那么有
$$
\mathbb{E}\lang X, Y\rang^2 =n\\
$$

## 独立的各向同性向量

设$X$和$Y$为$\R^n$中的两个独立各向同性的随机向量
$$
\bar{X}:={X\over\|X\|},\bar{Y}:={Y\over \|Y\|}\\
\|X\|_2\sim \sqrt{n},\|Y\|_2\sim \sqrt{n},\lang X,Y\rang\sim \sqrt{n}\\
|\lang \bar{X},\bar{Y}\rang| \sim {1\over \sqrt{n}}
$$
高维空间中独立各向同性的随机向量趋于几乎正交。

设$X$和$Y$为$\R^n$中的两个独立各向同性的零均值随机向量
$$
\mathbb{E}\|X-Y\|_2^2=2n\\
$$
任意的随机向量$X=(X_1,\cdots,X_n)$，其中$X_i$是独立的随机量，并且满足零均值和单位方差，那么X就是$\R^n$中的各向同性随机变量。

## 旋转对称性

对于任意服从多维正态分布的n维向量$g\sim N(0,1)$和给定的正交矩阵$U$，都有
$$
Ug\sim N(0,I_n)\\
$$

1. 对任一服从多维正态分布的n维随机向量$g\sim N(0,I_n)$和给定的$n$维向量$u\in \R^n$，都有
   $$
   \lang g,u\rang \sim N(0,\|u\|_2^2)
   $$

2. 对于相互独立的随机变量$X_i\sim N(0,\sigma_i^2)$，都有
   $$
   \sum_{i=1}^n X_i \sim N(0,\sigma^2),\sigma^2=\sum_{i=1}^n \sigma_i^2
   $$

3. 设G是一个$m\times n$的高斯随机矩阵，即G中的元素均服从标准正态分布$N(0,1)$的独立随机变量，设$u\in \R^n$为一固定的单位向量，那么有
   $$
   Gu\sim N(0, I_m)\\
   $$
   

## 正态分布特性

设$X\sim N(0,I_n)$，对于任意给定的$\R^n$中的向量$u,v$，都有
$$
\mathbb{E}(\lang X, u\rang \lang X, v\rang)=\lang u, v \rang\\
$$

$$
X_u:=\lang X,u\rang\Rightarrow X_u\sim N(0, \|u\|_2^2)\\
\Rightarrow \|X_u-X_v\|_{L^2}=\|u-v\|_2
$$

## 正态分布与球面分布

$$
g\sim N(0,I_n)\\
\mathbb{P}\{|\|g\|_2- \sqrt{n}|\ge t \}\le 2\exp(-ct^2),\forall t \ge 0\\
$$

$$
g=r\theta\\
r=\|g\|_2:向量的长度\\
\theta={g\over \|g\|_2}:方向向量\\
r与\theta相互独立\\
\theta\sim Unif(S^{n-1})\\
r\approx \sqrt{n}\\
N(0,I_n)\approx Unif(\sqrt{n}S^{n-1})\\
$$

## 紧框架

如果
$$
\sum_{i=1}^N u_iu_i^T =A I_n\\
\{u_i \}_{i=1}^N是\R^n空间中的紧框架，A是界。
$$

## 紧框架与各向同性

考虑$\R^n$中的紧框架$\{u_i\}_{i=1}^N$，边界为$A=B$，设X是在框架上均匀分布的随机向量，即
$$
X\sim Unif\{u_i:i=1,\cdots,N\}
$$
那么，$\left({N\over A} \right)^{1/2}$是$\R^n$中的各向同性的随机向量。

考虑$\R^n$中的各向同性的随机向量X，它以概率$p_i$，取有限向量值，其中$i=1,\cdots,N$，那么向量
$$
u_i:=\sqrt{p_i}x_i,i=1,\cdots,N\\
$$
是$\R^n$中以A=B=1为界的紧框架。

## 高维次高斯分布举例

球面上的均匀分布是次高斯分布、欧几里得球上的均匀分布、

## 投影极限定理

当n增大时，球面上均匀分布的边际分布式渐近正太的。

如果$X\sim Unif(\sqrt{n}S^{n-1})$，那么对于给定单位向量x，当$n\to \infty$时有
$$
\lang X,x\rang \to N(0,1)依分布收敛
$$

## 凸集上的均匀分布

设K为凸集，且
$$
X\sim Unif(K)
$$
的所有边际分布都是次指数分布。

## Grothendieck不等式

考虑一个$m\times n$的实数矩阵$(a_{ij})$，假设对任意数$x_i,y_i\in\{-1,1 \}$，我们有
$$
\left|\sum_{i,j}a_{ij}x_iy_i \right|\le 1
$$
那么，对于任意希尔伯特空间H和满足$\|u_i\|=\|v_j\|=1$的任意向量$u_i,v_j\in H$，必有
$$
\left|\sum_{i,j}a_{ij}\lang u_i,v_j\rang \right|\le K
$$
其中$K\le 1.783$是一个绝对常数

## Grothendieck恒等式

考虑一个随机向量$g\sim N(0,I_n)$，那么，对于$S^{n-1}$中的任意固定向量$u,v$，必有
$$
\mathbb{E}(sign\lang g,u\rang sign\lang g,v\rang)={2\over \pi}\arcsin \lang u,v\rang\\
$$

## 核技巧

对于任意向量$u,v\in \R^n$和$k\in \N$，有
$$
\lang u^{\otimes k}, v^{\otimes k}\rang = \lang u,k\rang^k
$$
存在一个希尔伯特空间$H$和一个变换$\Phi:\R^n\to H$，使得
$$
\lang \Phi(u),\Phi(v)\rang = \lang u,v\rang^k
$$
存在一个多项式$f:\R\to \R$和$H,\Phi$，使得：
$$
\lang \Phi(u),\Phi(v)\rang =f(\lang u,v\rang)对所有的u,v\in \R^n\\
$$
对于任意有非负系数的实解析函数，即可表示为收敛级数函数$f:\R\to \R$:
$$
f(x)=\sum_{k=0}^\infty a_k x^k,x\in \R\\
多所有的k,a_k\ge 0
$$
上面结论成立。

更一般的，对于任意有非负系数的实解析函数，存在一个希尔伯特空间$H$和变换$\Phi,\Psi:\R^n\to H$，使得
$$
\lang \Phi(u),\Psi(v)\rang =f(\lang u,v\rang), 对所有u,v\in \R^n\\
\|\Phi(u)\|^2=\|\Psi(u)\|^2=\sum_{k=0}^\infty |a_k|\|u\|_2^{2k}
$$

## Moore-Aronsajn定理

给定一个半正定核$K$，存在唯一的希尔伯特空间$\mathcal{H}$由函数组成，使得：

1. 对所有$x$，函数$k(\cdot, x)\in \mathcal{H}$
2. 对所有$f\in \mathcal{H}$和$x$，有$f(x)=\lang f,k(\cdot, x)\rang_{\mathcal{H}}$
3. $\mathcal{H}$由$\{k(\cdot, x):x\in X\}$张成。

设$K:\mathcal{X}\times \mathcal{X}\to \R$是$\mathcal{X}$集合上的双变量函数，只有K是一个半正定核，可以找到一个希尔伯特空间核一个变换$\Phi:\mathcal{X}\to H$，使得
$$
\lang \Phi(u),\Phi(v)\rang =K(u,v),对所有u,v\in \mathcal{X}
$$

## Mercer定理

如果核函数满足连续、对称、正定的条件，那么存在一组特征函数$\{\phi_i\}_{i=1}^\infty$和对应的特征值$\{\lambda_i\}_{i=1}^\infty$，其中（$\lambda_i\ge 0$且$\sum_{i=1}^\infty \lambda_i^2 <\infty$），使得核$K$可以展开为一下形式的一致收敛级数：
$$
K(x,y)=\sum_{i=1}^\infty \lambda_i\phi_i(x)\phi_i(y)\\
$$
其中，特征函数$\phi_i$构成了$L^2(\mathcal{X})$空间的一组标准正交基，即
$$
\int_{\mathcal{X}}\phi_i(x)\phi_j(x)dx=\delta_{ij}=
\begin{cases}
1&i=j\\
0&i\ne j\\
\end{cases}
$$
可通过求解积分特征方程：
$$
\int_{\mathcal{X}}K(x,y)\phi_i(y)dy=\lambda_i \phi_i(x)
$$

## 可逆矩阵的奇异值分解

$$
A=\sum_{i=1}^ns_iu_iv_i^T\\
A^{-1}=\sum_{i=1}^n{1\over s_i}v_iu_i^T
$$

## 广义逆

$$
AA^+A=A\\
A^+AA^+=A^+\\
(AA^+)^*=AA^+\\
(A^+A)^*=A^+A
$$

最小奇异值
$$
s_n(A)={1\over \|A^+\|}
$$

## 矩阵范数

对于任意矩阵$A$的奇异值$s_i$，
$$
s_i\le {1\over \sqrt{i}}\|A\|_F
$$
低秩近似的最佳结果是截断奇异值矩阵

极端奇异值$s_1(A)$和$s_n(A)$满足不等式
$$
s_n(A)\|x\|_2\le \|Ax\|_2\le s_1(A)\|x\|_2,\forall x\in \R^n
$$
能精确地保持距离地最佳矩阵被称为等距。

## 分离集与网

设$\mathcal{N}$是$K$的一个极大$\varepsilon$-分离子集，则$\mathcal{N}$是$K$的一个$\varepsilon$-网。

## 覆盖数与填充数

对于任何集合$K\sub T$和任意$\varepsilon>0$，我们有
$$
\mathcal{P}(K,d,2\varepsilon)\le \mathcal{N}(K,d,\varepsilon)\le \mathcal{P}(K,d,\varepsilon)
$$

## 覆盖数和体积

设$K$是$\R^n$的子集，$\varepsilon>0$，则有
$$
{|K|\over |\varepsilon B_2^n|}\le \mathcal{N}(K,\varepsilon)\le \mathcal{P}(K,\varepsilon)\le {\left|\left(K+{\varepsilon\over 2}B_2^n \right) \right|\over\left| {\varepsilon\over 2}B_2^n\right|}\\
|\cdot|:\R^n中的体积\\
B_2^n:\R^n中的单位欧几里得球\\
\varepsilon B_2^n:以\varepsilon为半径的欧几里得球\\
$$

## 欧几里得球的覆盖数

对于任何$\varepsilon>0$，单位欧几里得球$B_2^n$的覆盖数满足
$$
\left({1\over \varepsilon} \right)^n \le \mathcal{N}(B_2^n, \varepsilon)\le \left({2\over \varepsilon}+1 \right)^n
$$

## 汉明立方体的覆盖数和填充数

设$K=\{0,1\}^n$，对每个整数$m\in [0,n]$，有
$$
{2^n\over \sum_{k=0}^m\binom{n}{k}}\le \mathcal{N}(K,d_H,m)\le \mathcal{P}(K,d_H,m)\le {2^n\over \sum_{k=0}^{\lfloor m/2 \rfloor}\binom{n}{k}}
$$

## $\varepsilon$-网与算子范数

设A是一个$m\times n$矩阵，$\varepsilon\in [0,1)$，那么对于球面$S^{n-1}$上任何一个$\varepsilon$-网$\mathcal{N}$，我们有
$$
\sup_{x\in \mathcal{N}}\|Ax\|_2\le \|A\|\le {1\over 1-\varepsilon}\sup_{x\in \mathcal{N}}\|Ax\|_2\\
$$
网的二次型：对于球面上$S^{n-1}$上任意$\varepsilon$-网$\mathcal{N}$以及球面$S^{m-1}$上任意$\varepsilon$-网，我们有
$$
\sup_{x\in \mathcal{N},y\in \mathcal{M}}\lang Ax, y\rang \le \|A\| \le {1\over 1-2\varepsilon}\sup_{x\in \mathcal{N},y\in \mathcal{M}} \lang Ax,y\rang\\
$$

## 带有次高斯元素矩阵的范数

设A为一个$m\times n$随机矩阵，它的每一个元素项$A_{ij}$都是独立的、零均值的次高斯随机变量，对于任意$t>0$，我们有
$$
\|A\|\le CK(\sqrt{m}+\sqrt{n}+t)\\
$$
成立的概率至少为$1-2\exp(-t^2)$，其中$K=\max_{ij}\|A\|_{\psi_2}$

范数的期望
$$
\mathbb{E}\|A\|\le CK(\sqrt{m}+\sqrt{n})\\
$$
假设矩阵元素$A_{ij}$有单位方差，则具有
$$
\mathbb{E}\|A\|\ge C(\sqrt{m}+\sqrt{n})\\
$$
设A为一个$n\times n$随机矩阵，它的每一个元素项$A_{ij}$都是独立的、零均值的次高斯随机变量，对于任意$t>0$，我们有
$$
\|A\|\le CK(\sqrt{n}+t)\\
$$
成立的概率至少为$1-4\exp(-t^2)$，其中$K=\max_{ij}\|A_{ij}\|_{\psi_2}$

## Weyl不等式

对任意具有相同维数的对称矩阵$S$和$T$，均有\
$$
\max_i |\lambda_i(S)-\lambda_i(T)|\le \|S-T\|
$$

## Davis-Kahan定理

设$S$和$T$是具有相同维数的对称矩阵。固定$i$，并且假定$S$的第$i$个最大的特征值和谱中其他特征值是完全分离的：
$$
\min_{j;j\ne i}|\lambda_i(S)-\lambda_j(S)|=\delta>0\\
$$
则矩阵S和T中第i个最大的特征值分别对应的特征向量，其夹角满足：
$$
\sin(v_i(S),v_i(T))\le {2\|S-T\|\over \delta}
$$

## 次高斯矩阵的双侧界

若$A$是$m\times n$矩阵，且各行$A_i$是$\R^n$中独立的、零均值的次高斯各向同性随机向量，那么对任意$t\ge 0$，有
$$
\sqrt{m}-CK^2(\sqrt{n}+t)\le s_n(A)\le s_1(A)\le \sqrt{m}+CK^2(\sqrt{n}+t)\\
$$
成立的概率至少为$1-2\exp(-t^2)$，其中$K=\max_i\|A_i\|_{\psi_2}$
$$
\left\| {1\over m}A^TA-I_n \right\|\le K^2 \max (\delta, \delta^2),\delta = C\left(\sqrt{n\over n}+{t\over \sqrt{m}} \right)
$$

## 协方差估计

设$X$是$\R^n$中的次高斯随机向量，假设存在$K\ge 1$，使得
$$
\|\lang X,x\rang\|_{\psi_2}\le K\|\lang X,x\rang\|_{L^2},\forall x\in \R^n\\
$$
对于每一个整数$m$，必有
$$
\mathbb{E}\|\Sigma_m-\Sigma\|\le CK^2 \left(\sqrt{n\over m}+{n\over m} \right)\|\Sigma\|
$$

## 样本复杂度

对于任意$\varepsilon \in (0,1)$，如果取一个容量为m($m\sim \varepsilon^{-2}n$)的样本，n为维度。能保证协方差估计有一个与$\varepsilon$有关的较好误差。
$$
\mathbb{E}\|\Sigma_m-\Sigma\|\le \varepsilon\|\Sigma\|
$$
对于任意$u\ge 0$，
$$
\|\Sigma_m-\Sigma\|\le CK^2\left(\sqrt{n+u\over m}+{n+u\over m} \right)\|\Sigma\|
$$
成立的概率至少为$1-2e^{-u}$

## 球面上利普希茨函数的集中

考虑随机向量$X\sim Unif(\sqrt{n}S^{n-1})$，考虑$f:\sqrt{n}S^{n-1}\to \R$，有
$$
\|f(X)-\mathbb{E}f(X)\|_{\psi_2}\le C\|f\|_{Lip}
$$

## 等周不等式

对于所有给定体积的子集$A\sub \R^n$中，欧几里得球体具有最小的表面积

## 放大

设A为球面$\sqrt{n}S^{n-1}$的子集，$\sigma$表示该球面上的测度面积，如果有$\sigma(A)\ge {1\over 2}$，那么对任意的$t\ge 0$，必有
$$
\sigma(A_t)\ge 1-\exp(-ct^2)
$$

## 高维空间的零一律

在某些高维概率空间中，事件发生的概率要么是0，要么是1，没有中间值。

## 集中和放大等价

考虑在度量空间$(T,d)$中的取值随机向量$X$。假设存在常数$K>0$，使得：
$$
\|f(X)-\mathbb{E}f(X)\|_{\psi_2}\le K\|f\|_{Lip}
$$

## 高斯集中不等式

对任一随机向量$X\sim N(0,I_n)$及其利普希茨$f:\R^n\to \R$，必有
$$
\|f(X)-\mathbb{E}f(X)\|_{\psi_2}\le C\|f\|_{Lip}
$$

## 汉明立方体集中

考虑随机向量$X\sim Unif\{0,1\}^n$，及函数$f:\{0,1\}^n\to \R$，有
$$
\|f(X)-\mathbb{E}f(X)\|_{\psi_2}\le {C\|f\|_{Lip}\over \sqrt{n}}
$$

## Johnson-Lindenstrauss引理

设$\mathcal{X}$为$\R^n$中N个点的集合，$\varepsilon>0$，假设$m\ge {C\over \varepsilon^2}\log N$，考虑$\R^n$中一个随机m维子空间$E$，它在$G_{n,m}$上服从均匀分布，用P表示$\R^n$到E上的正交投影，那么缩放后的投影$Q:=\sqrt{n\over m}P$为$\mathcal{X}$上的近似等距，即
$$
(1-\varepsilon)\|x-y\|_2\le \|Qx-Qy\|_2\le (1+\varepsilon)\|x-y\|_2,\forall x,y \in \mathcal{X}
$$

## 随机投影

设$P$为$\R^n$到均匀分布在$G_{n,m}$中的随机$m$维子空间上的投影，设$z\in \R^n$为定点，$\varepsilon>0$，则有\
$$
\left(\mathbb{E}\|Pz\|_2^2 \right)^{1\over 2}=\sqrt{m\over n}\|z\|_2\\
$$

$$
(1-\varepsilon)\sqrt{m\over n}\|z\|_2 \le \|Pz\|_2\le (1+\varepsilon)\sqrt{m\over n}\|z\|_2
$$

成立的概率至少为$1-2\exp(-c\varepsilon^2m)$

## 矩阵伯恩斯坦不等式

设$X_1,\cdots,X_N$为独立的、零均值的$n\times n$对称随机矩阵，且$\|X_i\|\le K$对所有$i$几乎处处成立。那么，对所有$t\ge 0$，必有
$$
\mathbb{P} \left\{\left\|\sum_{i=1}^N X_i \right\|\ge t \right\}\le 2n\exp \left(-{t^2/2\over \sigma^2+Kt/3} \right)
$$

## Golden-Thompson不等式

对任何$n\times n$对称矩阵$A$和$B$，有
$$
tr(e^{A+B})\le tr(e^Ae^B)
$$

## Lieb不等式

设$H$是一个$n\times n$对称矩阵，在矩阵上定义函数
$$
f(X):=tr\exp (H+\log X)\\
$$
那么$f$在$n\times n$正定对称矩阵空间上是凹的。

设$H$为一个$n\times n$固定的对称矩阵，$Z$为一个随机$n\times n$对称矩阵，那么
$$
\mathbb{E}tr\exp(H+Z)\le tr \exp(H+\log \mathbb{E}e^Z)
$$

## 矩母函数

设$X$是$n\times n$零均值的随机对称矩阵，并且$\|X\|\le K$几乎处处成立，那么
$$
\exp (g(\lambda)\mathbb{E}X^2)\succeq \mathbb{E}\exp (\lambda X),g(\lambda)={\lambda^2/2\over 1-|\lambda|K/3},
$$
当$|\lambda|<{3\over K}$时成立。

## 矩阵不等式

### 伯恩斯坦不等式

设$X_1,\cdots,X_N$是$n\times n$独立的、零均值的随机对称矩阵，并且对所有的$i$，$\|X_i\|\le K$几乎处处成立，有
$$
\mathbb{E}\left\|\sum_{i=1}^N X_i \right\| \lesssim \left\|\sum_{i=1}^N \mathbb{E}X_i^2 \right\|^{1\over 2}\sqrt{\log n}+K\log n
$$

### 霍夫丁不等式

设$\varepsilon_1,\cdots, \varepsilon_n$是独立的对称伯努利随机变量，$A_1,\cdots,A_N$是$n\times n$对称矩阵，对任意$t\ge 0$，有
$$
\mathbb{P}\left\{\left\| \sum_{i=1}^N \varepsilon_i A_i \right\| \ge t \right\} \le 2n \exp\left(-{t^2\over 2\sigma^2} \right)
$$

### Khintchine不等式

设$\varepsilon_1,\cdots, \varepsilon_N$是独立的对称伯努利随机变量，$A_1,\cdots,A_N$是$n\times n$对称矩阵。
$$
\mathbb{E}\left\|\sum_{i=1}^N \varepsilon_i A_i \right\| \le C\sqrt{\log n} \left\|\sum_{i=1}^N A_i^2 \right\|^{1\over 2}
$$
对每一个$p\in [1,\infty)$
$$
\left(\mathbb{E}\left\|\sum_{i=1}^N \varepsilon_i A_i \right\|^p \right)^{1\over p} \le C \sqrt{p+\log n}\left\| \sum_{i=1}^N A_i^2 \right\|^{1\over 2}
$$

### 矩阵伯恩斯坦不等式

设$X_1,\cdots, X_N$是独立的零均值$m\times n$随机矩阵，并对所有的$i$，$\|X_i\| \le K$几乎处处成立，对$t\ge 0$，有
$$
\mathbb{P}\left\{\left\| \sum_{i=1}^N X_i \right\|\ge t \right\} \le 2(m+n)\exp \left(-{t^2/2\over \sigma^2+Kt/3} \right)\\
\sigma^2 =\max \left(\left\|\sum_{i=1}^N \mathbb{E}X_i^TX_i \right\|, \left\|\sum_{i=1}^N \mathbb{E}X_iX_i^T \right\| \right)
$$

## 一般协方差估计

设$X$是$\R^n$中的一个随机变量，假定对某个$K\ge 1$，有
$$
\|X\|_2 \le K(\mathbb{E}\|X\|_2^2)^{1\over 2}几乎处处
$$
对任意正整数$m$，必有
$$
\mathbb{E}\|\Sigma_m-\Sigma\|\le C\left(\sqrt{K^2 n\log n\over m}+{K^2n \log n\over m} \right)\|\Sigma\|
$$

## 混沌的期望

$$
\mathbb{E}X^TAX=\text{tr}A
$$

## Hanson-Wright不等式

设$X=(X_1,\cdots, X_n)\in \R^n$是一个具有独立零均值的次高斯坐标的随机向量，$A$是一个$n\times n$的矩阵，则对任意$t\ge 0$，有
$$
\mathbb{P}\{|X^TAX-\mathbb{E}X^TAX| \ge t \} \le 2 \exp \left(-c \min \left({t^2\over K^4\|A\|_F^2},{t\over K^2\|A\|} \right) \right)
$$

## 高斯混沌的矩母函数

设$X,X'\sim N(0,I_n)$相互独立，$A=(a_{ij})$是一个$n\times n$矩阵，则有
$$
\mathbb{E}\exp(\lambda X^TAX')\le \exp(C\lambda^2 \|A\|_F^2)\\
$$
对所有满足$|\lambda|\le {c\over\|A\|}$

## 各向异性随机向量的集中

设$B$是一个$m\times n$矩阵，$X=(X_1,\cdots,X_n)\in \R^n$是$\R^n$中的一个随机向量，其坐标为独立零均值单位方差的次高斯分布，那么有\
$$
\left\| \|BX\|_2-\|B\|_F\right\|_{\psi_2} \le CK^2\|B\|\\
K=\max_i \|X_i\|_{\psi_2}\\
$$

## 与子空间的距离

设$E$是$\R^n$中的一个$d$维子空间，考虑随机向量$X=(X_1,\cdots,X_n)\in \R^n$，它有独立零均值单位方差的次高斯分布坐标，有
$$
(\mathbb{E}d(X,E)^2)^{1\over 2}=\sqrt{n-d}\\
$$
对每一个$t\ge 0$，距离集中
$$
\mathbb{P}\{\|d(X,E)-\sqrt{n-d}|>t \}\le 2\exp \left(-{ct^2\over K^4} \right)
$$

## 建立对称分布

设$X$是一个随机变量，$\xi$是一个独立对称的伯努利随机变量。

$\xi X$和$\xi|X|$是对称随机变量。

设$X'$是$X$的一个独立副本，$X-X'$是对称的。

## 对称化

用$\varepsilon_1,\varepsilon_2,\cdots, \varepsilon_3,\cdots$表示一列独立对称的伯努利随机变量。

设$X_1,\cdots,X_N$为赋范空间中的独立零均值随机变量，有
$$
{1\over 2}\mathbb{E}\left\|\sum_{i=1}^N \varepsilon_i X_i \right\| \le \mathbb{E}\left\|\sum_{i=1}^N  X_i \right\|\le 2\mathbb{E}\left\|\sum_{i=1}^N \varepsilon_i X_i \right\|
$$

## 非独立同分布的随机变量范数

设$A$是一个$n\times n$对称随机矩阵，其主对角线上及其上的元素为独立零均值的随机变量，那么有
$$
\mathbb{E}\|A\|\le C\sqrt{\log n}\mathbb{E}\max_i \|A_i\|_2\\
A_i:A的行\\
$$

## 矩阵补全

设$\hat{X}$是$p^{-1}Y$的秩为$r$的最佳近似矩阵，那么只要$m\ge n\log n$，就有
$$
\mathbb{E}{1\over n}\|\hat{X}-X\|_F\le C\sqrt{rn\log n\over m}\|X\|_{\infty}\\
\|X\|_{\infty}=\max_{i,j}|X_{ij}|是X中元素的最大值
$$

## 压缩原理

设$x_1,\cdots,x_N$是某个赋范空间上的向量，$a=(a_1,\cdots,a_n)\in \R^n$，有
$$
\mathbb{E}\left\|\sum_{i=1}^N a_i \varepsilon_i x_i \right\| \le \|a\|_\infty \mathbb{E}\left\| \sum_{i=1}^N \varepsilon_i x_i \right\|
$$
一般分布上，设$x_1,\cdots,x_N$是某个赋范空间上的独立零均值的随机向量，$a=(a_1,\cdots,a_n)\in \R^n$，有
$$
\mathbb{E}\left\| \sum_{i=1}^N a_i X_i \right\| \le 4 \|a\|_\infty \mathbb{E}\left\|\sum_{i=1}^N X_i \right\|
$$

## 高斯对称化

设$X_1,\cdots, X_N$为某个赋范空间上的独立零均值随机向量，$g_1,\cdots, g_N \sim N(0,1)$为独立高斯随机变量，它们也与$X_i$独立，有
$$
{c\over \sqrt{\log N}}\mathbb{E}\left\|\sum_{i=1}^N g_i X_i \right\| \le \mathbb{E}\left\| \sum_{i=1}^N X_i \right\|\le 3 \mathbb{E}\left\|\sum_{i=1}^N g_i X_i \right\|
$$

## Talagrand压缩原理

考虑有界子集$T\sub \R^n$，设$\varepsilon_1,\cdots, \varepsilon_n$是独立的对称伯努利随机变量，设$\phi_i:\R\to \R$是压缩的，即它是利普希茨函数，且$\|\phi_i\|_{Lip}\le 1$，有
$$
\mathbb{E}\sup_{t\in T}\sum_{i=1}^n \varepsilon_i \phi_i(t_i)\le \mathbb{E}\sup_{t\in T}\sum_{i=1}^n \varepsilon_i t_i
$$

## Slepian不等式

设$(X_t)_{t\in T}$和$(Y_t)_{t\in T}$是两个零均值高斯过程，假定对于所有的$t,s \in T$，均有
$$
\mathbb{E}X_t^2 =\mathbb{E}Y_t^2, \mathbb{E}(X_t-X_s)^2\le \mathbb{E}(Y_t-Y_s)^2\\
$$
那么，对所有$\tau \in \R$，我们有
$$
\mathbb{P}\left\{\sup_{t\in T}X_t \ge \tau \right\}\le\mathbb{P}\left\{\sup_{t\in T}Y_t \ge \tau \right\}\\
\mathbb{E}\sup_{t\in T}X_t \le \mathbb{E}\sup_{t\in T}Y_t
$$

## Sudakov-Fernique不等式

设$(X_t)_{t\in T}$和$(Y_t)_{t\in T}$是两个零均值高斯过程，假定对于所有的$t,s \in T$，均有
$$
\mathbb{E}(X_t-X_s)^2\le \mathbb{E}(Y_t-Y_s)^2\\
$$
那么
$$
\mathbb{E}\sup_{t\in T}X_t \le \mathbb{E}\sup_{t\in T}Y_t
$$

## 高斯矩随机矩阵的范数

设$A$是一个具有独立的服从$N(0,1)$分布元素的$m\times n$，那么
$$
\mathbb{E}\|A\|\le \sqrt{m}+\sqrt{n}
$$

## Sudakov最小值不等式

设$(X_t)_{x\int T}$是一个零均值的高斯过程，那么，对任意的$\varepsilon\ge 0$，有\
$$
\mathbb{E}\sup_{t\in T}X_t \ge c\varepsilon \sqrt{\log \mathcal{N}(T,d,\varepsilon)}  5
$$

## 稳定维数

对于任意子集$T\sub \R^n$，我们有d
$$
d(T)\le \text{dim}(T)\\
$$

## 集合的随机投影大小

考虑$\R^n$中有界集合T，设P为$\R^n$到一个随机m维子空间$E\sim \text{Unif}(G_{n,m})$上的投影，则
$$
\text{diam}(PT)\le C(w_s(T)+\sqrt{m\over n}\text{diam}(T))
$$
成立的概率至少为$1-2e^{-m}$

## 随机投影的等价模型

设$P$为$\R^n$到一个随机$m$维子空间$E\sim \text{Unif}(G_{n,m})$的投影，$Q$是一个$m\times n$矩阵，它是均匀地从正交群中选取一个随机的$n\times n$矩阵$U\sim \text{Unif}(O_{(n)})$，然后取$U$的前$m$行得到的。

对$\R^n$中的任意固定点$x$，有
$$
\|Px\|_2与\|Qx\|_2同分布
$$
对$S^{m-1}$中的任意固定点$z$，有
$$
Q^Tz\sim \text{Unif}(S^{n-1})
$$

## Dudley积分不等式

设$(X_t)_{t\in T}$为定义的度量空间$(T,d)$上有次高斯增量的零均值随机过程，则有
$$
\mathbb{E}\sup_{t\in T}X_t \le CK \int_0^\infty \sqrt{\log \mathcal{N}(T,d,\varepsilon)}\text{d}\varepsilon
$$
离散形式
$$
\mathbb{E}\sup_{t\in T}X_t \le CK \sum_{k\in \mathbb{Z}}2^{-k}\sqrt{\log \mathcal{N}(T,d,2^{-k})}
$$

## 双侧Sudakov不等式

设$T\sub \R^n$，令
$$
s(T):\sup_{\varepsilon \ge 0}\varepsilon\sqrt{\log \mathcal{N}(T,\varepsilon)}
$$
则有
$$
cs(T)\le w(T)\le C\log (n)s(T)\\
$$

## 一致大数定律

设$X,X_1,X_2,\cdots,X_n$是在$[0,1]$中取值的独立同分布的随机变量，则有
$$
\mathbb{E}\sup_{f\in \mathcal{F}}\left| {1\over n}\sum_{i=1}^n f(X_i)-\mathbb{E}f(X) \right|\le {CL\over \sqrt{n}}
$$

## Kantorovich-Rubinstein对偶定理

Kantorovich最优传输问题，设$X,Y$是Polish空间（比如$\R^d$），$\mu\in \mathcal{P}(X)$，$v\in \mathcal{P}(Y)$是两个概率测度，$c(x,y)$是传输成本函数，Kantorovich问题（Monge-Kantorovich松弛）是
$$
W_c(\mu,v)=\inf _{\pi \in \Pi(\mu,v)}\int_{X\times Y}c(x,y)d\pi(x,y)
$$
$\Pi(\mu,v)$是以$\mu$和$v$为边缘分布的耦合测度集合。

当成本函数$c(x,y)=d(x,y)$是一个度量空间$(M,d)$上的距离，并且考虑$p=1$时，有对偶公式：
$$
W_1(\mu,v)=\sup_{f\in\text{Lip}_1}\left[\int_M fd\mu-\int_M fdv \right]\\
$$
其中Lip$_1$是Lipschitz常数不超过1的函数$f:M\to \R$的集合。

## Pajor引理

令$\mathcal{F}$为有限集合$\Omega$上的布尔函数类，则
$$
\|\mathcal{F}\|\le |\{\Lambda \sube \Omega:\Lambda被\mathcal{F}散离 \}|
$$
计数上式右边时，包含了空集$\Lambda=\empty$

## Sauer-Shelah引理

设$\mathcal{F}$是$n$点集$\Omega$上的一个布尔函数类，则有
$$
|\mathcal{F}| \le \sum_{k=0}^d \binom{n}{k}\le \left({en\over d} \right)^d\\
$$

## 覆盖数与VC维数

设$\mathcal{F}$是概率空间$(\Omega,\Sigma,\mu)$上的一个布尔函数类，那么，对于每个$\varepsilon \in (0,1)$，必有\
$$
\mathcal{N}(\mathcal{F},L^2(\mu),\varepsilon)\le \left({2\over \varepsilon} \right)^{Cd}
$$

## 降维引理

设$\mathcal{F}$是概率空间$(\Omega,\Sigma,\mu)$上的一个N个布尔函数的类，假设$\mathcal{F}$中的所有函数都是$\varepsilon$-可分的，即
$$
\|f-g\|_{L^2(\mu)}>\varepsilon,\ \ 对所有可分f,g \in \mathcal{F}
$$
那么存在数$n\le C\varepsilon^{-4}\log N$和$n$点子集$\Omega_n\sub \Omega$，使得$\Omega_n$上的均匀概率测度$\mu_n$满足
$$
\|f-g\|_{L^2(\mu_n)}>{\varepsilon \over 2}, \ \ 对所有可分f,g\in \mathcal{F}
$$

## 经验过程与VC维数

设$\mathcal{F}$是概率空间$(\Omega, \Sigma, \mu)$上的具有有限VC维数$vc(\mathcal{F})\ge 1$的一类布尔函数。设$X,X_1,X_2,\cdots,X_n$是$\Omega$上分布由$\mu$确定的独立随机点，那么\
$$
\mathbb{E}\sup_{f\in \mathcal{F}} \left|{1\over n}\sum_{i=1}^n f(X_i)-\mathbb{E}f(X) \right|\le C\sqrt{vc(\mathcal{F})\over n}
$$

## Glivenko-Canteli定理

设$X_1,\cdots,X_n$是具有共同累积分布函数$F$的独立随机变量，$F_n(x)$是一个随机函数
$$
F_n(x):={|\{i\in [n]:X_i\le x \}\over n},x\in \R\\
$$
那么
$$
\mathbb{E}\|F_n-F\|_\infty = \mathbb{E}\sup_{x\in \R}|F_n(x)-F(x)|\le {C\over \sqrt{n}}
$$

## 额外风险与VC维

设目标T是一个布尔哈数，假设空间$\mathcal{F}$是具有有限VC维数$(vc(\mathcal{F})\ge 1)$的布尔函数类，则有\
$$
\mathbb{E}R(f_n^*)\le R(f^*)+C\sqrt{\text{vc}(\mathcal{F})\over n}
$$

## 额外风险与一致偏差

$$
R(f_n^*)-R(f^*)\le 2\sup_{f\in \mathcal{F}}|R_n(f)-R(f)|
$$

## 通用链界

设$(X_t)_{t\in T}$是度量空间$(T,d)$上的零均值随机过程，且有次高斯增量，那么
$$
\mathbb{E}\sup_{t\in T}X_t \le CK\gamma_2(T,d)
$$

## Talagrand优化测度定理

设$(X_t)_{t\in T}$为集合$T$上的零均值高斯过程，考虑在$T$上定义的典范度量，即$d(s,t)=\|X_t-X_s\|_2$，那么
$$
c\gamma_2 (T,d)\le \mathbb{E}\sup_{t\in T} X_t \le C \gamma_2 (T,d)
$$

## 次高斯Chevet不等式

设$A$是一个$m\times n$矩阵，其元素$A_{ij}$是独立的零均值次高斯随机变量。设$T\sub \R^n$和$S\sub \R^m$是任意的有界集，那么，有
$$
\mathbb{E}\sup_{x\in T,y\in S}\lang Ax,y\rang \le CK(w(T)\text{rad}(S)+w(S)\text{rad}(T))\\
\text{rad}(T):=\sup_{x\in T}\|x\|_2\\
K=\max_{ij}\|A_{ij}\|_{\psi_2}
$$

## 矩阵偏差不等式

设$A$为$m\times n$矩阵，其行向量$A_i$是$\R^n$中独立的、各向同性的次高斯随机向量，则对任意子集$T\sub \R^n$，有
$$
\mathbb{E}\sup_{x\in T}| \|Ax\|_2-\sqrt{m}\|x\|_2|\le CK^2 \gamma (T)\\
\gamma(T):高斯复杂度\\
K=\max_i \|A_i\|_{\psi_2}
$$

## 次高斯增量

设$A$为$m\times n$矩阵，其行向量$A_i$是$\R^n$中独立的、各向同性的次高斯随机向量，则随机过程
$$
X_x:=\|Ax\|_2 -\sqrt{m}\|x\|_2\\
$$
有次高斯增量，即
$$
\|X_x-X_y\|_{\psi_2}\le CK^2 \|x-y\|_2,\ \ \for x,y\in \R^n\\
K=\max_i \|A_i\|_{\psi_2}
$$

## 单位向量，原始过程

设$x,y\in S^{n-1}$，则有
$$
\left\|\|Ax\|_2-\|Ay\|_2 \right\|_{\psi_2}\le CK^2 \|x-y\|_2
$$

## 低维分布的协方差估计

设$X$为$\R^n$中的次高斯随机向量。假设存在$K\ge 1$，使得
$$
\|\lang X,x\rang\|_{\psi_2}\le K\|\lang X,x\rang\|_{L^2},\forall x\in \R^n
$$
那么，对于每个正整数$m$，我们有\
$$
\mathbb{E}\|\Sigma_m-\Sigma\|\le CK^4 (\sqrt{r\over m}+{r\over m})\|\Sigma\|\\
r=\text{tr}{\Sigma\over \|\Sigma\|}是\Sigma^{1\over 2}的稳定秩
$$

## 无限集上的Johnson-Lindenstrauss引理

考虑集合$\mathcal{X}\sub \R^n$。假设$A$是一个$m\times n$矩阵，其行向量$A_i$是$\R^n$中独立的、各向同性的次高斯随机向量。那么在高概率（比如0.99）下，缩放矩阵
$$
Q:={1\over \sqrt{m}}A\\
$$
满足
$$
\|x-y\|_2-\delta \le \|Qx-Qy\|_2\le \|x-y\|_2+\delta, \forall x,y \in \mathcal{X}\\
\delta ={CK^2 w(\mathcal{X})\over \sqrt{m}}\\
K=\max_i \|A_i\|_{\psi_2}
$$

## 逃逸定理

考虑集合$T\sub S^{n-1}$，设$A$是$m\times n$维矩阵，其行向量$A_i$是$\R^n$中独立的、各向同性的次高斯随机向量，如果
$$
m\ge CK^4 w(T)^2
$$
则随机子空间$E=\text{ker} A$满足
$$
T\cap E=\empty
$$
的概率至少是$1-2\exp(-{cm\over K^t})$，这里$K=\max_i \|A_i\|_{\psi_2}$

## 基于M*界的信号恢复

考虑无噪声形式:
$$
y=Ax, x\in T\\
$$
假设A的行$A_i$是独立的、各向同性的次高斯随机向量，那么上述信号恢复的解满足$\hat{x}$满足
$$
\mathbb{E}\|\hat{x}-x\|_2\le {CK^2 w(T)\over \sqrt{m}}
$$
其中$K=\max_i \|A_i\|_{\psi_2}$

## 精确稀疏恢复

设$A$的行$A_i$是独立的、各向同性的次高斯随机向量，并令$K:=\max_i \|A_i\|_{\psi_2}$，则下面事件发生的概率至少为$1-2\exp \left(-{cm\over k^4}\right)$：m

假设一个未知信号$x\in \R^n$是$s-$稀疏的，且测量次数$m$满足
$$
m \ge CK^4 s\log n\\
$$
则稀疏恢复的解$\hat{x}$是精确的。

## RIP蕴含精确恢复

设一个$m\times n$矩阵A关于参数$\alpha, \beta和(1+\lambda)s$满足RIP，其中$\lambda >\left({\beta\over \alpha}\right)^2$，则每个$s$-稀疏向量$x\in \R^n$都能通过解问题被精确恢复。

## 一般矩阵偏差不等式

设$A$为一个$m\times n$高斯随机矩阵，其元素是独立同分布的$N(0,1)$分布，$f:\R^m \to \R$为一个正齐次和次可加的函数，并且设$b\in \R$满足
$$
f(x)\le b\|x\|_2,\forall x \in \R^m\\
$$
那么，对任意的子集$T\sub \R^n$，有
$$
\mathbb{E}\sup_{t\in T}|f(Ax)-\mathbb{E}f(Ax)|\le Cb\gamma(T)\\
\gamma(T):高斯复杂度\\
$$

## 次高斯增量

设$A$为一个$m\times n$高斯随机矩阵，其元素是独立同分布的$N(0,1)$分布，并设$f:\R^m\to \R$是一个正齐次和次可加的函数，那么随机过程
$$
X_x:=f(Ax)-\mathbb{E}f(Ax)\\
$$
相对于欧几里得范数具有次高斯增量，即
$$
\|X_x-X_y\|_{\psi_2}\le Cb\|x-y\|_2,\forall x,y \in \R^n
$$

## Johnson-Lindenstrauss嵌入

> 给定一个高维空间 \( \mathbb{R}^d \) 中的一组点（有限集合），可以通过一个随机线性映射将它们嵌入到低维空间 \( \mathbb{R}^k \) 中，其中 \( k \) 仅依赖于点的个数 \(n\) 和对精度的要求 \(\varepsilon\)，而与原始维度 \(d\) 无关，并且能够以很高的概率**保持点之间的欧氏距离**（在 \( 1 \pm \varepsilon \) 因子内）。

设 \( P \subset \mathbb{R}^d \) 是 \( n \) 个点的集合，给定 \( 0 < \varepsilon < 1 \)，存在整数 \( k = O(\varepsilon^{-2} \log n) \) 和一个线性映射 \( f: \mathbb{R}^d \to \mathbb{R}^k \)，使得对所有 \( u, v \in P \)：

\[
(1 - \varepsilon) \|u - v\|^2 \leq \|f(u) - f(v)\|^2 \leq (1 + \varepsilon) \|u - v\|^2
\]

这里 \( \|\cdot\| \) 表示欧几里得范数。

## 一般的Chevet不等式

设$A$是一个$m\times n$的高斯随机矩阵，它的元素是独立同分布的$N(0,1)$分布，令$T\sub \R^n$和$S\sub \R^m$是任意有界集，那么
$$
\mathbb{E}\sup_{x\in T}|\sup_{y\in S}\lang Ax,y\rang -w(S)\|x\|_2|\le C\gamma(T)\text{rad}(S)\\
$$

## 集合的随机投影

设$A$是一个$m\times n$高斯随机矩阵，其元素是独立同分布的$N(0,1)$分布，并设$T\sub \R^n$是一个有界集，那么，下列结论成立的概率至少为0.99：
$$
r\_ B_2^m \sub \text{conv}(AT)\sub r_+B_2^m\\
r_\pm :=w(T)\pm C\sqrt{m}\text{rad}(T)\\
$$

## Dvoretzky-Milman定理

设$A$是一个$m\times n$高斯随机矩阵，其元素是独立同分布的$N(0,1)$分布，并设$T\sub \R^n$为一个有界集，令$\varepsilon \in (0,1)$。假定
$$
m\le c\varepsilon^2 d(T)\\\
$$
其中，$d(T)$为T的稳定维数，那么
$$
(1-\varepsilon)B \sub \text{conv}(AT)\sub (1+\varepsilon)B\\
$$
成立的概率至少为0.99，其中$B$是一个半径为$w(T)$的欧几里得球。

如果$m\gtrsim d(T)$，$\R^n$中结合$T$zai1多维子空间上的随机投影近似保持$T$的几何形状不变，对于较小的$m$，投影集$PT$近似于一个直径为$w_s(T)$的圆球，并且它的大小不会随着$m$变小而收缩。
